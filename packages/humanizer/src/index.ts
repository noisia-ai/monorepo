// TODO mejora-futura: portar el skill humanizer real en F3.7 para validar
// y limpiar textos generados por IA antes de persistir findings.
export function humanizeCopy(input: string) {
  return input;
}
