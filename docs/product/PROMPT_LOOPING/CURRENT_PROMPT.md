> **PAUSA EXPRESA DEL OPERADOR — 2026-09-10T03:23:01.164529+00:00.** Loop `noisia-topics-to-signal-uat-loop` PAUSED por app y verificado en este chat. No reanudar desarrollo, automatización ni proveedores hasta nueva instrucción. UAT1a609cb/SQL0152 y NOI-82 entregados intactos. NOI-19 sólo LOCAL en `noisia-client-workspace-entry-2026-09-10`, base1a609cb más cambios sin commit: Backend8/PG6/Root10 aprobados, UI/cierre global/commit/entrega pendientes. Leer `SAFE_PAUSE_CLIENT_ENTRY_2026-09-10.md`. Cero permisos/gasto/remoto nuevo; última evidencia UAT sigue02:52 y UI02:59. Conservar Compass, historia y drafts; no repetir SQL/gates/imports/fit.

> **NOI-19 en desarrollo LOCAL — 2026-09-10T03:18:09.328547+00:00.** Checkout aislado `noisia-client-workspace-entry-2026-09-10`, rama `codex/noisia-client-workspace-entry-2026-09-10`, base UAT1a609cb. Backend: acceso DB y selección sin ejecución; Root: loaders/rutas scoped/DTO; Frontend: navegación y componentes reutilizados, limpieza al revocar; Import: PG compuesto y revisión. PG focal6/6 y Root10focal PASS; UI/checks globales/commit pendientes. APIs Topics/import ya estaban scoped: no retirar controles inexistentes por una tabla vieja. SQL0152/UAT anterior intactos; sin migración nueva ni permisos/proveedores reales. Leer `PLAN_CLIENT_WORKSPACE_ENTRY_2026-09-10.md`. LoopACTIVE en este chat; Compass e historia conservados.

> **Renovación incremental entregada UAT — 10 septiembre 03:00 UTC / 9 septiembre México.** NOI-82 cerrado en `1a609cb0cd1b24c661818e37bc65dcdfb3303b59`: Studio/Worker activos, anteriores retirados, SQL0152 aplicado una sola vez y verificado. Leer `DELIVERY_INCREMENTAL_PERMISSION_RENEWAL_2026-09-10.md`. Renovar permiso vencido/revocado conserva owner, resultados pagados y topes, con admisión/reanudación atómica y UI existente. PG/Worker con HTTP simulado, suites/build/13 DOM/revisiones cerrados sin P0/P1/P2; no repetir gates ni SQL. Recibo remoto 02:52:42.201Z y UI posterior: 32 Topics/32 de 357, dos seleccionados/98 en Signal, 6,826 raíces; costos/selección/generación intactos, actividad/permisos nuevos cero. **Siguiente desarrollo: NOI-19**, entrada cliente a marcas asignadas sin reporte, Topics/Datos scoped y selección separada de ejecución/gasto; leer `PLAN_CLIENT_WORKSPACE_ENTRY_2026-09-10.md`, todavía plan. No abrir Admin global ni conceder roles reales. Segunda carga real por UI y permiso vigente siguen pendientes; no E2E completo. Loop ACTIVE en este chat, sin ventana/presupuesto nuevos. Sonnet4.6/noOpus, sin proveedores. Compass, historia y tres drafts ajenos conservados.

> **SQL0152 instalado y verificado — 10 septiembre02:43UTC.** No reaplicar. Commit focal limpio `1a609cb0cd1b24c661818e37bc65dcdfb3303b59`: renovación del permiso incremental en misma actualización; todos checks/PG/DOM/build/revisión cerrados. Cuatro funciones,15tablas/ACL intactas; cero permisos/proveedores. Leer `DELIVERY_INCREMENTAL_PERMISSION_RENEWAL_2026-09-10.md`. Studio/Worker conservan d4cd78e hasta verificar entrega nueva. Recibo íntegro223650bytes/SHA44dd6564…e23f5 respaldado en checkout renewal. No repetir gates/imports/fit/SQL; Compass/historia/drafts conservados.

> **NOI-82 en desarrollo LOCAL — 10 septiembre 02:14 UTC.** Nuevo checkout `noisia-incremental-permission-renewal-2026-09-10`, rama `codex/noisia-incremental-permission-renewal-2026-09-10`, desde d4cd78e. Backend cierra renovación/SQL0152, Root estado/cola/servicio, Frontend controles existentes y recuperación, Import prueba PG/Worker compuesta con proveedor local simulado. Leer `PLAN_INCREMENTAL_PERMISSION_RENEWAL_2026-09-10.md`. SQL0152 sólo LOCAL en desarrollo; no aplicar ni enviar proveedores. UAT d4cd78e/SQL0148–0151 y sus recibos cerrados permanecen intactos. No repetir gates/imports/fit/SQL; no nuevo permiso. Compass e historia conservados.

> **Entrega incremental comprobada en UAT — 10 septiembre 02:03 UTC / 9 septiembre México.** Studio y Worker `d4cd78e0bbbee30895580a820404f6d5d3e8d46b` activos, anteriores retirados; SQL0148–0151 aplicado una sola vez y verificado. Autodeploy Studio restaurado. Se conectan evidencia incremental → autorización explícita → interpretación guardada → Topics editables → proyección a Signal. Menciones nativo y su mejora de rendimiento quedan incluidos. UI real conserva 7,396 recibidas/16 CSV, 32 Topics de 32/357 unidades, dos seleccionados/98 en Signal y cita → Menciones con 6,826 consultables. Recibo posterior 02:03:26.993Z: cero actividad, permisos o gasto nuevo; selección/costos intactos. Leer `DELIVERY_INCREMENTAL_MONITORING_COMPOSED_2026-09-09.md`. No repetir SQL, imports, fit, Voyage ni gates cerrados. **Siguiente desarrollo: NOI-82, renovación explícita del permiso editorial incremental vencido, en checkout aislado desde d4cd78e.** Sigue pendiente una segunda carga real por UI y permiso vigente para comprobar el ciclo remoto nuevo; derechos cliente y calidad también pendientes. No declarar E2E completo. Loop ACTIVE en este chat; Sonnet4.6/noOpus, sin nuevo permiso de proveedor. Compass, historia y tres drafts ajenos conservados.

> **SQL0148–0151 instalado y verificado — 10 septiembre 01:53 UTC.** Paquete exacto de `d4cd78e` aplicado una sola vez en UAT: 49 funciones, 13 triggers, cuatro columnas; hashes de 15 tablas y permisos previos intactos. Cero permisos o llamadas nuevos. Recibo íntegro privado `uat-0148-0151-migration-receipt.json` en el checkout compuesto. **No reaplicar SQL.** Commit `d4cd78e` enviado sólo a rama UAT; Worker construye y Studio conserva `1836972`, con autodeploy deshabilitado temporalmente para respetar DB → Worker → Studio. Restaurar autodeploy Studio y entregar el mismo commit sólo después de comprobar todas las réplicas Worker nuevas. Leer `DELIVERY_INCREMENTAL_MONITORING_COMPOSED_2026-09-09.md`. Los datos, costos, selección de dos Topics/98 y Compass permanecen intactos; no renovar proveedor ni repetir gates/imports/fit.

> **Incremental compuesto cerrado LOCAL — 10sept01:37UTC / 9septMéxico.** Commit limpio `d4cd78e0bbbee30895580a820404f6d5d3e8d46b`,37archivos nuevos/59contraUAT1836972 (incluye foundation/consumer/resolver previos). TC/lint11/11, DB238/Studio836/Worker641PASS, PGcompuestos/33DOM/revisión0P0/P1/P2 y build73sPASS; sólo fixtureJSX corregida. Leer `DELIVERY_INCREMENTAL_MONITORING_COMPOSED_2026-09-09.md`. Paquete0148–0151 en ensayoLOCAL/revisión; todavía NOUAT/noSQLaplicado. Preflightreadonly01:24todosworkspaces0actividad/candidatos/hijos/permisos, flagproveedortrue pero deadlineviejo9sept06UTCintacto. PlanDB→Worker→Studio; no renovarpermisos ni repetirfit/imports/gates. UAT1836972/Mencionesaceptado,2Topicsseleccionados98 y6,826raícesconservados. LoopACTIVE sinventana nueva. Compass/historia/3drafts intactos.

> **Menciones aceptado en UAT — 10 septiembre01:14UTC / 9 septiembreMéxico.** Studio/Worker `1836972` activos, SQL0147 intacto. P2 real cerrado: lector39.202s→1.161/1.085/1.097s, sin cambios de datos/globales. UI real: búsqueda, dos plataformas, fecha sin datos/retorno, orden, foco URL y cita Topics→Menciones PASS. Recibo final costos/selección/generación byteidénticos, actividad0/permisos0; conserva dos Topics seleccionados/98 en Resumen y6,826 raíces. Leer `DELIVERY_SIGNAL_MENTIONS_NATIVE_2026-09-09.md`. Incremental LOCAL compuesto en `noisia-incremental-monitoring-completion-2026-09-09`, base5501a91 preserva1836972:0150productor gratis/0151materialización,UI,admisión atómica/estado/recuperación implementados y gates PG compuestos PASS. Root cierra seam ACK histórico begin/revoke y flag proveedor antes de checks globales/commit. SQL0148–0151 todavía LOCAL; no aplicar ni desplegar sucio. Loop ACTIVE sobre este chat, sin ventana nueva ni permiso proveedor. Sonnet4.6/noOpus; Compass e historia intactos.

> **Continuidad de reanudación — 9 septiembre México / 10 septiembre UTC.** Menciones nativo `8fdf94e` en Studio/Worker UAT, SQL0147 intacto. Checks/build/revisión locales cerrados; UI real confirma 6,826 conversaciones y páginas. **P2 de rendimiento real pendiente:** paginar seguía cargando tras24s; Root/Backend diagnostican lectura PG, sin repetir fit/imports/proveedores. Leer `DELIVERY_SIGNAL_MENTIONS_NATIVE_2026-09-09.md`. Antes de esta entrega ya existían dos Topics seleccionados (revisión2), 98 menciones en Resumen; conservar, no volver al resumen histórico63. Costos y selección antes/después intactos, cero actividad/permisos. Incremental completo sólo LOCAL en `noisia-incremental-monitoring-completion-2026-09-09`: foundation/consumer/resolver recompuestos y Menciones conservada; Backend0150 productor gratis, Import0151 catálogo/serving, FrontendUI, Rootadmisión atómica/estado. No aplicar0148–0151 aún. Loop ACTIVE sobre este chat; permiso Claude no renovado, Sonnet4.6/noOpus. Compass e historia intactos.

> **REANUDADO por instrucción del operador — 2026-09-09 23:33:29 UTC.** El operador volvió y pidió continuar; la pausa inferior queda como historia. Loop ACTIVE sobre este chat mediante app, sin renovar la ventana ni el presupuesto antiguos. Primer corte: Menciones de Signal nativo, misma generación/derechos/fechas que Resumen y Topics, lectura paginada y foco; checkout limpio `noisia-signal-mentions-native-2026-09-09` desde c8f05b9. Backend DB, Frontend UI, Root API/integración, Import revisión. Leer `RESUME_SIGNAL_MENTIONS_NATIVE_2026-09-09.md`. Después conexiones incrementales del plan vigente. UAT c8f05b9/SQL0147 y editorial070c94e/SQL0148–0149 LOCAL preservados; cero proveedores nuevos, permiso Claude pendiente, Sonnet4.6/noOpus. No repetir imports/Voyage/fit/SQL/gates, no producción ni drafts ajenos. Compass e historia conservados.

> **PAUSA EXPRESA DEL OPERADOR — 2026-09-09 15:24:29 UTC.** El operador pidió corte seguro y volver más tarde. Loop `noisia-topics-to-signal-uat-loop` verificado PAUSED; Backend/Frontend/Import terminados. No reanudar desarrollo, automatización ni proveedores hasta nueva instrucción. Se conservan UAT `c8f05b9`/SQL0147, editorial LOCAL limpio `070c94e`/SQL0148–0149 sólo local y los tres drafts ajenos intactos. Estado de producto y costos: consultar recibos del cierre; no se hizo un nuevo análisis remoto en esta pausa. Leer `SAFE_PAUSE_OVERNIGHT_2026-09-09.md` y `OPERATING_SNAPSHOT_2026-09-09.md`. Compass, plan y toda la historia conservados.

> **PARADA SEGURA — 2026-09-09 14:47:50 UTC.** Ventana autónoma de ocho horas concluida; loop `noisia-topics-to-signal-uat-loop` **PAUSED por app sobre este chat** y agentes terminados. Leer `SAFE_PAUSE_OVERNIGHT_2026-09-09.md`, `OVERNIGHT_PRODUCT_DELIVERY_2026-09-09.md` y el snapshot. UAT Studio/Worker `c8f05b9`/SQL0147: 7,396 recibidas/16 CSV, 32 Topics editables de 32/357 grupos, selección real con 63 asociaciones en Signal Resumen/Topics. Última lectura 2026-09-09T14:42:32.143Z: cero ejecuciones/llamadas/outbox activos y cero permisos/envíos nuevos. Claude USD 1.918865 confirmado + USD 1.6818 reserva terminal; Voyage USD 0.594449, permiso nuevo pendiente. Editorial `070c94e` limpio y probado sólo LOCAL, SQL0148/0149 no UAT. Faltan Menciones nativo, conexión editorial incremental completa, permisos cliente y calidad; no afirmar E2E terminado. No iniciar trabajo/proveedor por el loop vencido, no repetir gates/imports/Voyage/fit/SQL, no producción. Compass e historia conservados.

> **Continuidad 9 septiembre 14:00 UTC:** resolver editorial incremental LOCAL cerrado en `070c94e`, sobre `b6da353`/`f35d78c`. Origen numérico y dueño editorial separados, ediciones conservadas, hashes históricos intactos; 26 focales/470 query PASS, TC/lint11, revisión sin P0/P1/P2. Leer `DELIVERY_INCREMENTAL_EDITORIAL_PROVENANCE_LOCAL_2026-09-09.md` y `PLAN_COMPLETE_INCREMENTAL_MONITORING_2026-09-09.md`. SQL0148/0149 LOCAL; lector DB/activación/materialización y productor/admisiónUI pendientes. UAT Studio/Worker c8f05b9/SQL0147 intacto,32Topics/32de357,selección63,7396recibidas/16CSV. Cero proveedores/permisos nuevos, presupuesto pendiente. Revisión final de producto y parada segura al14:47:24UTC mediante app. No repetir gates/imports/Voyage/fit/SQL. Compass e historia conservados.

> **Continuidad 9 septiembre 13:40 UTC:** consumidor editorial incremental LOCAL cerrado en `f35d78c` sobre `b6da353`:17 archivos, cola existente/ledger/plan exacto/recuperación/PG integrado; TC/lint11, DB236 y Worker624 PASS, revisión0P0/P1/P2. Leer `DELIVERY_INCREMENTAL_EDITORIAL_CONSUMER_LOCAL_2026-09-09.md`. SQL0148/0149 LOCAL; no desplegar como monitoreo completo. Faltan productor de evidencia/admisiónUI y resultados→Topics→Signal; se define corte mínimo siguiente. UAT Studio/Worker `c8f05b9`/SQL0147 intacto,32Topics/32de357unidades,selección63,7396recibidas/16CSV. Cero proveedores/permisos, presupuesto nuevo pendiente. No repetir imports/Voyage/fit/SQL/gates. Parada segura al14:47:24UTC y PAUSED por app. Compass e historia conservados.

> **Entrega 9 septiembre 13:18 UTC:** Studio/Worker UAT `c8f05b9` comprobados. Overview muestra los ámbitos reales de 16 archivos: marca 2, competencia 13, categoría 1; conserva 7,396 recibidas/9,131 filas. Leer `DELIVERY_OVERVIEW_FILE_SCOPES_2026-09-09.md`. Topics legible y Dashboard entregados; SQL0147, 32 Topics/32 de 357 unidades, selección63 y costos intactos. Editorial incremental LOCAL b6da353 + SQL0149/consumer/routing en desarrollo, no UAT. Cero proveedores/permisos, solicitud de presupuesto pendiente. No repetir imports/Voyage/fit/SQL/gates. Fin 14:47:24 UTC y pausa segura por app. Historia y Compass conservados.

> **Continuidad 9 septiembre 13:10 UTC:** Studio UAT `db97205` comprobado: Topics separa catálogo, interpretación y costos Claude; Worker conserva `d97ada4` sin cambio de código. Leer `DELIVERY_TOPICS_READABILITY_2026-09-09.md`. Base editorial incremental LOCAL `b6da353` cerrada con SQL0148 local, streaming/almacenamiento/plan y protocolo monetario; leer `DELIVERY_INCREMENTAL_EDITORIAL_FOUNDATION_LOCAL_2026-09-09.md`. SQL0149/consumer siguen en desarrollo, no desplegar foundation aislada. Ámbitos de archivos en Overview en checks locales. SQL0147 y UAT reales intactos, 32 Topics/32 de 357 unidades, selección 63 y costos conservados; cero proveedores/permisos. No repetir imports/Voyage/fit/SQL/gates. Fin de ventana 14:47:24 UTC, parada segura y pausa por app. Compass e historia conservados.

> **Entrega9sept12:39UTC:** Dashboard `d97ada4` verificado en Studio/Worker UAT. National7,396recibidas/16archivos, navegación a Overview/Topics comprobada. Leer `DELIVERY_ADMIN_DASHBOARD_CORPUS_2026-09-09.md`. SQL0147 y selección/costos intactos, cero proveedores/permisos. Recuperación incremental0a80532 cerrada; interpretación de nuevos grupos sólo LOCAL en desarrollo0148, no consumer entregado. No repetir imports/Voyage/fit/SQL/gates. Fin de ventana14:47:24UTC y parada segura. Compass e historia conservados.

> **Continuidad9sept12:33UTC:** UAT Studio/Worker `0a80532` comprobados, SQL0147 intacto, recuperación de entrega incremental cerrada. Leer `DELIVERY_INCREMENTAL_DELIVERY_RECOVERY_2026-09-09.md`. Dashboard `d97ada4` enviado a rama UAT, runtime pendiente; leer `DELIVERY_ADMIN_DASHBOARD_CORPUS_2026-09-09.md`.32Topics/32de357unidades y selección63asociaciones conservados; no nueva carga real. Backend SQL0148 LOCAL prepara evidencia/claims editoriales incrementales; adaptador streaming19tests y helper monetario72tests cerrados LOCAL, sin consumer integrado ni autoridad nueva. Cero proveedores/permisos; Sonnet4.6, noOpus, autorización de gasto sigue sin respuesta. No repetir imports/Voyage/fit/SQL0131–0147/gates cerrados. Ventana termina14:47:24UTC: parada segura y PAUSED por app. Historia y Compass conservados.

> **Continuidad Sonnet entregada en UAT — 9 septiembre 12:08 UTC.** Studio/Worker `72fe142` y SQL0147 verificados; Topics ofrece autorización con importe/vencimiento y parada de nuevos envíos en la misma ejecución. No se activó permiso: cero operaciones de autorización y cero llamadas nuevas; la petición de presupuesto sigue sin respuesta. Leer `DELIVERY_INTERPRETATION_SELF_SERVICE_2026-09-09.md` y recibos posteriores. Conserva 32/357 grupos, 32 Topics, selección63/6826 en Signal, 7,396 recibidas/16 archivos. Costo confirmado USD 1.918865 + reserva terminal USD 1.6818. No repetir SQL0147/imports/Voyage/fit/gates ni cambiar fecha/env. Recuperación de entrega incremental13 archivos en checks locales; Dashboard recibirá el summary canónico ya disponible. Adaptador editorial y admisión de nuevas unidades se desarrollan sólo LOCAL, sin wiring ni permiso. Ventana hasta14:47:24UTC y pausa segura después. Compass e historia conservados.

> **Continuidad self-service en entrega — 9 septiembre 11:57 UTC.** Corte focal `72fe14202183509d40f3aed415fb206334d4e901` cerrado local: Topics permite autorizar/detener interpretación Sonnet sobre la misma ejecución; 22 archivos, checks/build/PG/UI/revisiones cerrados sin P0/P1/P2. SQL0147 aplicado y verificado una vez, diez tablas intactas, cero permisos creados; no repetir. Leer `DELIVERY_INTERPRETATION_SELF_SERVICE_2026-09-09.md` y recibos posteriores. UAT previo `4de84e5`; verificar ambos nuevos runtimes. Ningún gasto Claude nuevo: USD 1.918865 confirmado + USD 1.6818 reserva terminal. Petición de presupuesto sigue sin respuesta; no autorizar por UI ni renovar fecha/env. Sonnet 4.6, no Opus. Backend/Frontend desarrollan recuperación local de entrega incremental; QA prepara adaptador editorial local sin wiring/proveedor. 32 Topics/63 asociaciones en Signal y 7,396 recibidas conservados; no simular segunda importación ni declarar monitoreo completo. Ventana termina 14:47:24 UTC: parada segura y pausa del loop. Compass e historia conservados.

> **UAT comprobado — 9 septiembre 11:26 UTC.** Studio y Worker `4de84e5` activos con SQL0145–0146 intacto. Recuperación numérica y reutilización de caché completa entregadas; productor/proyección activos. Refresh real conserva 32 Topics y Signal parcial con 63 de 6,826 menciones seleccionadas; costos/selección intactos, cero proveedores. Leer `DELIVERY_WORKSPACE_NUMERIC_RECOVERY_2026-09-09.md`. SQL0147/renovación de permiso desde UI sólo en desarrollo LOCAL, no aplicado ni autorizado. No repetir gates/imports/Voyage/fit. Fin de ventana 14:47:24 UTC y pausa segura; Compass e historia conservados.

> **Recuperación self-service cerrada LOCAL — 9 septiembre 11:20 UTC.** Commit `4de84e5`, 23 archivos: retry numérico explícito y reutilización de caché completa con cap 0. Checks/build/PG/UI/revisión cerrados, sin P0/P1/P2; entrega focal UAT enviada, runtime pendiente de comprobación. Leer `DELIVERY_WORKSPACE_NUMERIC_RECOVERY_2026-09-09.md`. UAT anterior `4b481e2` con productor/proyección activos y SQL0145–0146 ya verificados. No repetir SQL, imports, Voyage, fit ni gates. Nuevo trabajo local: permiso Sonnet desde UI en misma ejecución (SQL0147 en desarrollo; NO aplicar aún). No hay renovación real ni nuevos proveedores. Los 32 Topics y Signal parcial real se conservan. Fin 14:47:24 UTC; historia y Compass intactos.

> **Admisión incremental activa en UAT — 9 septiembre 11:09:54 UTC.** Studio y Worker `4b481e2` verificados; Worker de flags `5e548c3f-d57b-4911-90c9-f31816181e19` tiene productor numérico y proyección incremental activos. SQL0145–0146 intacto. La comprobación posterior encuentra cero candidatos, ejecuciones, outbox y llamadas inciertas activas; no se inventó nueva carga ni hijo UAT. Los 32 Topics, la selección real de 63 asociaciones en Signal y los costos permanecen intactos. Recibo privado `uat-admission-after.json`; leer `DELIVERY_WORKSPACE_NUMERIC_ADMISSION_2026-09-09.md`. Siguiente corte local: recuperación numérica explícita y reutilización de embeddings con caché completa/cap 0. Interpretación nueva requiere permiso; fecha anterior 06:00 UTC sigue vencida, sin respuesta a renovación. Fin de ventana 14:47:24 UTC, luego parada segura y pausa del loop. Compass e historia conservados.


> 2026-09-09T10:54:30.976247+00:00: corte17archivos4b481e2 de admisión automática cerradoLOCAL/checks/build/PG/UI. Entrega focal UATen curso, sinSQLnuevo; SQL0145–0146 y runtime1c4f506 ya verificados. Flagsnumérico/proyección offhastaambasréplicasnuevas. LeerDELIVERY_WORKSPACE_NUMERIC_ADMISSION_2026-09-09.md. Cero proveedores. Siguiente retry_numeric/cache-only; no inventar carga UAT. Fin14:47:24UTC. Historia/Compass intactos.


> 9 septiembre10:38:48UTC: SQL0145–0146 aplicado una vez enUAT,30funciones/7triggers verificados y9tablas de datos/costos/selección conhashintacto. Commit1c4f506 enviado sólo a ramaUAT; despliegues aúnporverificar. No repetirSQL. Recibo privado uat-0145-0146-migration-receipt.json. Proveedor nuevo0.


> Corte 2026-09-09T10:33:05.507560+00:00: incremental→Topics→Signal cerrado LOCAL en `1c4f506`, SQL0145–0146 aún no UAT. Leer `DELIVERY_WORKSPACE_INCREMENTAL_SIGNAL_LOCAL_2026-09-09.md`. Checks/build/PG/revisión cerrados; no repetir. UAT147b0fb conserva32Topics y selección real63/6826. Siguiente entrega focal + productor automático sólo tras embeddings nuevos completos. Cero proveedores; no permiso renovado. Fin ventana14:47:24UTC. Historia/Compass intactos.

> **Continuidad local — 9 septiembre 09:41 UTC.** Commit `6640dbb` cierra el consumidor numérico incremental, cap0, recuperación durable y separación del análisis editorial; 14 archivos verificados en checkout aislado, PG 2/2 y revisión sin P0/P1/P2. Leer `DELIVERY_WORKSPACE_INCREMENTAL_NUMERIC_LOCAL_2026-09-09.md`. SQL0145 sólo LOCAL, no aplicar ni afirmar entrega UAT todavía. UAT `147b0fb`/SQL0144 conserva el Signal real comprobado (un Topic seleccionado, 63 asociaciones; proyección 6,826/6,826). Siguiente corte en desarrollo: vínculo incremental a clasificación y Signal, con identidad/ediciones/selección persistentes y después disparo automático. No repetir imports, Voyage, fit ni gates cerrados. Cero proveedores nuevos; permiso Claude anterior vencido, renovación pendiente. Ventana activa hasta 14:47:24 UTC; Compass e historia conservados.

> **E2E parcial REAL UAT cerrado — 9 septiembre 09:10–09:18 UTC.** Studio y Worker `147b0fb` verificados. Worker usa recuperación aprobada (`recovery`), conservando identidades aisladas y vencimiento de gasto; ya no debe usar `empty-cut` después del primer arranque. Misma proyección `dc8758ed-9223-4563-af24-1a58c7bc6ea4`, generación `8de2a60d-4106-4e27-9604-fb4032940638`, terminó **6,826/6,826 raíces**, 1,231 asociaciones sobre 1,203 raíces y 30 Topics con miembros. Existen **32 Topics editables de 32/357 unidades interpretadas**. Root seleccionó por UI «Quejas de servicio al cliente en renta de autos»: **63 menciones visibles en Signal Topics y Resumen, con evidencia original y enlaces**. Cobertura parcial/precisión sin calibrar explícitas. Leer `DELIVERY_TOPICS_SIGNAL_PAGES_2026-09-09.md` y `.data/workspace-projection-performance-2026-09-09/uat-signal-real-receipt.json`. Cero gasto nuevo; USD 1.918865 confirmado + USD 1.6818 reserva terminal. Sigue sin respuesta la autorización nueva de Claude; no renovarla. **SQL0145 y consumidor incremental están LOCAL en desarrollo**, no aplicados ni entregados. Siguiente cerrar consumidor DB/Worker incremental, nuevas cargas automáticas y continuidad de selección/Signal; no repetir SQL0144, imports, Voyage, fit ni gates cerrados. Ventana autónoma termina 14:47:24 UTC; loop activo y parada segura al vencer. Compass e historia conservados.

> **Entrega focal enviada — 9sept08:56UTC.** Cut `147b0fb91d29f035c556ab427460196c68d743fa` push sólo ramaUAT: Resumen/Topics mismolector, UX asociaciones descubiertas, proyección paginada y EOF centinela. Checks/build/PG/revisión cerrados sinP0/P1/P2; noSQLnuevo. Leer `DELIVERY_TOPICS_SIGNAL_PAGES_2026-09-09.md`. Verificar nuevos Studio/Worker antes de afirmar activo. Último UAT8b14383 a08:53: mismaengine4c55af5c,32Topics, mismaproyeccióndc8758ed/generación8de2a60d,1804/6826raíces,317membresías/312raíces/30Topicsconmembresía. No iniciar otroanálisis: conservarcursor y verificar recuperación trasdeploy. ClaudeUSD1.918865confirmado+USD1.6818reserva,0gasto nuevo; fecha vencida06:00UTC intacta, Sonnet4.6/noOpus. Pregunta presupuesto nueva pendiente. Consumerincremental DB0145/spool/validación en desarrolloLOCAL separado; cuatroarchivosnuméricos cfe58fe cerrados pero monitoreo/Signalincremental aún noentregado. Noimports/Voyage/fit/SQL0144/gatesrepetidos. LoopACTIVE hasta14:47:24UTC yparada segura después; Compass e historia conservados.

> **Catálogo real UAT — 9 septiembre08:09UTC.** Studio/Worker8b14383 y SQL0144 comprobados; productor progresivo activo sólo tras retirar Worker antiguo. La MISMA ejecución4c55af5c-e17f-430a-b17d-6771e94bd30e conserva32/357 unidades y ahora **32Topics editables visibles por UI**. Incluyen mixtos/insuficientes: no32tópicos validados. Proyeccióndc8758ed-9223-4563-af24-1a58c7bc6ea4/generación8de2a60d-4106-4e27-9604-fb4032940638 corriendo,76/6826raíces a08:09UTC; seguir misma ejecución. Signal aún sin selección real. Rendimiento serial~0.62raíces/s en investigación local; no reiniciar fit/imports/Voyage/SQL. Frontend cierra Resumen nativo y cobertura; adapter incremental numérico en pruebas locales. Claude USD1.918865+reserva1.6818 intactos,0nuevo; permiso vencido06:00UTC, pregunta nueva pendiente, no renovar por loop. Ventana autónoma hasta14:47:24UTC. Leer DELIVERY_PROGRESSIVE_TOPICS_2026-09-09.md y evidencia posterior. Compass e historia conservados.

> **SQL0144 aplicado/verificado UAT — 9 septiembre 07:52:37 UTC.** No reaplicar. Commit focal `8b14383` en entrega UAT; runtime anterior d419d65. Leer DELIVERY_PROGRESSIVE_TOPICS_2026-09-09.md y recibo privado uat-0144-migration-receipt.json. Ledger, ejecuciones, artefactos y outbox preservados; misma ejecución32/357,0Topics,0gasto nuevo. Verificar ambos servicios y retiro de Worker anterior antes de activar NOISIA_WORKSPACE_TOPIC_PROGRESS_ENABLED sólo Worker. PermisoClaude nuevo pendiente, no envíos ni cambio de fecha. Frontend Signal nativo e incremental Python locales continúan; ventana hasta14:47:24 UTC. No imports/Voyage/fit/gates repetidos. Historia y Compass conservados.

> **Topics progresivos cerrados LOCAL — 9 septiembre 07:47 UTC, commit `8b143835c586edf61b01dc44bdb5f1c21830492c`.** 28 archivos; DB/Studio/Worker, PG compuesto, QA ES/EN móvil, typecheck/lint/build y revisión cerrados sin P0/P1/P2. Leer `DELIVERY_PROGRESSIVE_TOPICS_2026-09-09.md`. SQL0144 todavía local; Root prepara preflight/migración/entrega focal UAT. Habilitar productor de progreso sólo después de ambos runtimes nuevos y retiro de Worker anterior. UAT d419d65: corpus7,396/16/9,131 comprobado; misma ejecución32/357,0Topics hasta derivación. Sin nuevo Claude; permiso preguntado pendiente. Frontend desarrolla Resumen Signal nativo/cobertura; frente computacional adapter incremental local, sin tocar esta entrega. Ventana activa hasta14:47:24 UTC. No repetir imports, Voyage, fit, SQL0143 ni gates cerrados. Historia y Compass conservados.

> **Corpus y recorrido entregados UAT — 9sept07:28UTC, commitd419d65.** Studio/Worker activos; Marcas/Overview/Datos7,396raíces/16CSV/9,131filas comprobados porUI, sin reimport ni gasto. Incluye f6b3af1. Leer DELIVERY_ADMIN_CORPUS_JOURNEY_2026-09-09.md. Siguiente corte LOCAL0144: Topics progresivos desde32/357, edición sin otra llamadaClaude, censo completo con pendientes y selecciónSignal. No aplicar0144 niactivarproductor anteschecks/revisión; primeraactivación requiereWorkeranteriorretirado. Ventana hasta14:47:24UTC; grantClaude nuevo siguepreguntado sinrespuesta, noenvíos. Historia/Compass conservados.

> **Ventana E2E autorizada por el operador — 9 septiembre 06:47:24 a 14:47:24 UTC.** Continuar desarrollo delegado y entregas focales UAT durante ocho horas; no sólo trabajo local. Leer `PLAN_PRODUCT_E2E_OVERNIGHT_2026-09-09.md`. Prioridad: corpus recibido coherente en Marcas/Overview/Datos → Topics editables progresivos con evidencia → clasificación/selección/Signal → incremental. National es caso de prueba, no implementación por cliente. Root integra, agentes en marcha. Cap nuevo Claude hasta USD 30 preguntado asincrónicamente y PENDIENTE: todavía sin llamadas Claude ni renovación del permiso del 8 septiembre. Sonnet 4.6; no Opus. Base local f6b3af1/UAT948d781, 32/357 unidades y 0 Topics al comienzo. No repetir imports, Voyage, fit, SQL0143 ni gates cerrados. Conservar historia/Compass. Loop ACTIVE hasta final de ventana; entonces parada segura y pausa con herramienta de app. No producción/main ni tres drafts ajenos.

> **Diagnóstico de permiso vencido cerrado LOCAL — 9 septiembre 06:21 UTC, commit `f6b3af1`.** PostgreSQL devuelve vencimiento comprobado antes de nuevas reservas/envíos; Worker conserva el rechazo explícito sin volverlo incertidumbre; UI ES/EN conserva progreso/costos y explica permiso de gasto vencido. PG focal, componente real, suites DB/Studio/Worker, typecheck/lint y revisión cerrados sin P0/P1/P2. Leer `DELIVERY_WORKSPACE_ADMISSION_EXPIRY_LOCAL_2026-09-09.md`. Pendiente entrega focal UAT de este ajuste; UAT sigue `948d781`/SQL0143, misma ejecución `4c55af5c-e17f-430a-b17d-6771e94bd30e`, dispatch 6, 32/357 unidades y 0 Topics. Sonnet 4.6 permanece; no Opus ni Advisor Opus. Cero gasto nuevo: USD 1.918865 confirmado + USD 1.6818 reserva terminal. No hay autorización Claude del 9 septiembre ni renovación implementada. El error genérico histórico no se reescribe. Loop ACTIVE sólo trabajo local sin proveedor; no repetir este diagnóstico, imports, Voyage, fit, SQL0143 ni gates cerrados. Conservar Compass e historia; incremental automático y Signal real siguen pendientes.

> **Sonnet 4.6 ACTIVO en UAT; permiso diario vencido — 9 septiembre 06:03 UTC / medianoche México.** Studio y Worker `948d781`, SQL0143 verificado. No nuevas llamadas Opus, incluido Advisor. Sonnet añadió 28 interpretaciones válidas por USD 0.875955; misma ejecución `4c55af5c-e17f-430a-b17d-6771e94bd30e`, dispatch 6, conserva **32 de 357 unidades**, ocho artefactos de interpretación, 15 numéricos y **0 Topics**. Terminó a las 06:00:03 UTC con `workspace_engine_worker_failed`, coincidiendo con vencimiento de admisión; mejorar diagnóstico genérico. Las diez llamadas Sonnet están settled; no hay llamadas en vuelo ni outcome_unknown. Claude total confirmado USD 1.918865 + reserva terminal histórica USD 1.6818 = exposición USD 3.600665. **Cero envíos después de las 06:00 UTC; no hay permiso de gasto para el 9 septiembre.** El flag proveedor sigue true pero admisión vencida; no ampliar fecha por loop. No reanudar ni abrir otro análisis para eludir el permiso o la revisión editorial inmutable. Leer `DELIVERY_SONNET_46_2026-09-09.md`, `uat-sonnet-daily-close-receipt.json` y evidencia posterior. Loop ACTIVE para trabajo sin proveedor. Mantener Sonnet; nuevo gasto necesita permiso fechado y continuidad segura en esta misma ejecución. Topics, clasificación, selección, Signal e incremental automático siguen pendientes. No repetir imports, Voyage, fit, SQL0143 ni gates cerrados; no producción. Historia y Compass conservados.

> **Sonnet 4.6 entregándose en UAT — 9 septiembre 05:42 UTC / 8 septiembre México.** Commit focal `948d781e3022bf4e5283093c534d305583ccbd0c` enviado sólo a rama UAT. SQL0143 aplicado a las 05:41:13.249 UTC y verificado; no repetir. Checks, build, PG y revisión cerrados, sin P0/P1/P2 pendientes en este cambio. Worker construye `bdb66902-b4a3-412a-8472-ea3e282eb08d`; verificar ambos servicios antes de habilitar Sonnet. UAT previo `99b2b58` sigue con proveedor deshabilitado. Misma ejecución `4c55af5c-e17f-430a-b17d-6771e94bd30e` fallida, dispatch 5, cuatro de 357 grupos interpretados, cero Topics. Claude confirmado USD 1.042910, reserva terminal USD 1.6818, exposición USD 2.724710; cero Sonnet nuevo. El operador excluye toda nueva llamada Opus, incluido Advisor. Sonnet 5 sólo opción posterior. El cambio conserva fit y cuatro interpretaciones, sin reescribir historial. Permiso USD 30 sólo 8 septiembre México; no admitir nuevos envíos después de `2026-09-09T06:00:00.000Z` ni renovar por loop. Leer `DELIVERY_SONNET_46_2026-09-09.md` y recibos posteriores. Siguiente: verificar despliegue, revisión editorial de la MISMA ejecución y Sonnet dentro de permiso vigente; después Topics, clasificación, selección y Signal. Incremental automático sigue pendiente. No imports, Voyage, fit, SQL anteriores ni gates repetidos; no producción. Compass e historia conservados.

> **Cambio del operador: Sonnet 4.6, sin nuevas llamadas con Opus.** Corte 9sept05:24UTC / 8septMéxico. La ejecución4c55af5c-e17f-430a-b17d-6771e94bd30e falló en dispatch5 con repair_invalid:4/357 grupos interpretados,0Topics. ClaudeUSD1.042910 confirmado +USD1.6818 reservado terminal; exposiciónUSD2.724710. No llamadas en vuelo al último recibo. Worker interpretation_enabled=false enviado a despliegue; verificar. UATcódigo99b2b58/SQL0142 intactos. Root/Backend/Frontend preparan LOCAL Sonnet4.6 y una revisión editorial sellada en la MISMA ejecución, reusando fit y4interpretaciones, sin reescribir snapshots ni costos Opus. SQL0143 LOCAL en desarrollo, no aplicar hasta checks/revisión/entrega. El cambio de modelo NO renueva el presupuesto ni su fecha:USD30 sólo8septMéxico, admisión hasta2026-09-09T06:00:00.000Z. Sonnet5 sólo opción posterior. No Opus ni AdvisorOpus. No imports/Voyage/fit/SQL0142/gates repetidos; producción intacta. Historia y Compass conservados.

> Corte9sept05:08UTC: **Studio/WorkerUAT99b2b58 ySQL0142 verificados. Evidencia externa499 confirmada yguardadaprivada**, mismafila terminal_confirmed sinliquidarUSD1.6818; originalUSD0.147415 intacto. **Reanudada porUI MISMAejecución4c55af5c-e17f-430a-b17d-6771e94bd30e dispatch5running**, checkpoint357grupos/15artefactos;0Topics alúltimorecibo. Seguir éstaynootroPOST. Workeradmisiónhasta2026-09-09T06:00:00.000Z comprobada;permisoUSD30 sólo8septMéxico,norenovarporloop. Nointerrumpirrespuestasyaenviadas. LeerRECOVERY_PROVIDER_TERMINAL_RECEIPT_2026-09-09.md y.data/workspace-engine-2026-09-08/uat-terminal-confirmation-receipt.json,uat-terminal-resume-receipt.json. No repetirSQL0142/imports/Voyage/fit/gates ni pedirlogin. Siguienteinterpretaciónválida→Topics→clasificación→selección→Signal. Historia/Compass conservados.

> Corte 9 septiembre05:00UTC / 8sept23:00México: **SQL0142 aplicado y verificado UAT; ledger intacto. Commit99b2b58922783438f7652eeb2a5d6025d8f96155 enviado sólo a ramaUAT**, ambosservicios construyen. Leer RECOVERY_PROVIDER_TERMINAL_RECEIPT_2026-09-09.md y .data/workspace-engine-2026-09-08/uat-0142-migration-receipt.json. Nuevo gasto0, terminal aún no confirmadoDB. Siguiente verificar runtimeStudio/Worker nuevo +env de admisión hasta2026-09-09T06:00:00.000Z→confirmación administrativa499 privada→Reanudar MISMAejecución4c55af5c-e17f-430a-b17d-6771e94bd30e. ReservaUSD1.6818 intacta, confirmadoUSD0.147415; nofactura/cuerpo recuperado. No repetirSQL0142/imports/Voyage/fit/gates. No pedirlogin: operador ya abrióConsola. PermisoClaudeUSD30 sólo8septMéxico, sinrenovaciónporloop. 357grupos/15artefactos/0Topics; Signal aún pendiente. Historia/Compass conservados.

> Actualización vigente — 9 septiembre UTC, noche del8sept México: **el operador abrió Console y ya se inspeccionó**. No pedir login de nuevo. Provider `req_011Ces5jp4wBNQoqPyARnkvp` confirma499 Client disconnected a02:34:48.179Z/119.472s, correlacionado con reparación `e8a1641b-7807-4629-8f57-ed72fbc3230e`; input29190/output4479/cache0. No se recuperó respuesta ni factura. La reservaUSD1.6818 y confirmadoUSD0.147415 siguen intactos. Leer `docs/product/PROMPT_LOOPING/RECOVERY_PROVIDER_TERMINAL_RECEIPT_2026-09-09.md` y su evidencia privada. Backend/Worker/Frontend/root implementan LOCAL estado terminal confirmado con evidencia administrativa y máximo1 sucesor de transporte, misma reparación/petición/bundle y reserva adicional; NO enviar ni mutar UAT antes de SQL0142/checks/revisión/despliegue y recibo posteriores. UAT conserva Worker555db6a/Studio9f5bc83/SQL0141,357grupos/15artefactos/0Topics. Root agrega admisión de nuevos envíos con fecha de vencimiento para respetar USD30 sólo8septMéxico (vence09sept06:00UTC); no renovar el permiso. No repetir imports/Voyage/fit/gates cerrados. Historia yCompass conservados.

> Corte vigente — 8 septiembre México / 9 septiembre 02:53 UTC: **Worker UAT `555db6a` activo; Studio UAT `9f5bc83` activo; SQL0141 sin cambios.** El Worker permite hasta 10 minutos para futuras llamadas y conserva la incertidumbre sin retry. Recibo: `docs/product/PROMPT_LOOPING/DELIVERY_WORKSPACE_INTERPRETATION_TIMEOUT_2026-09-08.md`. Verificación real posterior al despliegue: misma ejecución `4c55af5c-e17f-430a-b17d-6771e94bd30e`, dispatch4 fallido, 6,826 raíces / 20,821 fragmentos / 357 grupos / 15 artefactos conservados; **0 interpretaciones válidas y 0 Topics**. Claude confirmado USD0.147415, llamada `e8a1641b-7807-4629-8f57-ed72fbc3230e` outcome_unknown con reserva USD1.6818; exposición USD1.829215. No reenviar, liberar reserva sin evidencia ni abrir otro análisis para eludirla. Login de consola Claude solicitado al operador y pendiente; la sesión sigue cerrada. La corrección no recupera esa respuesta. Voyage USD0.594449 intacto. Tope Claude USD30 sólo 8sept America/Mexico_City; no renovar por heartbeat. Siguiente: conciliar llamada → interpretación válida → Topics editables → clasificación → selección individual → Signal real. Incremental automático, transporte recuperable y publicación parcial con excepciones siguen pendientes. Loop ACTIVE sobre este chat, silencioso sin cambio accionable. No repetir SQL, imports, embeddings, fit ni gates cerrados; no producción. Historia y Compass se conservan.

> Corte vigente 8sept México /9sept02:36UTC: UAT Studio/Worker **9f5bc83** + **SQL0141 verificado**. La misma ejecución **4c55af5c-e17f-430a-b17d-6771e94bd30e**, dispatch4, conserva357grupos/15artefactos y0Topics. ReparaciónClaude **e8a1641b-7807-4629-8f57-ed72fbc3230e** enviada02:32:48, corte de nuestro transporte a120s sinrespuesta/requestID: **outcome_unknown**, reservaUSD1.6818. Original1recibo settledUSD0.147415; exposiciónUSD1.829215, saldoautorizado trasexposiciónUSD28.170785 sólo8septMéxico. **No reintentar ni liberar reserva sin evidencia.** UI bloquea correctamente. Root pidióloginConsolaClaude aloperador (sin sesión); correcciónlocal deltimeout para llamadasfuturas en curso, no recupera ésta. Leer `.data/workspace-engine-2026-09-08/uat-editorial-repair-unknown-receipt.json` y evidenciaposterior. VoyageUSD0.594449intacto; no repetirSQL/Voyage/imports/Python ni nuevoanálisisparaeludirincertidumbre. Catálogo/clasificación/selección/Signal siguen pendientes. Historia yCompass conservados.

> Continuidad 8 septiembre México / 9 septiembre 02:24 UTC: **SQL0141 aplicado y verificado UAT**, ledger intacto (Claude1 recibo settled **USD0.147415**, reservas/incertidumbre0). Corte **9f5bc83** enviado a rama UAT: reparación editorial máxima1/lote, misma configuración/cap, paquete y citas intactos; todo comprobado local y revisión0P0/P1/P2. Ver [recibo editorial](DELIVERY_WORKSPACE_EDITORIAL_REPAIR_2026-09-08.md). UAT runtime previo19992de; verificar los dos despliegues antes de Reanudar por UI MISMA ejecución **4c55af5c-e17f-430a-b17d-6771e94bd30e**, dispatch3 falló output_invalid tras recuperar357grupos/15artefactos. No repetir primer send, Python, Voyage ni SQL. Después catálogo→clasificación→selecciónindividual→Signal; aún no entregado. Voyage totalUSD0.594449. Claude restanteUSD29.852585 del permisoUSD30 sólo8septMéxico; no ampliar a otra fecha. La automatización debe consultar evidencia posterior; historia/Compass conservados.

> Continuidad 8 septiembre México / 9 septiembre 01:57 UTC: fix local **19992de** publicado sólo a la rama UAT; despliegue en progreso. Misma ejecución **4c55af5c-e17f-430a-b17d-6771e94bd30e**, dispatch 2, conserva **15 artefactos / 180 grupos open + 177 guided**, fallo por términos auxiliares vacíos antes de Claude. Ver [recibo de recuperación](DELIVERY_WORKSPACE_INTERPRETATION_PADDING_2026-09-08.md). Siguiente: verificar ambos despliegues y reanudar por UI el bundle existente sin refit ni uploads; luego interpretar, clasificar, seleccionar y comprobar Signal. PostgreSQL/checks/revisión cerrados, sin P0/P1/P2. Voyage total USD0.594449, Claude real0. No repetir embeddings/SQL/imports ni extender el permiso Claude USD30 fuera del8sept México. Consultar cualquier evidencia posterior; historia inferior conservada.

> UATaeea21e recuperación almacenamientoPASÓ. Mismaexec4c55af5c-e17f-430a-b17d-6771e94bd30e/dispatch2 fallóantesClaude01:35:41UTC en interpretation_cluster_invalid: BERTopic tiene paddingstringsvacíos en keywordsauxiliares. 15artefactosDBdurables,0callsClaude,0fitcheckpoint. BundlesegundoFIT180open/177guided (357grupos), SHAverificados; NOREPETIRFIT. Backend/Frontend corrigen blanktermsadapter y retryestrechosólosobrebundlecompleto, referencias/censo intactos. Leer .data/workspace-engine-2026-09-08/uat-interpretation-padding-failure.json. NoOtroPOSTantesdeploy, noSQL, noVoyage. CostesUSD0.594449Voyagetotal,Claude0.

> Corte enviado8sept19:26México: `aeea21e` corrige transporte de manifests en bucketprivado y recuperación estrecha por UI antes de checkpoint/model/artifacts/calls. Checks11/11, DB231/Studio704/Worker309 PASS y PGfocal/revisión cerrados. Studio/Worker UAT construyenelcut; noSQLniembeddingnuevo. Esperardeploy y Reintentar MISMAexec4c55af5c-e17f-430a-b17d-6771e94bd30e. Laejecuciónprevia sícalculófullcorpus124s,178gruposopen/178guided; se perdióscratchantescheckpoint yhabrárefitnuméricoconsusvectorcache. CostesVoyageUSD0.594321corpus+0.000128contexto;Claude0. Recibo DELIVERY_WORKSPACE_STORAGE_RECOVERY_2026-09-08.md y .data/workspace-engine-2026-09-08/uat-storage-failure-receipt.json. NoafirmarSignalcompleto.

> Bloqueo real 8sept19:13 México: run4c55af5c-e17f-430a-b17d-6771e94bd30e falló storage_verification_failed después del fit/export completo6826raíces/20821fragmentos/4guías y antesClaude. Sólo parte manifest4527bytes está en storage y suSHA es correcto; envelope application/json fue incompatible con MIME del bucket privado (octet-stream permitido). Fix adaptador y recuperación explícita estrecha en curso; no modificar bucket/SQL/corpus ni repetirVoyage. No reintentar hasta deploy. Contexto pagadoUSD0.000128 y corpusUSD0.594321 intactos;Claude0. Recibo .data/workspace-engine-2026-09-08/uat-storage-failure-receipt.json.

> Ejecución REAL en curso 8sept19:10 México: Studio/Worker UATe030a26 activos. Contexto Brand OS completado por UI run4c3bcc61-b256-4315-af4a-3fd8498fddc6:4guías,0intereses,1call,USD0.000128,reservas/incertidumbre0. Corpus original Voyage intacto USD0.594321. Motor real aceptado por UI `4c55af5c-e17f-430a-b17d-6771e94bd30e`, estado running/exporting al9sept01:10:11UTC; Claude capUSD30 hoy México. SEGUIR MISMA SOLICITUD; NO iniciar otra ni repetir preparación/embeddings. Recibo .data/workspace-context-initialization-2026-09-09/uat-context-receipt.json. Próximo comprobar cómputo/interpretación/clasificación, selección individual y Signal. No afirmar completado aún.

> Corte 8 septiembre 19:04 México: fix `e030a26` de preparación sin catálogo ya enviado a rama UAT focal. Tests DB231 PASS/62 SKIP, Studio701 PASS/6 SKIP, PG focal y revisión independiente correctos; typecheck/lint11/11. Studio/Worker construyen el ajuste. UAT anterior f4d2b60 activo/SQL0136–0140 comprobado; no reaplicar SQL ni corpus. Próximo clic real de preparación (4guías Brand OS)→análisis completo→selección→Signal. Cero proveedores nuevos a este corte. Recibo DELIVERY_WORKSPACE_CONTEXT_INITIALIZATION_2026-09-08.md. Historia conservada.

> Continuidad 8 septiembre 19:02 México: Studio/Worker UAT `f4d2b60` activos, SQL0136–0140 aplicado y verificado; no reaplicar. Límites Claude USD30/ejecución y diario configurados en America/Mexico_City; nuevo consumo0. QA UI detectó bloqueo sin catálogo: Brand OS requiere4guías de contexto y preparación quedaba oculta con0intereses. Backend/Frontend corrigen acción existente para inicializar catálogo vacío real→cotizar contexto→preparar; GET readonly, sin términos ficticios ni SQL nuevo. No repetir corpus Voyage completo USD0.594321. Sólo falta Voyage de contexto (autorizado). Luego análisis real→selección→Signal; todavía no E2E comprobado. Leer WORKSPACE_TOPICS_SIGNAL_UAT_EXECUTION_2026-09-08.md. Historia preservada.

> **Entrega UAT en curso — 8sept18:45 México.** SQL0136–0140 aplicado/verificado una vez (9sept00:45:07.750UTC), no reaplicar. Commit focal `f4d2b60` enviado a la rama UAT; Railway construye Studio4a2b8e56 y Worker823dedd3. Ocho variables de interpretación siguen STAGED sin desplegar; sólo activarlas después de Worker nuevo comprobado. Claude nuevo0. Leer [ejecución UAT](WORKSPACE_TOPICS_SIGNAL_UAT_EXECUTION_2026-09-08.md). La nota inferior de SQL sólo local queda como historia.

> **Corte LOCAL cerrado — 8sept,18:32 México / 9septUTC.** Commit `f4d2b60c8f70e402933e6966435e0390a78d4953`: clasificación de raíces completas desde ambas vías, selección individual persistente y Signal nativo con conteos/evidencia. Leer [recibo local](DELIVERY_WORKSPACE_TOPICS_SIGNAL_LOCAL_2026-09-08.md). Checks/build/PG/UI/revisión cerrados; tres drafts ajenos intactos. **UAT sigue ae3e36c/SQL0135**; siguiente imagen Worker aislada, SQL0136–0140 y entrega focal UAT, prueba real con embeddings ya pagados. No repetir imports/embeddings/gates. Claude nuevo0; topeUSD30 sólo8sept México. LoopACTIVE; historia inferior conservada.

> **REANUDADO por el operador — 8sept,23:42UTC (17:42 México).** Nueva indicación: continuar desde26db9cf; loop reactivado en este chat. La pausa inferior queda como historia. Siguiente entrega: clasificación de todas las menciones→selección individual→Signal nativo. No repetir embeddings/gates cerrados. Claude sigueUSD0 nuevo y tope autorizadoUSD30 sólo durante8sept America/Mexico_City; Voyage necesario autorizado.

> **PAUSA DEL OPERADOR — 8sept,18:19UTC.** Trabajo detenido y loop `PAUSED`, tres agentes finalizados. No reanudar automáticamente ni gastar por autorizaciones anteriores. HEAD focal26db9cf guardado; UATae3e36c/SQL0135. Leer [parada segura](SAFE_PAUSE_2026-09-08.md). Lo inferior conserva historia, no una instrucción de continuar.

> Corte local cerrado 8sept: `26db9cf` conecta la misma ejecución cálculo→Claude con recibos→Topics editables, SQL0139 local; checks, builds y revisión cerrados. **UAT sigueae3e36c/SQL0135.** [Recibo y siguiente conexión](DELIVERY_WORKSPACE_INTERPRETATION_LOCAL_2026-09-08.md). Próximo: clasificación de todas las raíces, selección explícita y reader Signal nativo; memo técnico disponible en.data/workspace-engine-2026-09-08/frontend-native-signal-contract.md. No repetir interpretación/fit/embeddings ni gates cerrados sin nueva causa. Voyage realUSD0.594321; Claude real nuevo0. Historia inferior conservada.

> Integración activa 8sept: misma ejecución cálculo→Claude→Topics editables sobre5f5ca24local. [Plan y responsabilidades](WORKSPACE_ENGINE_INTERPRETATION_INTEGRATION_2026-09-08.md). Aún sin entrega nueva UAT ni llamadas Claude; clasificación/Signal pendientes. Voyage ya completo, no repetir. Historia inferior conservada.

> Corte local cerrado8sept: commit5f5ca24 conecta motor completo workspace (BERTopic/UMAP/HDBSCAN, contexto0intereses, outbox/lease/checkpoint y ledger de interpretación); checks y PGverdes. **No desplegado**: UATae3e36c/SQL0135, Voyage real completadoUSD0.594321. Leer [recibo local y próxima unión](DELIVERY_WORKSPACE_ENGINE_LOCAL_2026-09-08.md). Siguiente entrega nueva completa interpretación→Topics→clasificación→Signal; no repetir embeddings ni benchmarks. SQL0136–0138 aún no remotos, Claude nuevo0. Historia inferior conservada.

> Actualización 8sept,17:13UTC: Voyage real completado en UAT sobre ae3e36c. 6,826/6,826 menciones y20,821/20,821fragmentos; USD0.594321 confirmados, reservas/incertidumbre0. Run2cd32bca-f9cf-4913-8df1-2351b695ba4c. Leer [recibo real](DELIVERY_VOYAGE_REAL_CORPUS_2026-09-08.md). No repetir POST/imports/embeddings. Motor nuevo aún local; despacho outbox/lease final en corrección, interpretación/Topics/Signal pendientes. Claude nuevo0; permisoUSD30hoy, Voyage necesario sin límite. Historia inferior conservada.

# CURRENT — Compass aprobado y preparación self-service lista

## Trabajo activo — recorrido genérico y autorización actual, 8 septiembre 2026

El operador pidió continuar End To End para cualquier marca; National sólo sirve como caso descartable. [Plan activo y autorización](./E2E_ENGINE_EXECUTION_2026-09-08.md). Claude API hasta USD30hoy America/Mexico_City entre producto/Advisor; Voyage necesario sin límite impuesto por el operador. Son permisos de gasto, no depósitos. Las restricciones financieras inferiores providerfalse/cap5 son historia y no requieren nueva autorización genérica. Root coordina consumos/recibos; cero llamadas nuevas al abrir el corte. Advisor dedicado no configurado actualmente, no usar secretos históricos ni la clave de producto como sustituto.

Se implementa motorPython real con todos los fragmentos, víaabierta+guiada, cero intereses válido y artefactos/modelos persistidos; después integración Topics/Claude/selección/Signal y nueva carga incremental. Backend inputs/artefactos; Python motor; Frontend recorrido; root orquesta. La aceptación es el flujo de producto, no un adapter aislado. No repetir gates cerrados ni especializarNational. Lo inferior conserva evidencia de92d5d0alocal y UATae3e36c.

---


## Vigente — persistencia local cerrada; motor real siguiente, 8 septiembre 2026

**Commit LOCAL `92d5d0ade818061bd51c4fd009caae0e6bbbcd21`; UAT Studio/Worker sigue `ae3e36c`.** [Recibo y límites](./DELIVERY_WORKSPACE_CLASSIFICATION_LOCAL_2026-09-08.md), [ADR024](../../adr/024-workspace-classification-generations.md). SQL0136 sólo local, ensayo completo y SHA `a8fe308882ab85dcce8956f087bee6a12e39fa98854e6638e42fb64cc581296d`. No se hizo push ni migración UAT. Doce archivos focales comprometidos; tres drafts ajenos intactos y sin stage.

Ledger0087 y ejecución existentes aceptan decisiones nativas, multilabel disperso, correcciones ligadas a contenido/contexto, copia con linaje y último completo separado de vigencia. Sin nuevas tablas, corpus artificial, productor/cola registrada, API/UI, motor o proveedor por defecto. Revisión independiente cerrada sin P0/P1/P2; root typecheck/lint finales verdes, paquetes tocados y PG3/3 cerrados. PG guardó/copió1,001 asignaciones; PG/BullMQ recuperó3raíces/133fragmentos y luego copió3 sin motor/texto. No repetir estas pruebas sin causa. Nueva ingesta→clasificación todavía sólo probada como delta unitario; el PG de este corte reutilizó el mismo manifiesto. No declarar segunda carga real, calidad semántica o escala2M comprobadas.

**Próximo resultado concreto:** conectar el cómputo semántico real guiado y el descubrimiento abierto de `tools/signal-semantic-lab/src/signal_semantic_lab/discovery.py` al workspace completo y a esta persistencia, primero localmente sin proveedores. Comprobar influencia efectiva de Brand OS/intereses y descubrimiento aun sin intereses; no convertir top32 en membresía ni exigirlo como filtro. Artefactos/modelos persistidos, asignación de nuevas menciones y detección de novedad también entre conversaciones ya asignadas, IDs estables/reconciliación. Integrar registro de modelo compatible sin autoridad inventada y medir costo del commit por raíz. Probar una segunda carga real local con tema nuevo. Después operación en Topics/cola existente, Claude con evidencia, selección y Signal. NOI-31/78 abiertos; no iniciar otra auditoría general ni añadir infraestructura por sí sola. No desplegar este consumidor aislado con un motor simulado.

National sigue16CSV/9,131filas/7,396raíces/6,826preparadas/570excluidas, sin intereses ficticios ni embeddings/BERTopic/Claude/Signal nuevos. Proveedorfalse; cero gasto. ProductoUSD11.362961/AdvisorUSD1.343826 intactos; cotizaciónUSD8.328543 frente a capUSD5. No habilitar proveedor/subir máximo por efecto del loop. La zona SentiOne ya preguntada sigue pendiente: no repetir ni inferir respuesta; avanzar código independiente. NOI-81 unknown/presupuesto, NOI-19 acceso cliente y NOI-80 retención siguen abiertos.

Root integró y guardó el corte; Backend, Worker y revisor están finalizados. Loop existente ACTIVE en este chat `01a079df-5eea-7a32-a910-f185e5f1d484`. No producción/main, Laika/Alexa, originalesJSONL/secretos, fixtures UAT, limpieza destructiva o deploy del repo documental sucio. No repetir imports, SQL0131–0135 o gates cerrados. Compass y toda la historia inferior se conservan.

---


## Vigente — preparación de intereses entregada en UAT, 8 septiembre 2026

**Studio y Worker UAT `ae3e36c1e8e2f7b9e2159b699acc08ebcf4c5848`, ambos comprobados.** [Recibo del corte](./DELIVERY_WORKSPACE_TOPIC_PROTOTYPES_2026-09-08.md), [ADR023](../../adr/023-workspace-topic-prototype-preparation.md). SQL0135 aplicado/verificado; mismo ledger/caché/outbox/Worker para intereses y corpus. Costo/progreso/recuperación inline en Topics, antes de importar; las fuentes de contexto vigentes y cada rol conservan su identidad. Los tres drafts ajenos siguen intactos y excluidos.

Checks, build, PG real con transporte simulado y UI ES/EN cerrados.202referencias/187textos deduplicados, fallo tras recibo, recuperación/cache sin otra llamada, mención posterior reutilizada y búsqueda lista. Revisión sin hallazgos pendientes; P2 de snapshots fuera de orden corregido y probado. National abre Topics sin errores:0intereses, sin runs de preparación; lector real UAT confirmó no_topics. No inventar intereses/fixtures para QA. La navegación JSON directa fue bloqueada por el navegador: no afirmar GET autenticado HTTP200; lector desplegado sí verificado.

**Próximo trabajo concreto:** conectar clasificación persistente/versionada e incremental del workspace con los motores existentes: todos los registros elegibles con estado explicable, correcciones conservadas y nuevas cargas asignadas o pendientes/emergentes. Reutilizar contratos/colas; evaluar el acceso a SQL0087 sin fabricar corpus legacy. Top32 sigue siendo recuperación no calibrada y no una clasificación final. Descubrimiento abierto debe incluir también conversaciones con candidatos guiados; predecir clusters conocidos no descubre temas nuevos. Probar primero localmente, sin proveedor. Después interpretación/selección y Signal. NOI-31/78 siguen obligatorios y abiertos.

NOI-81 aún conserva conciliación integral de unknown y presupuesto desde producto; NOI-19 cliente integral y NOI-80 retención física abiertos. National16CSV/9,131filas,6,826preparadas/20,821fragmentos, sin embeddings reales. Pregunta de zona SentiOne ya pendiente: no repetir ni reparar/analizar fechas por intuición. Cotización previaUSD8.328543 frente a máximoUSD5. ProductoUSD11.362961/AdvisorUSD1.343826 intactos; proveedorfalse. Ninguna llamada real, elevación de cap o activación por efecto del loop; cualquier ejecución real requiere caso/tope propios.

No producción/main, Laika/Alexa, originalesJSONL, secretos históricos, limpieza destructiva, fixtures UAT ni deploy del repo documental sucio. No repetir SQL0131–0135, gates, benchmarks o QA cerrados sin causa nueva. Agentes cerrados; reusar sólo subtareas acotadas. Loop existente ACTIVE en este chat `01a079df-5eea-7a32-a910-f185e5f1d484`. El Compass y el canon anterior se conservan como historia y evidencia.

---

## Historia anterior — no instrucción vigente

## Trabajo activo — preparación de intereses desde Topics, 8 septiembre 2026

Entrega focal en curso: commit `ae3e36c1e8e2f7b9e2159b699acc08ebcf4c5848` enviado sólo a la rama UAT. SQL0135 aplicado una vez y verificado a las14:52:33.155Z, SHA256`0a57447aa200be31e678151c4f2f1ae052c57a94b45219837f5ba2ec02e6f457`. No reaplicar. Root typecheck/lint, suites, build y revisión cerrados; Railway está construyendo Studio/Worker. Faltan comprobar ambos HEAD remotos y lectura autenticada de los endpoints/UI. Cero proveedores; tres drafts ajenos intactos.

Implementación focal local sobre `919fd35`: [plan y límites](./WORKSPACE_TOPIC_PROTOTYPE_PREPARATION_2026-09-08.md). Se reutilizan ledger/caché/outbox/Worker; SQL0135 distingue intereses del corpus, con cotización/recuperación y control inline en Topics. Root integra; Backend, Worker y Frontend trabajan en superficies acotadas. UAT sigue en919fd35, proveedor deshabilitado, cero gasto nuevo. National y los gates cerrados se conservan; no reimportar ni crear intereses ficticios. La clasificación/BERTopic/Claude/Signal del Compass continúan pendientes, sin atribuirlos a preparar vectores.

---

## Vigente — búsqueda de Topics entregada en UAT, 8 septiembre 2026

**Studio y Worker UAT `919fd3518ed5c8dec8489a3a4c6d20a4fda3085a`, ambos comprobados.** [Recibo del corte](./DELIVERY_WORKSPACE_TOPIC_SEARCH_2026-09-08.md), [ADR022](../../adr/022-workspace-topic-search-evidence.md). SQL0134 aplicado/verificado; proveedor deshabilitado. Root integró y desplegó sólo el worktree focal, preservando tres contract-drafts ajenos. Checks/build y revisión independiente cerrados; ningún P0/P1/P2 pendiente en la superficie revisada.

El catálogo y la cola existentes consumen todos los fragmentos/raíces e intereses aplicables con Brand OS completo, caché por perfil, lease, checkpoint, reintento y extractos verificados. PG/BullMQ local: 133 fragmentos, 67 Topics por raíz, evidencia al fragmento130, crash/retry; permisos/revocación y reader legacy por ID comprobados. UI ES/EN y recuperación local pasaron; API/UI UAT actualizadas, sin errores. No repetir estos gates, ni imports/preparación/SQL0131–0134 o benchmarks cerrados sin una nueva causa.

**Límite importante:** top32 por raíz es una lista de recuperación no calibrada, con evaluados/retenidos/omitidos explícitos. No es etiquetado semántico masivo, recall medido, BERTopic, Claude, asignaciones aprobadas o Signal nuevo. SQL0087 sigue sin esas entradas workspace. NOI-31/78 permanecen abiertos; el Compass y su primer release incremental completo conservan autoridad.

**Próximo trabajo concreto:** preparar prototipos de intereses/Brand OS desde Topics con perfil completo y el control existente de costo, reservas, caché/recibos y recuperación (NOI-81). El compilador está implementado; falta ejecutar esa preparación desde producto. No reutilizar prototipos legacy query ni crear otro framework/ledger de asignaciones. Probar transporte simulado local antes de proveedor; no habilitar proveedor, elevar máximo remoto ni gastar como efecto del loop. Después conectar clasificación persistente, descubrimiento abierto con métodos existentes, interpretación y Signal. Las conversaciones con candidatos guiados también participan en descubrimiento; predecir clusters existentes no descubre emergentes.

National conserva 16 CSV/9,131 filas, preparación real6,826 menciones/20,821 fragmentos, sin embeddings reales y sin intereses creados. GET UAT muestra `missing_embeddings`, sin ejecuciones ni resultados. No crear fixtures o intereses ficticios para simular QA. Cotización anterior USD8.328543 frente a máximoUSD5; productoUSD11.362961 y AdvisorUSD1.343826 intactos. La pregunta de zona SentiOne ya hecha sigue pendiente: no repetirla ni reparar/analizar fechas por intuición; permite avanzar código independiente. Nueva llamada real requiere caso y tope propios.

NOI-81 conserva conciliación self-service de unknown/costos y preparación de intereses; NOI-19 cliente integral y NOI-80 retención física siguen abiertos. Tres agentes cerrados; reutilizar sólo para tareas acotadas y sin otro orquestador. Loop existente ACTIVE sobre `01a079df-5eea-7a32-a910-f185e5f1d484`. No producción, Laika/Alexa, secretos históricos, datos hardcodeados, limpieza destructiva ni deploy del repo documental sucio. Canon y recibos anteriores se conservan como historia.

---

## Historial anterior — no instrucción vigente

## Trabajo activo — búsqueda completa de Topics en verificación final, 8 septiembre 2026

Código focal nuevo implementado sobre `561ecc3`; todavía no desplegado. [Plan y límites](./WORKSPACE_TOPIC_COMPUTATION_EXECUTION_2026-09-08.md), [ADR022](../../adr/022-workspace-topic-search-evidence.md). Compilador completo, SQL0134, Worker paginado, evidencia recuperable y UI Topics. Es recuperación no calibrada: compara todo, conserva hasta 32 candidatos por raíz y registra omisiones; no satisface aún clasificación masiva, BERTopic, Claude ni Signal. Cero proveedores. SQL0134 sólo local; UAT conserva Studio561ecc3/Worker67569e9. Checks y build verdes; revisión independiente está cerrando dos cruces de lectores/permisos legacy. Root integra y despliega el corte después de la corrección y su prueba. No reiniciar auditoría ni repetir imports/preparación/embeddings cerrados.

---

## Vigente — base de embeddings entregada, 8 septiembre 2026

**Studio UAT `561ecc3c1c5e03f1a6fbba292c8ca1f38c30e0d6`; Worker UAT `67569e991e2ba027d7fa78581739783686092bf5`, ambos comprobados.** [Recibo de entrega](./DELIVERY_WORKSPACE_EMBEDDINGS_FOUNDATION_2026-09-08.md). SQL0133 aplicado transaccionalmente y verificado; proveedor deshabilitado en ambos servicios. El corte final sólo cambió Studio y Railway omitió correctamente el Worker.

El manifiesto completo ya tiene ejecución de embeddings recuperable, caché por workspace/perfil, cotización y recibos de costo. PG/BullMQ local con transporte simulado procesó 50,002 menciones y 55,519 fragmentos, recuperó un recibo persistido sin reenviarlo y comprobó un segundo recorrido 100% caché. Checks, build y revisión focal cerrados. Esto todavía no prueba embeddings reales ni clasificación, BERTopic, Claude o Signal nuevos.

National conserva 16 CSV / 9,131 filas, 6,826 menciones preparadas y 20,821 fragmentos. Cotización real por UI: cota conservadora USD 8.328543; máximo del servidor USD 5. Mensaje de límite, botón deshabilitado y consola sin errores comprobados en el HEAD final. Cero llamadas pagadas; no cambiar el máximo ni habilitar proveedor por esta entrega. Saldo producto USD 11.362961; Advisor USD 1.343826.

**Siguiente trabajo concreto:** consumir esta base en clasificación y descubrimiento del workspace, usando todas las partes de cada mención, intereses/Brand OS como guía y asignaciones versionadas incrementales. Reutilizar los métodos existentes y probar primero localmente sin proveedores. No crear un study_corpus artificial ni aplicar límites legacy de población/texto. No abrir otra auditoría de estado ni repetir imports, preparación, SQL0131/0132/0133, gates o QA cerrados sin nueva causa.

NOI-31/78 siguen abiertos por clasificación, descubrimiento e incrementalidad obligatoria. NOI-81 registra respuestas/costos inciertos y presupuesto operables desde el producto antes de activación pagada en producción; la conciliación de unknown no está implementada. NOI-19 conserva acceso integral cliente y NOI-80 retención/retirada física de derivados. La pregunta de zona SentiOne ya hecha sigue pendiente: no repetirla ni reparar/analizar National por intuición; permite continuar código independiente. Nueva ejecución real requiere caso y tope propios.

Loop existente ACTIVE sobre `01a079df-5eea-7a32-a910-f185e5f1d484`. Tres agentes finalizados; root integró y verificó la entrega. Preservar los tres archivos ajenos de contract-drafts. Sin producción, Laika/Alexa, datos hardcodeados, secretos históricos, limpieza destructiva ni despliegue del repositorio documental sucio. Compass, plan, canon y recibos históricos se conservan.

---

## Historial de preparación y entregas anteriores — no instrucción vigente


## Trabajo activo — embeddings reutilizables, 8 septiembre 2026

Se implementa [el consumo semántico del manifiesto preparado](./WORKSPACE_EMBEDDINGS_EXECUTION_2026-09-08.md): caché versionada, cotización/tope, Worker recuperable y pruebas con proveedor simulado. Root: contrato compartido, API y management; Backend: SQL0133/runtime/recibos; Worker: transporte/outbox/PG; Frontend: controles inline y recuperación. UAT permanece en `bed52d9`; SQL0133 aún no aplicada remotamente. Cero llamadas pagadas. Conservar recepción/preparación de National y pregunta temporal pendiente. No sustituye el Compass ni los recibos siguientes.


## Vigente — preparación de texto entregada, 8 septiembre 2026

**Studio y Worker UAT `bed52d9bdea7d72f986e994bc13d0a8f1ee260a7` activos y comprobados.** [Recibo del corte](./DELIVERY_WORKSPACE_CORPUS_TEXT_PREPARATION_2026-09-08.md). National terminó una solicitud normal por UI:7,396raíces recorridas,6,826menciones preparadas,570excluidas,20,821fragmentos; run `ea577ad4-06ac-4752-9a6e-49237a122bbd`, revisión1 vigente. POST202en611ms; duración74.11s; recarga durante/después y ES/EN comprobados. Los16CSV/9,131filas siguen aceptados; ocho competidores sin archivo no bloquean. No volver a subir ni preparar National sólo para repetir evidencia.

SQL0132 aplicado y verificado; checks/revisión/PG+BullMQ50,001/recuperación/incremental local cerrados. Tres contract-drafts ajenos siguen sucios y excluidos. Texto preparado **no es** embeddings, clasificación, BERTopic, Claude ni Signal. Cero llamadas pagadas; productoUSD11.362961 y AdvisorUSD1.343826. Zona SentiOne ya preguntada sigue pendiente; no repetir pregunta ni reparar fechas por intuición. Leer Compass, plan y recibo antes de elegir código. Historia inferior conservada, no instrucciones actuales.

**Siguiente trabajo concreto:** conectar esta preparación workspace-native con el cálculo semántico completo, empezando por embeddings reutilizables/versionados y una ejecución recuperable con estimación y límite de costo. Reutilizar cola/Worker y autoridad de workspace; no crear study_corpus de puente ni truncar texto/población. Implementar primero contratos y recorrido con proveedor simulado local, sin llamadas pagadas nuevas. Conectar después clasificación, guía Brand OS/Topics y descubrimiento emergente de todo residual; incrementalidad y Signal multiámbito siguen dentro del primer release completo, no opcionales. Nuevas llamadas reales requieren caso y tope propios antes de encolar. La respuesta temporal pendiente sólo condiciona reparación/análisis real de National, no este código independiente.

NOI-31/78 siguen abiertos por clasificación/descubrimiento. NOI-27 conserva navegación/operación, NOI-55 recibos de release. NOI-80 retención/retirada de activos compartidos sigue pendiente: invalidar uso no borra copias; no ejecutar limpieza ahora. No abrir otra auditoría de estado o QA de recepción como siguiente turno.

---

## Historial previo de operación — no instrucción vigente

## Activo — National y archivos reales, 8 septiembre 2026

**Entrega vigente: corpus recibido visible y refresco corregido en UAT `65926c0`.** [Recibo y límites](./NATIONAL_CORPUS_VISIBILITY_2026-09-08.md). Studio/Worker con HEAD exacto verificado; Actualizar HTTP200 en 937 ms frente a 33.1 s, recarga sin errores nuevos. National conserva 16 archivos / 9,131 filas, 7,396 menciones únicas y 1,735 duplicados. P2 de esta consulta cerrado sin ANALYZE/DDL/mutaciones remotas ni proveedores. Siguiente: preparación completa workspace-native e incremental (NOI-31/78); no confundir recepción con clasificación. Zona SentiOne aún pendiente, no reiterar pregunta ni tocar proyecciones por intuición.

**National ya tiene los 16 CSV aceptados por UI, con 9,131 registros leídos.** No estamos esperando archivos y no deben volver a cargarse. [Resultado, recibos y pendientes](./NATIONAL_IMPORT_ACCEPTANCE_2026-09-08.md). Marca `f6f724fa-13a8-40b5-82cb-7c48f7f2252e`, workspace `497e1cb0-56d6-4c1f-a5d5-2f723f13a183`. La aceptación se comprobó en `33d48d35af4c73486d4f4873aa02968037764a54`; la entrega temporal y su corrección de pantalla se verificaron originalmente en `2c2e3f409a1ab0fe38f4724b9a4295ddedc1486f`. Plan vigente v2/rev2. Root probó recuperación encadenada y nueva selección de Query 4: ambas reconocen la primera aceptación, sin inflar los 16 archivos/9,131 registros. No repetir los gates de importación ni SQL0131.

Normalización temporal70604a4 y corrección de pantalla `2c2e3f409a1ab0fe38f4724b9a4295ddedc1486f` verificadas en la entrega anterior de Studio/Worker UAT y conservadas en el corte vigente. React418 quedó resuelto usando la zona del workspace para las aprobaciones: HTML y DOM muestran la misma fecha, recarga HTTP200 sin errores nuevos de consola y 16 recibos/9,131 registros conservados. Root typecheck/lint11/11, Studio579pass/4skip y build verdes. Sin cambios remotos de datos. [Recibo temporal y límites](./NATIONAL_TIMESTAMP_NORMALIZATION_2026-09-08.md). Zona editable, interpretación sin TZ del host, errores ES/EN y validación completa antes de persistir. PG reprodujo el fallo tardío601→500filas parciales; después del fix falla sin menciones, la recarga corregida deja601fechas consistentes y el duplicado conserva su aceptación. Checks locales/revisión/build verdes. No modificar esos archivos ni repetir los gates sin una nueva causa.

Siguiente: confirmar zona de exportación de SentiOne (pregunta asíncrona pendiente; los9,131timestamps no traen offset) y reparar las proyecciones de fechas/campos vacíos desde originales antes del análisis. No inferir respuesta ni reimportar para reparar. Continuar trabajo independiente del Compass hacia preparación completa, clasificación y descubrimiento incremental; recibir archivos no prueba BERTopic/Claude/Signal. NOI-12 conserva import/reparación; NOI-17 orientación/cobertura; NOI-27 preparación; NOI-55 entrega. NOI-79 registra la incompatibilidad legacy23514 anterior y no desplaza self-service. Cero gasto nuevo: producto USD11.362961, Advisor USD1.343826. Loop activo; historia inferior conservada, no instrucciones vigentes.

## Entrega anterior — marca nueva, esperando archivos reales

**UAT activo en Studio y Workers: `c40899b01d8b77cae71d712c92054ca71b12be7f`.** [Recibo de entrega y límites](./DELIVERY_SELF_SERVICE_PRE_IMPORT_2026-09-07.md). Health profundo200 y alta autenticada actualizada, sin errores de consola. Root checks y PostgreSQL local real pasaron. El loop existente queda **PAUSED** sobre esta tarea hasta la entrada de datos.

La siguiente acción del operador es crear su marca/prospecto nuevo y cargar CSV originales de SentiOne mediante `/studio/brands/new` → Contexto → Topics e intereses → Importar. No hay una nueva marca activa elegida en esta tarea ni menciones cargadas por el agente. **No reparar, recuperar ni operar Laika/Alexa como atajo.**

Al recibir los archivos por UI, verificar import/Worker y continuar preparación genérica, corpus completo, clasificación, descubrimiento e incrementalidad del Compass. No dar por hechos estos stages. Acceso integral de cliente sigue NOI-19. Saldo producto11.362961 y Advisor1.343826, cero llamadas pagadas en este corte. Los snapshots y gates anteriores conservados abajo son historia.


## Activación — 7 septiembre 2026, marca nueva

El operador autorizó comenzar y avanzar hasta necesitar sus menciones reales por la UI. Esta instrucción sustituye el límite anterior de preparación documental; el contexto y las entregas históricas permanecen como evidencia. **No reparar ni recuperar Laika/Alexa, no insertar datos hardcodeados ni fixtures en UAT.**

Primera integración en curso: navegación Contexto → Topics → Importar; intereses guardables antes del corpus sin embeddings; preparación genérica de carga manual con derechos reales, slots de marca/competencia/categoría y transporte durable existente. El acceso cliente completo sigue abierto en NOI-19: capacidades scoped de API no equivalen a que el shell, alta y todas las acciones de Brand OS ya admitan clientes. No abrir Studio entero como atajo.

Tres agentes acotados trabajan sobre el mismo worktree focal `907d485`: `self_service_frontend` (pantallas/i18n), `self_service_backend` (Topics y capacidades), `self_service_import` (setup/import). El orquestador integra, revisa y entrega sólo el cambio focal a UAT. Los tres archivos ajenos de `signal-topic-contract-drafts` se preservan. Las solicitudes antiguas de tareas en app no son ejecutores duplicados.

El loop existente está ACTIVE sobre Noisia V02 MAIN y se pausará al necesitar la carga real del operador. Sin producción ni nuevos proveedores pagados. Saldo mínimo producto USD 11.362961; Advisor separado USD 1.343826, sin cambios.


El operador aprobó la auditoría y fijó el [Compass self-service](./COMPASS_SELF_SERVICE_2026-09-07.md): crear marca → contexto/intereses → importar marca/competencia/categoría → cómputo completo → descubrimiento/Claude → Topics editables → excepciones → Signal. La segunda/tercera importación con clasificación a existentes y aparición de emergentes es obligatoria para el núcleo, no un opcional posterior.

Se prepararon [plan](./PLAN_SELF_SERVICE_MONITORING_2026-09-07.md), [orquestación](./ORCHESTRATION_SELF_SERVICE_2026-09-07.md), memos Frontend/Backend y [mapa Linear](./LINEAR_SELF_SERVICE_MAP_2026-09-07.md), bajo NOI-73. 45 tickets existentes enriquecidos y seis nuevos; historia/recibos conservados. No se hicieron cambios de producto, nuevas llamadas pagadas ni despliegues. Loop PAUSED. Esta petición termina en documentación, organización y aviso de listo antes de iniciar implementación.

La operación futura debe funcionar con actor cliente autorizado por workspace; gestión sólo `noisia_internal`, límite64, primer chunk900 y follow de catálogo completo son pendientes de producto registrados. No declarar autoservicio por pantallas ya visibles ni escalar elevando sólo constantes.

---

## Estado anterior conservado — auditoría de Admin y descubrimiento terminada

La petición vigente fue una auditoría, ya terminada: [AUDIT_ADMIN_AND_DISCOVERY_2026-09-07.md](./AUDIT_ADMIN_AND_DISCOVERY_2026-09-07.md). No se hicieron cambios de producto, despliegues ni experimentos de clasificación. Loop PAUSED. La consulta de esta auditoría a Claude, con Fable 5.1 como asesor, costó USD 0.451370 del presupuesto separado de auditoría; no alteró el saldo de producto.

Corrección del alcance anterior: la incorporación de Laika proviene de una taxonomía histórica de Claude sobre una muestra de 100 menciones; no prueba una corrida BERTopic nueva. Los diez candidatos V2 de Alexa sí proceden de la interpretación de clusters históricos. El laboratorio existe, pero el importador de este corte está fijado a ese run y Admin no permite descubrir otro corpus. El siguiente corte del dictamen es una propuesta, no una reanudación automática del desarrollo.

## Entrega UAT conservada, con alcance corregido

La entrega de [PLAN_TOPICS_TO_SIGNAL_UAT_2026-09-07.md](./PLAN_TOPICS_TO_SIGNAL_UAT_2026-09-07.md) está completa en UAT. Railway sirve `907d485a9cde309c98fe2c157b539f11a4c0d995` en Studio y Workers; las migraciones aditivas 0127/0128 están aplicadas y PostgreSQL real reconcilia la generación final.

El operador ya puede crear un tópico manual, incorporar un candidato disponible, editar su definición, buscar menciones, corregir pertenencia, seguirlo en Signal, archivarlo y restaurarlo. La aceptación se ejecutó con una incorporación histórica de Laika. Brand OS conserva contexto y límites; el laboratorio conserva BERTopic y Claude conserva interpretación, pero Brand OS no está conectado como guía del agrupamiento actual. Topics es el objeto operativo y Signal consume su clasificación publicada.

La aceptación final usa los dos casos del plan: `Fricciones posteriores a la compra` y `Entrega y logística de pedidos`. Publicación `d12c0c49-91eb-4a48-aaf4-69374410c8f9`, generación `a3bb8bdf-2a16-43b8-9549-28c43bee2431`, perfil v7, denominador 192, 12 raíces aprobadas y 13 asignaciones. El tópico manual aporta 9; el descubierto aporta 4; una raíz es multilabel y no duplica el denominador. Signal muestra los mismos conteos y evidencia navegable.

La política automática se endureció en `907d485`: exige al menos dos positivos y dos negativos no vistos. Como UAT no tiene todavía ese holdout, el sistema se abstiene; las 13 asignaciones publicadas son correcciones humanas y las coincidencias restantes quedan como duda. Esto retiró los falsos positivos promocionales que el umbral anterior había servido como relevantes.

El único transporte nuevo fue una llamada `voyage-4-large` para cuatro embeddings de definición: 1 llamada completada, 1,157 microUSD = USD 0.001157, sin Claude y sin reintentos pagados. Saldo mínimo reconciliado: USD 11.362961. QA autenticado pasó en ES/EN y 390/740/escritorio, sin overflow ni errores de consola. Typecheck, lint, tests focales/estándar y build Studio están verdes. Los tres archivos locales del trial anterior permanecen fuera de los commits. Producción no fue tocada. Loop verificado PAUSED.

Recomendación actual: primero descubrimiento repetible desde Topics sobre un corpus nuevo, con contexto, procedencia y resultados relevantes comprobados. Después import incremental y publicación multiámbito; luego reportes con agente y MCP. El inventario, errores visibles y capacidades sin montaje están en la auditoría; no se reabren los checks válidos de la entrega anterior.

---

## Historia anterior — no instrucción vigente

# CURRENT — LAB-3E-C-TRIAL — genuine suggestion available; baseline trial in progress

C-UI/C-exec delivered to UAT at exact commit 8be57093ce6ee21a7c2d5aebca176e512117ef0c.
Studio 81ee33d5-5885-44e3-b97f-16e87337bfbc and Worker ee4bccdd-a28e-4845-af07-1c6189a7bbb3
are ACTIVE. Migrations 0125+0126 applied once and verified. All delivery, local QA, review and
PG gates remain CLOSED; do not repeat them.

Flight 10/10 completed and reconciled: one provider call, 8,219 input and 520 output tokens,
USD 0.010819 settled. This flight has zero unsettled reserves and no unknown cost/outcome.
Aggregate conservative balance is USD 11.364118 of USD 20. Prior unknown reserves remain intact;
only this flight's USD 1 temporary reserve was released and replaced with actual cost.
No more provider calls are authorized; the ten experiments are used.

The genuine suggestion and its seven own citations are available in UAT. Root saved the
original suggestion as an ordinary draft and is running its baseline deterministic trial.
Next comes a measured edit, save and comparison trial. Preserve baseline results and assess
relevant/irrelevant examples; lexical matches alone do not prove semantic precision.
No production, Topic adoption/publication/serving or automatic activation.

Terminal evidence: `.data/signal-topic-evaluation/lab-3e/flight-after-7a8a9bc004581e884426be4fa73941d0e9a8e0f0403643719e124abb9d6073a4.json`.
That terminal receipt predates the saved draft/trial; current editor progress comes from root.


## Nuevo hallazgo UAT — corregir sólo la prueba léxica

El vuelo terminó y su coste está liquidado. La primera prueba del borrador original
agotó el límite SQL de 15 segundos y no guardó recibo; no existe baseline numérico.
La regla editada ya quedó guardada: términos futbolísticos observados AND Alexa,
es/MX, sin exclusiones arbitrarias ni filtro de scope. Root espera la corrección
focal de rendimiento antes de probar esta revisión. Investigar/validar preparación
repetida de tsvector; reutilizar patrón existente de cohortes. No ampliar timeout,
no repetir proveedor, no reabrir CUI/Cexec/PG cerrados.

---

# Superseded CURRENT — LAB-3E-C-exec — functional rule suggestion through existing Worker

C-UI CLOSED local e0cbc185254b6e6bb6db79f0356ac11dad99748b. Final QA/check evidence in
`.data/topic-rule-suggestion-ui-qa/`; no PG rerun required. Implement reviewed planC execution:
one purpose-specific durable execution record, actual evidence navigation, server budget cap,
existing queue/Worker, genuine B receipt, Studio launch/status. Root Studio; DB/Worker agents
in separate scopes. Local fake transport first, zero paid calls/remote operations in this step.
Next focal UAT delivery then one sealed real candidate cappedUSD1 inside9/10attempts and
USD11.374937 balance. Do not repeat C-UI closure or prior A/B/3D gates. Keep ordinary copy/save/
trial separate from generation, exact recovery and dirty form preservation. No activation.

---

# Superseded CURRENT — 69B.5I-E-LAB-3E-C-UI — integrate the editable suggestion in the existing drawer

**Coordination resumed in new task `01a079df-5eea-7a32-a910-f185e5f1d484`, 2026-09-07.**
The existing heartbeat is ACTIVE and retargeted through the app. Finish the remaining
C-UI QA/checks and focused commit; the handoff pause below is historical. Read
`HANDOFF_2026-09-06_NEW_CHAT.md` before resuming. C-UI is implemented/uncommitted; full code
and C-reader PG independently PASS. Final lost-response fix also independently PASS and actual
browser recovery now proves identical body/key and one synthetic write. Remaining late-scope,
POST-error/responsive/ES-EN final QA plus final checks/commit are listed in the handoff. Do not
repeat closed A/B/3D or actual C-reader proof. No C-exec or paid call opened. Old automation is
PAUSED so only the new chat should resume orchestration after it has adopted this state.

B CLOSED local independent PASS on e4db7ba9622e0a816bc0f06a53d456c74de2ccb3. Final PG
da118b2356f5ba266f7ee3f41e0f6e19903f5978d632975c6cc68d06f93d5557 covers exact0125
2f3561aa,34checks/fullrollback/sourceexact. DB169PASS/28explicitSKIP,11TC/lint green
(15existingwarnings), independent P0/P1/P2=0. Do not repeat B proof or closed3D/A.

Implement ONLY C-UI from PLAN_LAB_3E_C_SUGGESTION_PRODUCT_INTEGRATION.md, fully reviewed.
Work in /Users/brandhon_o/Downloads/noisia-topic-uat-cut-2026-09-06, existing branch;
preserve all unrelated main-repo user work. Extend current TopicCandidateRuleDraft and
minimal manager callback. New browser-safe management/API/product helpers, GET exact
rule-suggestion receipt and POST save|restore via B, localized es/en copy, OpenAPI/tests.
Add only a small scoped DB reader for this receipt's own current citations and an ordinary
prior-rule restore target if needed; do not expose historical naming refs as new evidence.

One local fixture: open/read explanation+cites → Use copies lexical/filters only (0POST)
→ edit → save linked ordinary draft → reopen → restore as a NEW draft → trial separately.
No forced human reason/rationale/confirmation. Preserve scopes/languages/markets, candidate
identity, original proposal, current rights and CAS. Getter RRRO; command SERIALIZABLE with
validation before commit. Persist exact pending body/key/scope for manual same-key recovery;
never overwrite dirty fields/rebase silently or accept late responses from another scope.
Show local_fixture as Simulación local. Generation capability false with honest explanation;
NO HTTP fixture receiver, paid launch button, provider call, Worker or new execution DDL.

Focal/standard Studio+DB tests, typecheck/lint/build, actual component QA390/740/desktop
and fresh independent audit before a focused local commit. Root owns local rollback proof
on existing DB if reader behavior requires it; no fresh clone/full benchmark/large backup.
No remote/push/deploy/UAT mutation in this implementation gate. UAT remains3D d9a9ce7,
Workers unchanged; ten candidates are not activated. C-exec is the NEXT distinct gate,
with actual purpose-specific dispatch and budgeted proof, never a fixture presented as AI.
Nine of ten experiments, USD11.374937 conservative remaining; this gate USD0.

---

# Superseded CURRENT — 69B.5I-E-LAB-3E-B — local suggestion receipt and ordinary draft bridge

Recovery note2026-09-06 16:22CST: final localproof PASS in private receipt
da118b2356f5ba266f7ee3f41e0f6e19903f5978d632975c6cc68d06f93d5557, independently audited
P0/P1/P2=0. FinalDDL2f3561aa/helper24c388ce covered:34checks,84reads,outerrollback/sourceexact,
zero effects. Existing Docker/PostgreSQL running; no active proof or new clone. Final standard
checks and localcommit remain pending before B delivery closure/C-UI. Do not repeat the proof
or closed3D/A. UAT deephealth22:21:23Z HTTP200; no new deployment/provider/activation.

LAB3E-A CLOSED independent PASS P0/P1/openP2=0, local commit
9adee218b26d068b6bbff5bf79ad9b20450e1027 on product worktree
/Users/brandhon_o/Downloads/noisia-topic-uat-cut-2026-09-06. Six-file reviewed cut, clean tree.
QE15focal/372standard,11typechecks/lint PASS (15existing warnings). Context18KiB preserves
40Brand metadata and usable evidence; shared eligibility hides revoked refs. No DB/provider/UAT effect.
Do not repeat closed3D release: UAT remains d9a9ce7/3af84ebb,0124 once, postQA5feead07,
10pendingrev1 and no drafts/trials/cohorts/editorial; Workers012026e1/75f0873 unchanged.

Implement reviewed PLAN_LAB_3E_B_SUGGESTION_DRAFT_BRIDGE.md (independent plan9e32a852 PASS).
Only new DB suggestion module/unit tests/supplied-client PG helper, DB exports/schema mirror,
test discovery and one additive0125 migration for suggestion receipts+draft links. Root owns
product docs/private local proof. No edits to0123/0124/legacy Lab migrations or QE A unless a
causal integration mismatch is reported. No UI/HTTP routes, Worker, provider or remote changes.

Fixed local_fixture origin only. Closed fixture template has lexical/filters/explanation and
citation_count, never caller context/refs/verified/callback authority. Server loads exact
candidate/run/snapshot/CAS/current BrandOS and fresh mention evidence, verifies rights/content,
and constructs simulated output for A. Store distinct fixture/output/context/provenance digests,
not arbitrary prompts or full mention text; no paid-model claim/flight/reservation/permission.
Existing historical citations are not new navigation. Empty BrandOS stays explicit; DB/auth
failures do not become empty. Snapshot chosen by navigation must equal exact source run.

Append-only scoped receipt and ordinary createSignalTopicContractDraftV1+link in one caller-owned
SERIALIZABLE transaction/savepoint; same-key replay same actor/request, exact candidate/draft CAS.
Editing keeps original suggestion. Restore prior lexical/filters as a new ordinary revision;
no fake empty draft or editorial undo. GET is coherent read-only, no FTS/measurement/DML/provider,
and rechecks current source/evidence exposure. No activation or active profile per candidate.

Root runs one existing-local-PG proof on noisia_topic_eval_lab_20260906_1cd623f0d548 at loopback55439.
Install audited0123/0124 dependencies and new0125 inside same unconditional outerROLLBACK,
no ledger/commit/new clone. Prove receive/reopen/save/edit/restore/replay/atomic link failure,
negative authority/CAS/content/rights/directSQL, cap100 separate trial and two DISTINCT candidates
through existing catalog. Reconcile baseline exactly after rollback. No repeat21k benchmark.
Fresh independent diff/DDL/contracts/proof audit plus DB/focal/standard/typecheck/lint before close.

Next: smallest editable suggestion UI and real purpose-specific execution, separately specified.
Do not spend last experiment for fixtures. Nine of ten paid experiments; USD11.374937 conservative
remaining; current USD0. No production, Topic adoption/publication/serving, Discovery Review or
broad Front redesign. User has authorized continued work and reviewed UAT delivery; no generic
reauthorization question. Keep existing UAT delivery intact while building B locally.

---

# Superseded CURRENT — 69B.5I-E-LAB-3E-A — automatic RuleSpec suggestion adapter, local only

LAB3D CLOSED dual PASS at 2026-09-06T13:46:17Z. Studio exact commit
d9a9ce78554407f4eba6532fff5827019d6bf1ed / deployment3af84ebb-43c1-4d96-9816-21c548496b0c
Active/successful, deep health all ok. 0124 e510ca59 applied once plus ledger; do not repeat.
Independent post-QA receipt5feead077afe9801d1c821c59de185abf058eae321024f2fb972809464dbcf31
equals applied state: ten pending rev1, editorial0, archived proposal1, draft/trial/cohort tables0,
frozen/protected/activation/provider unchanged. Authenticated desktop1280/740/390, cohort disabled
missing-rule state, detail/citations3+5, refresh/Escape/focus, console0 and seven GET200/zero mutating
requests PASS. Workers original012026e1/75f0873 unchanged. Do not repeat closed gates or imports.

Implement only section A of reviewed PLAN_LAB_3E_AUTOMATIC_RULE_SUGGESTIONS.md on existing
product worktree /Users/brandhon_o/Downloads/noisia-topic-uat-cut-2026-09-06. Independent plan
review P0/P1/openP2=0. New pure QE module + tests + exports only; reuse existing RuleSpec schemas,
normalization, digests/compiler. Closed model suggestion lexical/filters/evidence_refs/explanation
or insufficient_evidence. Server context owns saved label/definition, CAS, candidate/run/snapshot/
session and Brand OS authority. The adapter checks consistency but never claims DB authentication.
Pinned candidate and available Brand OS context must survive optional-history compaction within
18KiB. Empty/unavailable Brand OS stays explicit and does not automatically reject a useful
mention-supported rule. Reject cross-context/unavailable/duplicate cites and unknown authority
fields; keep historical refs separate from newly read mentions. No opaque verified=true shortcut.

Return canonical RuleSpec/spec digest/suggestion digest/CAS/provenance binding, without I/O,
save, trial or provider transport. Test integration with existing parser/compiler, boundaries,
deterministic replay, preserved identity, edits and evidence/empty context. Relevant standard
QE/focal tests, all-repo typecheck/lint and diff check; fresh independent code review before close.
No Studio build or another database clone required for this pure module. Root owns docs and
post-release report; implementer owns only the three QE files. One focused local commit after
review; no push/deploy in A. Read-only planning of B may proceed, not B implementation yet.

Next B will use the smallest honest product receipt and ordinary draft bridge; do not bulk-copy
Lab migrations0116–0121 or invent a new permissions/agent framework. C later integrates the
editable suggestion UI and budgeted execution as separately specified slices. Do not spend the
last experiment for A/B tests. Nine of ten paid experiments, USD11.374937 conservative remaining;
current cost USD0/provider calls0. No production, Topic adoption/publication/serving, Discovery
Review redesign, broad Front or Workers/settings mutation. Keep UAT's delivered state intact.

---

# Superseded CURRENT — 69B.5I-E-LAB-3D — reviewed 0124 and Studio-only UAT release

LAB3D_PREFLIGHT_SHA256=sha256:bd9305572bbdb23508184467e520a9470fa176548b182cda4f245354874a2ec6
LAB3D_MIGRATION_SHA256=sha256:e510ca59a6f444990d263d306a3ee5bbac1c3e19ef4bf29d3f472333ea76eff7
LAB3D_RELEASE_LABEL=20260906-topic-rule-cohorts

Fresh RRROpreflight13:28Z PASS. Railway UI and gitremote confirm Studio releasebranch
bd7dbf9/059d80ed Active; Workers originalbranch75f0873/012026e1 Active. Deephealthallok13:26Z.
Independent private runner review closed P0/P1/P2=0 before invocation.0124absent/0123intact.

0124 applied ONCE with ledger; apply receipt014f6ac409eca6e8b180dc57574c00beb4e563d4ce2bf2173325b9071e1281a7.
Never repeat apply. Postapply RRROverify69686921d4019813ffdb697c6ad2a93f10de5d5ea2dcc1bbc7bb9afaae306963
PASS at13:33:46Z, baseline/effects preserved. Exactd9a9ce7 Studio releasebranch push started.

LAB3C CLOSED independentPASS P0/P1/openP2=0, reviewed18-file localcommit
d9a9ce78554407f4eba6532fff5827019d6bf1ed above3B3e4708b. Productworktreeclean.
Studio520standardPASS/1expectedSKIP,26focalPASS,DB158standard27explicitSKIP,
all11typechecks/lintPASS15existingwarnings,buildPASS. ActualPGreader34ff5855,
actualcomponentQA lostresponse/samekey/oldscope/hydrationCAS/frozenlabels/390/740/1280PASS.
No provider or UATeffect in3B/3C. Do not repeat those closed proofs.

Follow PLAN_LAB_3D_COHORT_UAT_RELEASE.md. Root and fresh independent reviewer must audit
private new runner c32cc14cd23dd1557985829ed9a8fd0065e801f689f6286897b03cd4c5e3a653
before any remote invocation. Then verify Studio/Workers target/branch/currentrestorepoint,
fresh RRROpreflight against exact3ApostQA24cb9cd9;0123once/emptydrafts/trials unchanged,
0124absent. Preregister fresh receipt/label/checksum here before one0124+ledger SERIALIZABLEapply.
0124SHA e510ca59a6f444990d263d306a3ee5bbac1c3e19ef4bf29d3f472333ea76eff7.
Unknowncommit meansreadonlyverify,neverblindapply. No oldrunner/import/clone/largebackup.

After schemaPASS push only exactreviewed3B+3C range frombd7dbf9 to existing Studio branch
codex/noisia-topic-results-uat-2026-09-06. Studio restorebd7dbf9/059d80ed; retain additiveDDL.
Workers originalbranch/75f0873/012026e1 unchanged, no settingschange orWorkersdeployment.
Verify exactnewStudio commit/deployment/deephealth; authenticatedreadonlyQA of tenexisting
candidates, missingrule state/disabledselection, drawer/citations/refresh/Escape/responsive/console.
PostQADB must reconcile0124twotablesempty,0123draft/trial0,10pendingrev1/editorial0/archive1,
21195frozen andactivation/providerdigests intact. No rule/catalog/trial or editorialsubmit inQA.

No production/provider/Topicadoption/publication/serving,DiscoveryReview/broadFront.
Useralready authorized reviewedUATdelivery; don't ask again. BudgetUSD11.374937lowerbound,
ninepaidexperiments unchanged. Afterhealthycut, scope next minimal local automatedclosedRuleSpec/
quality plan using existingcandidates and evidence; don't spend remainingexperimentautomatically.

---

# Superseded CURRENT — 69B.5I-E-LAB-3C — minimal cohort selection, save and joint trial UI

LAB3B CLOSED independentPASS P0/P1/openP2=0. Local reviewedcommit
3e4708b9c6bae753ccef328cf7d24af03071b75f,15files;0124checksum
e510ca59a6f444990d263d306a3ee5bbac1c3e19ef4bf29d3f472333ea76eff7 is NOT applied UAT.
Actual existing-localPG d2d54d8c2ed200e66a9cd2fc8bbd904f892034f924be4f74dc2058fee5190c6b:
21195considered,1105unique/98overlap,8099ms,cap100,threeway517union,33adversarialchecks,
rights10→0→10/noGETwrites/replay0FTS,allrollback/sourceexact. No concurrency-race test claimed.
DB153standardPASS27explicitSKIP,QE357PASS,all11typechecks,lint0errors15existingwarnings.

Implement reviewed PLAN_LAB_3C_COHORT_UI.md on existing product worktree, branch
codex/noisia-topic-cohort-ui-2026-09-06. DB agent adds only exact-run bounded candidate/current
draft summary reader/tests; use pagination and exact selected refresh, not latest-run substitution.
UI/API agent implements minimal existing-manager selection2..15, Guardar catálogo/Probar/Actualizar,
closed bodies/server actor+workspace/route-run binding and CAS, coherent RRROGET andSERIALIZABLEPOST,
same-key manual recovery of uncertain requests, bilingual copy and focused contracts/tests.
No user motive/prose justification/duplicate confirmation. Do not infer RuleSpecs from prose.
Preserve selected CAS across pagination, ignore late responses from a previous workspace/run,
show unsaved/stale/untested/unavailable states honestly and don't prove request completion from
latestGETalone. Keep existing candidate editorial and individual rule drawer behavior unchanged.

Root owns actual localPG reader proof, actualcomponent browserQA390/740/desktop andintegrationdocs;
independent agent audits final diff and evidence. No new storage, writers or0124change without
causal finding. No provider/Workers/remote/UAT/push/deploy/activation in3C. Only afterlocalPASS
open a separate focal0124+Studio-onlyUATrelease; useralready authorized reviewedUATdelivery.
UAT remains bd7dbf9/059d80ed with0123once anddraft0/trial0,Workers012026e1 unchanged.
No Topicadoption/publication/serving/production/DiscoveryReview/broadFront. Nineexperiments and
USD11.374937remaininglowerbound unchanged. Later automation proposes the sameclosedRuleSpec;
0087classification/rootidentity/multilabel andSignalactivation stay a later product stage.

---

# Superseded CURRENT — 69B.5I-E-LAB-3B — local cohort catalog plan and joint lexical trial

LAB3A CLOSED dualPASS. Studio bd7dbf9e00fc14775092d827e8018b6347a2e7b6, deployment
059d80ed-565b-4d76-a5cc-47e8c5ed1bb4 Active/deephealthallok.0123a63a9387 applied exactlyonce.
Authenticated390/740/1280, emptyruleGET, client-onlyedit/refresh/Escape/reopenblank,3+5citations,
console0 PASS. Independent postQA24cb9cd9b791f5f78a8383722948d8e8f16403b2a3b66bba69c3eec353a85e8b
at11:51:54Z equals prior appliedstate. Tenpendingrev1, drafts0/trials0/editorial0/provider0;
Workers012026e1 unchanged. Do not replay0123, import, earlier releases or QA gates.

On the existing clean product worktree, branch from reviewedbd7dbf9. Write an executable bounded
local plan for one candidate cohort/catalog draft with exact candidate/revision/RuleSpec/snapshot
bindings, plus one shared-population lexical trial. Review the plan independently, resolve only
causal scope/contract gaps, then implement this local slice. No generic operator reauthorization.
Use a caller-owned transaction for one taxonomy/Nterms/ruleset/profile draft; the legacy writer's
context-only reuse must not conflate changed cohorts. Do not substitute its old100-mention discovery
sample for frozen candidate context, truncate1500-character definitions or invent examples.

Reuse the closed RuleSpec compiler and source-availability checks. Shared trial reports unique
coverage, singles/multiplematches, pair overlaps and bounded examples on frozenmemberships, with
honest considered/not-tested/unavailable/filterexcluded/abstained partitions. Never sum individual
trialcounts as union or claim precision/recall. All writes remain local rollback proof initially.
The two earlier measured rules were alternative versions of ONEcandidate and rolled back; they
are NOT two saved candidate drafts. Tests require distinctcandidate drafts/explicitfixtures.

Manual rule editing remains an escape hatch, not a requirement for every user forever. Later
bounded AI may propose the same closed RuleSpec; no paidcall in3B. Do not create one activeprofile
per candidate or use0087assignments as experimentstorage. Preserve actual classification authority;
its active-profile requirement and current single-result-per-root append semantics belong to a
later dedicated classification integration, not grounds to halt the draft/jointtrial slice.

Scope: local code/contracts/tests/docs and reviewed new additive DDL only if needed. No UAT/remote
DDL/push/deploy, Workers/provider, activeTopic/adoption/publication/serving, production or broadFront/
DiscoveryReviewredesign. Real existinglocalPG proof, relevantstandard/focaltests/typecheck/lint and
fresh independentaudit before any later release. No largeclone/download. BudgetUSD11.374937lowerbound,
nineexperiments unchanged. UpdateSTATE/LOG and humanreport at material transitions.

---

# Superseded CURRENT — 69B.5I-E-LAB-3A — reviewed 0123 and Studio-only UAT release

LAB3A_PREFLIGHT_SHA256=sha256:8c5c8aa66b98513885f2d2a9e1fd14c5620bf3bf18071d2c5508ead2c295015b
LAB3A_MIGRATION_SHA256=sha256:a63a938702f45aac1bd4de00c942d2713b0b598afe051be3d3f662373e81abb8
LAB3A_RELEASE_LABEL=20260906-topic-rule-drafts

Fresh preflight11:37:02Z passed unchanged cb9b50f7 baseline,0123 absent; independently reviewed
runner9c10c27d includes the causally proven decimal-compatible generic activation digest.

LAB-2Z CLOSED independentPASS P0/P1/openP2=0, localcommit
bd7dbf9e00fc14775092d827e8018b6347a2e7b6 on reviewed4e64dc4. Clean product worktree,
16-file UI/reader cut. Studio505standard/11focal, DB134standard/17focal, root11typechecks,
lint0errors(15existingwarnings), Studio build and actual local component QA PASS. ActualPG
98f2f41307aa57afac900d997414a632c2d72ffd7f01e9cee728304e3339b6bb proves frozen21195,
latest/null/current-rights/noGETwrites and outerROLLBACK. No new paid call.

Prepare and independently review the narrow private operator runner before executing it.
Fresh pinned-target RRRO preflight must reconcile cb9b50f7 baseline:112/113/115/122 ledger,
frozen/protected/import cohort/10pending rev1/editorial0/provider0 unchanged;0123 absent.
Preregister exact fresh receipt/label/checksum here before apply. Only0123 and its ledgerrow
may commit in one SERIALIZABLE transaction, with original state unchanged and two empty
draft/trial tables,three functions/four guards verified. Unknowncommit is read/verify only,
never blind reapply. No old expired runner/import/capture or another large clone/download.

0123 SHA256 a63a938702f45aac1bd4de00c942d2713b0b598afe051be3d3f662373e81abb8.
App restore point is Studio8739567/deploymentf10b82f2; an additive rollback retains0123tables
and returns Studio to that commit. The retained393MBpre2Varchive is NOT current UAT state.
After schema reconciliation, push exact reviewed two-commit range only to existing Studio
release branch codex/noisia-topic-results-uat-2026-09-06. Prove Workers still original branch
and deployment012026e1 first; no Workers deployment/settings change. Verify exactnewStudio
commit/deployment/deephealth, authenticated read-only empty-rule UI and current citations,
desktop/390/740/Escape/focus. Reconcile DBafterQA,zero draft/trial/editorial/providerwrites.

No provider, Topic adoption/publication/serving, production, broad Front/Discovery redesign.
User already authorized reviewed UAT delivery; no generic authorization question. Budget
USD11.374937 lowerbound/nine experiments unchanged. Do not reopen closed gates.

---

# Superseded CURRENT — 69B.5I-E-LAB-2Z — simple candidate rule editor and trial UI, local only

LAB-2Y CLOSED independentPASS P0/P1/openP2=0, localcommit4e64dc499fe749373dd2df832453b420373770da.
RealPGreceipt2c818556006e28666020daf7ef78e06a5310311f5cae11fcfc3e1bf5f9890f6d proves21195memberships,
broad517/specific8matches,18adversarials, rightsreplay10→0→10 with0remeasurement/newreceipts,
allouterROLLBACK. Compiler342standard/9focal, DB130standard/13focal, TC11packagesandlintgreen.
0123checksum a63a938702f45aac1bd4de00c942d2713b0b598afe051be3d3f662373e81abb8; NOT appliedUAT.

On existing cleanproductworktree and branchcodex/noisia-topic-rule-drafts-2026-09-06, add only
the smallest component inside existing candidate drawer: savedcandidateidentity, explicit
any/all/not phrases bylines (not silently splitcommas/truncated), optionalknownfilters, save/test.
No motive/rationale/duplicateconfirmation, no default/inferredmatcher from editorialprose.
Preserve candidate editorial save/reject/restore/undo and exactcitationbehavior. Show counts/cap/
not-tested/examplesavailability/stale/error states honestly; lexicalmatch is not semanticaccuracy.
UI surface remains candidate management, not DiscoveryReviewredesign or activeSignalTopics.

DB agent adds bounded latesttrialGETreader with currentrights/contentprojection and tests;
GET never measures or writes. UI/API agent owns routes/wrappers/DTO/component/i18n/OpenAPI/tests.
Newtrial route binds draft to routeworkspace/run/candidate before invoking existingcore.
Mutationwrapper owns oneSERIALIZABLEclienttransaction, CAS/idempotency, no blindnewkeyretry;
readprojection uses coherent readonlysnapshot. Never emit rawSQL/errors/secrets tobrowser.

Root integrates actualPGproof of refresh/latest/stale/rights, local actualcomponentbrowser
QA390/740/desktop/keyboard/error/copy and fresh independentreview. Only then commit reviewed
allowlist and open separate0123+Studio-onlyUATcut. No remote/provider/migration/deploy/Workers/
Topicadoption/publication/serving/production now. UAT8739567/f10b82f2 remains healthy; no repeat
0115/0122/import/backup. Ninepaidexperiments,USD11.374937 lowerbound unchanged.

---

# Superseded CURRENT — 69B.5I-E-LAB-2Y — candidate Rule Spec draft and lexical shadow, local only

LAB-2X CLOSED PASS: Studio8739567/f10b82f2 Active, health allok, Workers012026e1 unchanged.
Authenticated exact30+5citation QA and independent cb9b50f7 post-QA receipt passed; ten pending
modelrevision1, no editorial/provider/activation effects. Do not repeat 0115/0122/import/backup.

Implement locally on the existing clean product worktree, new branch from8739567. Minimal cut:
new0123 draft versions and shadow-test receipts only; one draft is bound to an exact candidate
revision/run/snapshot. Reuse actual0112/0115 identity, actor authZ, immutable source and CAS.
Caller-owned transaction; idempotent replay is same actor and exact request with persisted result.
Revisions append; later source changes mark stale. No active catalog/profile created per candidate.

Closed topic-only Rule Spec with literal any/all/not terms/phrases and bounded known filters;
deterministic parameterized PostgreSQL simple FTS compiler. No arbitrary SQL/regex/JS, inferred
matcher from editorial prose, new provider call or narrative classification. Shadow reads exact
frozen memberships with current rights/canonical-root/content checks, deterministic cap/order,
timeout, bounded examples and denominators reconciling processed/excluded/unavailable/not-tested.
Lexical matches are not Topic assignments, not gold precision/recall, and not full-population claims
if capped. Preserve all0087 active-profile/LF/assignment boundaries and legacy record_tags.

Agents own disjoint DB storage/writers/tests and QE schema/compiler/tests. Root owns integration
proof and product docs. Fresh independent review and real existing-local-PostgreSQL rollback proof
before commit or UI/release. No new large clone, no UAT/migration/deploy/Workers/provider/production
or Topic adoption/publication/serving in this gate. No generic authorization question; user already
authorized continued development and later reviewed UAT. Nine experiments; USD11.374937 unchanged.

---

# Superseded CURRENT — 69B.5I-E-LAB-2X — reviewed Studio-only citation release

LAB-2W local PASS: independent P0=0/P1=0/openP2=0, DB16focal/117standard+26skip,
Studio34focal/494standard+1skip, typechecks/lint/final build green. Real PG35refs and actual
local browser lazy-read/metadata/unavailable/pagination/separate collections/Escape/responsive PASS.
Exactly19product files on clean evidence branch, based on current deployed dc48104.

Commit the reviewed allowlist and fast-forward only the existing Studio release branch
codex/noisia-topic-results-uat-2026-09-06. Workers must remain on original branch and012026e1.
Verify exact Studio commit/deployment/health, authenticated10candidate citations and archived
proposal evidence, historical panel collapsed/read-only, no editorial changes. No new migration,
import, backup download, provider call or settings/Workers mutation. Healthy dc48104/55c15747
is the existing application rollback point; database unchanged. User already authorized this UAT
delivery. After PASS open separate local draft Topic Contract/lexical shadow test implementation;
do not activate Topics or spending. BudgetUSD11.374937/nine experiments unchanged.

---

# Superseded CURRENT — 69B.5I-E-LAB-2W — readable candidate evidence, local implementation

LAB-2V CLOSED PASS: Studio deployment55c15747 Active on dc48104, deephealth allok, Workers
012026e1 unchanged. Exact imported10pending candidates/1historical proposal are visible in
authenticated UAT. Copy-without-save/Escape/reopen and390/740/1280layout PASS, consoleerrors0.
Independent postdeploy receipt739226cf46caf3e749cf0365aded51ddcb278ce5e79f0846f4acd2dc90c75d93
proves all ten model revision1,0editorial revisions/ops/events, unchanged frozen/protected hashes,
0115/0122 exactlyonce, no provider/cost/activation. DO NOT REPEAT import/migrations/backup.

Implement locally on clean product worktree /Users/brandhon_o/Downloads/noisia-topic-uat-cut-2026-09-06,
new local branch from dc48104. Add a lazy read-only exact-citation endpoint and display candidate
versus archived-proposal evidence separately in the existing drawer. Run-key/candidate/snapshot
scope is server-owned; no arbitrary mention-ID input, no latest-snapshot substitution. Use current
rights, canonical root/text hash and existing sanitized600char excerpt projection. Expose explicit
unavailable citations, bounded20-item pages/cursor and safe metadata. Preserve all cited references;
do not retrospectively constrain original refs by source_cluster_keys. No0118/live session needed
for historical suggestions. Show existing original explanation separately from suggestion rationale.

Also clarify the existing legacy evaluation card as historical when the new result exists;
do not change its actual receipt/counters, enable a retry, or redesign Discovery Review.
Root owns orchestration/docs. Agents split DB reader/tests and Studio API/UI/contracts on disjoint
files; independent fresh audit after tests. No schema/migration/Workers/provider/import writer/
editorial writer/Topic activation changes. Verify real local PostgreSQL rollback and focused+standard
tests/typechecks/build, then reviewed Studio-only UAT deploy and authenticated read-only QA.
Do not push the currently connected release branch before review. No additional paid experiment;
nine attempts and USD11.374937 conservative remaining unchanged. Broad Front stays paused.

---

# Superseded CURRENT — 69B.5I-E-LAB-2V — focused UAT release preparation and execution

LAB-2U real PostgreSQL proof and independent audit passed (P0/P1/P2=0). Portable artifact
b801de32bb3ba0e87a854eb45b4324c56cda31713d7da36ad252af2b0bdcf75e contains10candidates,
12turns/11retrievals/30citationlinks/10rankings and1historical refinement. Source output digest
d2057c30635108ca7486126fb52051c244ea6ff5e8f3f566e5d679124f137219 is reconstructed exactly.
0122 checksum460843ae14aab222080e2ae51fcd1295f39586679887da44af49ba50766597a9.
All import/editorial proof writes rolled back; original Lab output and ledger are intact.

Product-only0000–0113+0115+0122 schema proof and clean26-file release audit passed. Commit
dc48104b532517a757b403886fe4a5f1cc5cce14 was pushed ONLY to new branch
codex/noisia-topic-results-uat-2026-09-06. The dirty Lab branch and original remote75f0873 remain intact.
Fresh target/restore/ledger/frozen proof passed; private393358199-byte dump verified. Only0115+0122
and exact b801historical import ALREADY COMMITTED in UAT; DO NOT REPEAT APPLY. Separate RO verify
ce19e64e61213e3cbf7e5ab339ee760371b0d31d8798e01900ee1d2744ee7a45 and independent reconciliation PASS:
10pendingeditable candidates/30citations/1historical suggestion, zero new provider/cost/authorities/
outbox/activation; frozen/protected digests unchanged.

Root selected ONLY Studio's connected branch to the reviewed new release branch, confirmed the
single staged Branch change and clicked Deploy Changes once. New Studio deployment
55c15747-8bb3-46bc-a56c-1ddb6a2dc14e is building; previous healthyStudioa6e8db40 stays available.
Workers must remain deployment012026e1-3c63-4a63-87a0-e7df3f94ea99 on75f0873/original branch.
Wait for exactdc48104Studio success, verify deephealth and authenticated read-only UAT UI at
Amazon Alexa Brand OS. Check10candidates/1proposal/editor copy-without-save, Escape andresponsive
layout; preserve all candidate revisions and provider/activation state. Do not redeploy Workers.
If a mismatch occurs, diagnose only that cause; do not repeat import, backup or paid calls.
After UI PASS, next small local product gate is reading exact cited mention excerpts in the
existing candidate drawer, not a broad redesign or Topic activation. User already authorizes
reviewed UAT delivery and continued local development; do not ask again generically.
No new paid experiment is needed. Nine experiments andUSD11.374937 lower-bound budget unchanged.
No production, Topic activation or broad Front Recovery. Keep evidence and current gate up to date.

---

# Superseded CURRENT — 69B.5I-E-LAB-2U — portable historical result import, local implementation

LAB-2T identified the minimal UAT path: preserve the existing V2 candidate tables and0115editor,
add an explicit imported-result provenance lane, and import the completed ten-candidate artifact
instead of paying for a replay. Latest retained UAT evidence proves0112/0113; later local migrations
are not assumed deployed. No actual UAT health/deployed-commit claim is made by that old evidence.

Implement a new forward-only0122 storage/import cut locally. Imported runs cannot dispatch:
no execution authority/outbox, provider_execution=false, local provider calls/reservation/settlement=0.
Preserve original source telemetry, card, cost and digests in the immutable import receipt; copied
historical token/tool records are explicitly historical, not a new UAT charge. Revalidate snapshot,
workspace, rights, source clusters and each evidence binding. Preserve the complete10-candidate,
12-turn/11-retrieval/30-citation/10-ranking cohort. A bounded historical-refinement receipt/projection
may carry the one completed proposal without creating fake live sessions or bypassing expiry.
No Lab-only0116–0121 on UAT. Remapped destination IDs must have explicitly separate source digests.

Root prepares the portable exporter and local integration proof; delegated DB implementation and
independent review run separately. Reuse existing Docker/local databases; no further4GBclone.
Only after local tests/audit pass prepare action-time UAT target/ledger/restore/deploy reconciliation.
No provider call, remote mutation, Topic adoption/publication/serving, production or Front redesign
in this implementation gate. Nine experiments and USD11.374937 remaining lower bound unchanged.

---

# Superseded CURRENT — 69B.5I-E-LAB-2T — UAT product release gap and bounded implementation plan

LAB-2S is local PASS. Independent code audit P0=0/P1=0/P2=0; DB reader9/9, UI/API15/15,
standard Studio490pass/1skip using import-only127.0.0.1:1DATABASE_URL (no DB connection),
DB85pass/26explicit integration skips, Studio/Workers/DB typechecks and focal Studio lint green.
Real PostgreSQL proof0303627c3b9686092f7fc2d0aa16b47703c36d7b86082991dd50c83240d7c9c3
read the expired-session proposal and exercised save2/undo3/reject4/restore5 plus deferred constraints.
Outer rollback restored10candidates/1proposal/0editorial/0ops/0events and all protected digests.
Actual component browser fixture passed copy0POST, edit/save, reloadstale, undo, Escapefocus,
390/740/1360layout. Narrow minmax-grid fix closed longdigest overflow. No UAT is implied.

Now map the current UAT release gap from actual persisted deploy/migration evidence and repository
contracts. Identify a minimal product storage/runtime path to display the candidate pool and
proposal in the existing editor without replaying Lab-only0116–0121 or fabricating clone markers.
Separate storage/read UI delivery from paid runtime rollout if that creates useful progress.
Validate target/deployed commit/ledger/restore action-time read-only before any later UAT mutation.
No new provider call, blindretry, newdiskheavyclone, production, Topic activation or Front redesign.
User already authorizes continued local development and reviewed UAT rollout; don't reask generic
permissions. Nine experiments, lowerboundUSD11.374937. Implement the next bounded product cut
once its concrete contracts are reconciled, with independent review and tests.

---

# Superseded CURRENT — 69B.5I-E-LAB-2S — existing candidate editor integration, local only

LAB-2R deterministic candidate/Brand OS bootstrap passed fresh independent review P0=0/P1=0/P2=0;
15/15 focal tests, launcher/custody20/20 and Workers typecheck passed. Two required reads precede
the first transport, consume2of12navigation, and fail at zero cost. No new provider call was used.

Implement the smallest product cut: a bounded historical proposal projection in the existing
candidate-detail GET and one proposal block in FullEvidenceTopicCandidateManager. Reuse existing
candidate save/CAS/undo; "Usar propuesta" only copies name/description into unsaved fields,
preserving inclusion/exclusion. Dismissal is session-local, not a persisted proposal rejection.
Existing reject/restore still target the candidate. Show stale-source revision; never overwrite
silently, execute merge/split or adopt/publish/serve a Topic. No new tables, migration or provider.

Backend reader is delegated separately from root UI/DTO/OpenAPI/i18n work. Verify historical
proposals remain visible after navigation-session expiry, tenant/run/candidate isolation,
schema-unavailable/absent states, bounded related candidates, save/reload/undo and immutable source
proposal. Run local tests/typechecks and fresh independent review. Real PostgreSQL checks may
use the existing completed local clone in a rollback-only transaction; do not create another
large clone. No UAT/production, fetch/push/deploy, secret access or paused Front redesign.
Nine experiments dispatched; budget lower boundUSD11.374937. After this cut, prepare a concrete
UAT-capable product release plan rather than replaying Lab-only0118–0121 on UAT.

---

# Superseded CURRENT — 69B.5I-E-LAB-2R — guarantee initial Brand OS context, local only

The ninth real refinement completed and independent quality review confirms a useful draft,
not publication-ready content. Five cited refs resolve to the eight read mentions; the draft
needs to distinguish sponsored unboxing, avoid unverified official provenance and cite or omit
the match-tracking detail. It does not establish prevalence over all56MX mentions or relationships.
See RESULTADO_TOPIC_LAB_2026-09-06.md for the readable result and exact budget.

Local implementation delegated to `/root/refinement_context_bootstrap`, limited to the runner
and its tests: retrieve/trace existing candidate_context and server-derived brand_os_context
deterministically before first paid model call. They count as two of the existing12navigation
calls; no extra tool authority or provider calls. Any bootstrap failure settles zero-transport
definitely_not_sent; preserve all credential/session/auth/citation/activation boundaries and
unchangedUSD1 cap. Do not depend on the model following a prompt to receive required context.

Root independently reviews and runs focused tests/typecheck when ready. In parallel a read-only
code map identifies the smallest proposal-display/acceptance gap in the existing candidate manager.
No new paid flight, UAT/migration/push/deploy, candidate edit or Topic activation in this gate.
After local PASS, record the tested product handoff and proceed with its scoped implementation;
do not reopen clustering or ask for permissions already given. Nine experiments, remaining
lower boundUSD11.374937. Front remains paused.

---

# Superseded CURRENT — 69B.5I-E-LAB-2Q — review completed refinement and prepare product handoff

Flight6e2f5f94cdbaf922 completed at08:21:53Z. Clone1cd623f0d548 now has one appended
proposal85854b9ab4c12dee9396dc2ef05d24729281b31da2943bae561d14d7418d236c:
"Amazon Mexico Alexa/Echo World Cup 2026 Fan Campaign". Usage12271input/795output,
three calls,USD0.048738 settled. Terminal99bf6a2cd8af2f7ebabcf4b50882964985c7015c5f734ff3aac5e2d932416335.
Two actual navigation traces: candidate_context then eight representative mentions filteredMX;
five refs cited. No Brand OS tool, pagination or relationship was used in this paid flight.
Do not overclaim those capabilities as exercised; pagination passed separately in real rollback.
Ten original candidates remain pending/editable, zero editorial/adoption/publication/serving.

Read-only independent quality review is active. Validate every concrete campaign claim against
the actual retained mentions and check relevance, sample limits, duplicates and target unchanged.
Write a legible result and next product step. The experiment now proves useful candidate pool plus
a persisted evidence-based refinement, not a UAT deployment or production Topic activation.
Known settled totalUSD3.155063, unknown holdsUSD4.86, conservative probe bucketUSD0.61:
remaining lower boundUSD11.374937. Nine distinct experiments dispatched; do not launch blindly.

After quality verdict, continue a focused local product handoff: deterministic initial Brand OS
context should not depend on the model obeying a prompt, and display/proposal acceptance remains
separate from Topic activation. Preserve existing editable candidate manager. No UAT rollout
until a reviewed UAT-capable migration/deploy plan; current0118–0121 are local-only. Keep Front
paused and original LAB-2G immutable. Use existing authorization, not duplicate permission asks.

---

# Superseded CURRENT — 69B.5I-E-LAB-2P — one concise refinement successor

**LAUNCHED ONCE approximately08:21Z**, after independent action-time PASS at08:21:11Z.
Flight6e2f5f94cdbaf922 is now in progress; do not replay it on recovery. Read terminal first.
The currentUSD1 reservation leavesUSD10.423675 conservative headroom until settlement.

The SDK completed-result fix is independently PASS P0=0/P1=0. Real SDK max_tokens fixture
reproduced the old unknown classification and verified the fix preserves metered usage; refusal
also produces known-invalid. Historical unknown receipts remain unknown, with full caps held.
Workers suite228 pass/5 gated skips; focal26/26 and typecheck PASS. No shared temperature change.

Fresh direct local clone `noisia_topic_eval_lab_20260906_1cd623f0d548` preserves parent digest.
Host receipt`sha256:eea579595459ff44c52f8fa910eb1cfa9bc25eda71d911839cd97ad29b51236e`;
card`sha256:d5b9ef0de4b2cb9b3c5c52cc041a6f90045ba7dec9940667ab24f26b616088c3`;
flight`topic-refinement-flight-6e2f5f94cdbaf922`, candidate football-campaign revision1.
Existing0118–0121 applied once; all0116–0121 checksum/name parity retained. Budget before
this flightUSD11.423675, capUSD1,12turns. This is the ninth distinct experiment when dispatched.

Finish fresh action-time read-only check, launch once via existing Keychain child, then reconcile
actual terminal and read the proposal/evidence. No additional permission question, no replay of
old flights, no UAT/production/editorial/adoption/publication/serving or paused Front changes.

---

# Superseded CURRENT — 69B.5I-E-LAB-2O — fix the demonstrated known-output receipt gap

Flightbcca632a9a0c2507 terminalized `outcome_unknown` after4 provider calls. Its three durable
navigation traces are candidate_context(refs0), representative_mentions(refs10), brand_os_context.
No proposal/editorial/activation effect. Reserve its fullUSD1; aggregate lower boundUSD11.423675.
The actual failed fourth response is not retained; do not invent its HTTP status or cause.

Provider-disabled SDK inspection and real SDK fixture reproduce a specific defect: generateText
returns metered responses when stop_reason=max_tokens, but result.output then throws
NoOutputGeneratedError without usage. The adapter now catches that getter-only failure while it
still has the completed result, returns empty invalid text and known usage, plus closed
output_limit/missing_output diagnostics. HTTP/network/unknown failures remain ambiguous/no-retry.
The final prompt requests concise name/description/rationale and3–5 decisive refs within the
existing1000-token limit. Closed HTTP categories preserve diagnostic value without raw errors.

Finish independent audit of the narrow adapter/runner correction and regression suite. On PASS,
archive the terminal80d2cb97d6eb host receipt recoverably, create one fresh direct LAB-2G clone,
apply existing0118–0121 once, prepare card with remaining11423675micro, perform fresh action-time
check and execute the ninth distinct experiment withUSD1 cap. This is authorized already, not a
new permission question. Never resend the old flight. Review the actual terminal and proposal.

---

# Superseded CURRENT — 69B.5I-E-LAB-2N — execute one reviewed local refinement flight

**DISPATCHED ONCE at approximately08:15Z:** `topic-refinement-flight-bcca632a9a0c2507`
in clone80d2cb97d6eb. Card digest1960f9562cb812bd766a479ab7e39d939f3e2704f1bab70e8d4a04b8d9ba2053;
independent action-time audit PASS at08:14:49Z, authority expiry08:27:36.797Z. On resumption,
read its durable terminal/claim before any action. Do not run this launcher again. Remaining
lower bound after reserving this flightUSD11.423675. Await and reconcile, then review output.

Independent audit passed P0=0/P1=0 and both navigation P2s are closed. Real rollback proof
retrieved20+20 distinct mentions, cursors352/354 characters passed the actual DB512 constraint,
candidate-context trace refs remained zero, and every original counter was preserved after rollback.
Provider budget is reconciled to at least USD12.423675 after settled3.106325, unknown holds3.86
and a conservatively rounded USD0.61 standalone-probe bucket (bounded historical metadata).

Fresh direct clone: `noisia_topic_eval_lab_20260906_80d2cb97d6eb`; host receipt
`sha256:87952041cf0bc246ddcaa28cb50dd75cda831cc8aa471bb492d6c29412df22aa`.
Parent LAB-2G output digest matches exactly. The former6074882e5bec receipt is recoverably
archived as `host-receipt.archived-expired-unclaimed.json`; no data was deleted.

Complete local0118–0121 apply and card preparation with explicit remaining_micro_usd12423675.
Audit the fresh card/ledger and unclaimed unexpired session, then launch exactly one USD1-max
flight through the existing dedicated Keychain child. This is the eighth distinct experiment
flight under conservative combined accounting. No repeated permission request is needed.
Wait for the actual durable terminal; never resend this flight. Then review the resulting proposal
and evidence, update budget and write the useful product result. No candidate editorial changes,
UAT/production connection, Topic adoption/publication/serving or Front work.

---

# Superseded CURRENT — 69B.5I-E-LAB-2M — recover and complete bounded refinement

The operator resumed overnight autonomous work after an app crash. Preserve the ten useful
LAB-2G candidates. The last card in clone `noisia_topic_eval_lab_20260906_6074882e5bec` expired
unclaimed with zero provider effects. Do not resend it; archive its host receipt recoverably
only after any rollback proof finishes, then create one fresh direct LAB-2G successor.

First finish the two demonstrated navigation fixes and independent real PostgreSQL rollback
proof: candidate-context refs are not newly inspected citations; the signed next cursor must
reach page two through the unchanged 512-character API/DB limit. No applied migration changes.
Recover standalone probe usage to complete the cross-experiment budget. Known settled total is
USD3.106325 and conservative unknown holds USD3.86; stale historical card balances do not govern.

Then create/apply/prepare one fresh local direct successor, seal the actual reconciled balance,
verify exact 0116–0121 parity and zero claims, and execute one USD1-maximum refinement flight
with the already configured dedicated Keychain launcher. The user has authorized this work;
the separate action-time gate is an orchestrator transition, not another permission request.
Reconcile its durable terminal, evaluate the proposal against retrieved evidence, and continue
the product plan without activating Topics. No UAT/production/adoption/publication/serving or
Front action is part of this local refinement. Never reuse an ambiguous or claimed flight.

---

# Superseded CURRENT — 69B.5I-E-LAB-2L — prepare one fresh provider-disabled successor after root-schema correction

The prior local refinement flight `topic-refinement-flight-be2462bcc941ce99` remains terminal
`outcome_unknown` and immutable. It must never be retried, compensated, altered or used as the
source for a second dispatch. Its USD 1.00 hard-cap remains conservatively reserved because its
settlement is unknown.

Provider-disabled diagnosis and a fresh independent audit identified a causal protocol defect:
the candidate-refinement decision was a root discriminated union. Anthropic structured tool input
schemas require a root object. The correction wraps the decision in a strict `{ decision: ... }`
envelope and unwraps it only after a known response. A serialized fixture proves `input_schema.type`
is `object`, has the single `decision` property and omits `temperature`, `top_p` and `top_k`.
The correction also makes a locally rejected later turn settle prior known usage as a valid
`provider_response_invalid` receipt; only a first pre-transport rejection remains zero-cost
`definitely_not_sent`. Focused tests pass 8/8, DB/Workers typechecks and diff checks are green;
fresh independent audit is P0=0/P1=0.

Archive the terminal prior host receipt recoverably, then create exactly one direct physical clone
of immutable LAB-2G output. Reconcile its output digest and frozen `21,195 / 115 / 116 / 11,186 /
10,009` authority. Apply 0118–0121 exactly once only to that disposable fixed-loopback local clone,
then prepare a provider-disabled proposal-only successor card for one still-pending candidate. It
must retain the USD 1.00 hard cap, 12 bounded turns and 12 opaque evidence navigations, and must
have zero dispatch claims, terminal receipts, proposals, candidate editorial, UAT, production,
adoption, publication and serving effects.

Obtain a new independent P0/P1-free audit of the actual clone, exact ledger/card and correction
before any action-time credential read. This gate authorizes no provider call, no Keychain read,
no UAT connection, no migration outside the disposable clone, no Discovery Review mutation and no
candidate/Topic activation.

---

# Superseded current — 69B.5I-E-LAB-2K — diagnose one terminally ambiguous local refinement flight before any successor

The dedicated local Keychain item is now present. A fresh, independently audited direct clone
`noisia_topic_eval_lab_20260906_bb5cda6df853` was created from the immutable LAB-2G output,
with exact 0116–0121 ledger parity and a single USD 1.00 proposal-only flight. The launcher
claimed and dispatched it once. Its durable terminal receipt is `outcome_unknown` with
`provider_call_count=1`, no settled amount, error code `topic_refinement_provider_ambiguous`,
zero model trace/proposal rows, ten original pending candidates, zero candidate editorial changes,
and zero UAT, adoption, publication, serving or production effects.

This receipt is terminal evidence and **must not be retried, compensated, overwritten or reused**.
The historical completed LAB-2G run still has 12 provider calls; that number is not a new spend.
Reserve the new USD 1.00 cap conservatively until a provider settlement can be reconciled.

Perform a provider-disabled local diagnosis only. Reconcile the exact launcher/child/provider
boundary and model identifier against code and deterministic tests. The stored safe domain error
does not preserve raw provider response content; do not invent an HTTP status or charge. If a
causal correction is found, it must preserve the closed error taxonomy: local pre-transport
configuration errors can be `definitely_not_sent`; every HTTP, network/reset or unknown provider
outcome remains ambiguous and retry-free. No credential value may be printed, copied, stored or
read outside action-time execution.

Only after a focused correction (if one is causally proven), local tests and a fresh independent
P0/P1-free audit may a new direct clone and a separately sealed successor flight be proposed.
That successor is not authorized by this diagnosis gate. No provider dispatch, migration apply,
UAT/production connection, Discovery Review mutation, candidate editorial change, Topic adoption,
publication or serving action is authorized now.

---

# Superseded current — 69B.5I-E-LAB-2J — install dedicated local credential, then regenerate a fresh sealed flight

The direct physical-copy route remains the sole execution route. It clones the completed local
LAB-2G database `noisia_topic_eval_lab_20260905_20e4b67a137a`, whose immutable output is one
completed run, 12 model turns, 11 retrievals, 10 pending editable candidates, 30 evidence links
and 10 rankings. Its output digest is
`sha256:1a473971787650acc71f1631e0e30940b81397f181ae89074e1dee591eac960b`.

The prior direct clone `noisia_topic_eval_lab_20260905_48d4930f3e2d` has already completed a
provider-disabled 0118–0121 proof and terminalized its sole proof flight as `definitely_not_sent`.
It is preserved as evidence and **must not** be used for a provider request.

The fresh card for `noisia_topic_eval_lab_20260905_f0d940cb5269` passed independent audit with
`P0=0/P1=0`, but its action-time launcher stopped before a claim because the current macOS user
does not have a Keychain item with service `noisia.topic-refinement-lab.anthropic`. UAT's two
service variables are configured, but they must never be extracted or reused in this local lane.

The operator must install a newly issued dedicated local Lab credential in that Keychain item.
It is an external configuration step; do not log, write or copy its value into this repository,
UAT, environment file or prompt record. The expired/unexecuted f0d clone remains evidence only.

After verified presence only, archive the f0d host receipt recoverably. Then create exactly one new successor using
`create-signal-topic-candidate-refinement-successor-clone-v1.ts`; it must receive a new 0600
host receipt bound to the fixed loopback Docker container and preserve the parent output digest
byte-for-byte. Apply 0118–0121 exactly once using
`apply-signal-topic-candidate-refinement-successor-control-plane-v1.ts`, with the literal
local confirmation. Reconcile 10 pending candidates, 30 evidence links, 0 candidate edits,
adoptions, publications, serving effects, UAT connections and provider calls.

Then prepare—not execute—a single proposal-only refinement session and flight card. The card is
for one pending candidate, uses opaque server-owned paged evidence tools only, permits at most 12
bounded Claude model turns and has a USD 1.00 hard cap. It may persist only an append-only refinement
proposal; it may not edit a candidate, adopt/publish/serve a Topic, touch Discovery Review or
contact UAT/production. A new no-edit audit must report `P0=0/P1=0` after the actual fresh clone
and flight card exist. Only then may the launcher read the dedicated local Keychain credential
once at action time and dispatch the single flight. HTTP, network or unknown outcomes are ambiguous
and do not retry automatically.

---

# Historical — 69B.5I-E-LAB-2D — sealed local full-corpus Topic evaluation

The first persistence rehearsal exposed a real local contract gap: `evaluation_brief` was a valid
server-owned navigation response but was absent from the append-only retrieval ledger's allowed
operations. This is now corrected only in the anchored disposable Lab by forward-only migration
0117, applied exactly once after proving the 0116 ledger and clone marker. UAT remains untouched.

The model's first turn receives a bounded `evaluation_brief`: approved Brand OS elements plus up to
24 scope-balanced **historical proposals** from the full frozen BERTopic population. It does not
receive raw corpus text in that brief; it must next navigate bounded representative mentions before
it can form an evidence-linked candidate. The 10,009-message outlier reservoir remains navigable,
but is not a bootstrap proposal.

Independent audit is now PASS `P0=0/P1=0`. When the running process is injected with a newly
issued dedicated `NOISIA_TOPIC_EVALUATION_LAB_ANTHROPIC_API_KEY`, execute exactly one sealed local
evaluation: at most 12 model turns, 24 evidence navigations and USD 2.10. The normal run checks
both the 0116 and 0117 exact ledgers before durable claim. It persists only local pending editable
candidates, evidence links and a Top-10 projection; it never adopts, publishes or serves Topics.

The current process has no Lab credential configuration, so do not dispatch or substitute a
historical/pasted key. Once one is present, run the preflight, record the literal confirmation and
fresh idempotency key only in the process environment, reconcile the terminal receipt, then audit
the editable candidates for at least ten coherent, Brand-OS-relevant, evidence-backed candidates.

No provider request has been made in this gate. Do not access UAT, production, queue, Discovery
Review or a remote release path. If the result is weak, inspect candidate relevance and open the
next local algorithm/handoff iteration before another paid call.

# Previous — 69B.5I-E-LAB-2B sealed disposable full-corpus Topic evaluation

LAB-2A is independently PASS `P0=0/P1=0`. Commit `57fda53` applies 0116 exactly once only to the
externally anchored disposable clone and preserves the UAT profile/outbox unchanged. The Lab is
not a release gate: UAT executor availability, deployments and release controls do not block it.

Execute exactly one local disposable full-corpus evaluation only when the current runner process
has a newly provisioned dedicated `NOISIA_TOPIC_EVALUATION_LAB_ANTHROPIC_API_KEY` (never a
historical/pasted key), the closed Lab profile, literal confirmation and a fresh idempotency key.
The runner checks the pristine clone and 0116 ledger, reads the key once immediately before durable
claim, and reconciles terminally. Bound the single evaluation to 12 model turns, 24 evidence
navigations and USD 2.10, within the USD 18.147816 aggregate remaining ceiling. Do not dispatch
without that dedicated credential; record its absence as an external configuration prerequisite,
not as a product or migration blocker.

Persist only locally pending editable candidates, evidence links and a Top-10 projection. Never
adopt, publish or serve Topics; do not access UAT, production, queues, Discovery Review or a remote
release path. On a weak result, diagnose the full frozen corpus-to-Brand-OS handoff and local
algorithm/ranking alternatives before any subsequent paid evaluation.

# Historical — 69B.5I-E-LAB-2A — disposable provider execution authority and runner

LAB-1C is independently PASS `P0=0/P1=0`: the local clone is externally anchored to this Mac's
fixed Docker Unix socket, every ambient `DOCKER_*` route fails before spawn, and the full frozen
authority was reattested. Do not wait for R37 or UAT. Implement the smallest correct **local-only**
execution control plane so the authorized Topic experiment can run without lying about provider
usage.

The present V2 executor is UAT-only: SQL/TypeScript hard-code `runtime_profile=uat`, its lifecycle
requires a UAT outbox, and the offline persister would falsely record paid model turns as offline.
Do not reuse it unchanged. Add hand-verified forward-only `0116`, applied only to the externally
anchored disposable clone. It may admit `local_disposable_lab_v1` with the same immutable budget,
authority, terminal reconciliation and candidate constraints, but no UAT queue/outbox/drainer or
HTTP/UI route may claim it.

Add non-HTTP Lab-only authority creation/direct claim that derives immutable workspace/actor/source
from the frozen snapshot, never a caller actor/URL. Add a fixed-container Lab runner that rechecks
host receipt, marker, container/source digests and zero counters immediately before write; uses the
bounded server-owned evidence navigator and provider adapter with a dedicated Lab key read once only
at execution time; and writes only pending editable candidates, evidence links and a Top-10
projection inside the disposable clone. Actual paid calls must be counted accurately. Local
pre-transport validation is definitely-not-sent; HTTP/network/unknown after send terminalize as
`outcome_unknown` with no blind retry. A new fixed write transport is allowed; the LAB-1 pool stays
SELECT-only.

This gate is code/local-Postgres only: no provider request, UAT, remote, production, serving,
publication/adoption, Discovery Review or secret disclosure. Test 0116 adversarially in the existing
clone, preserving UAT profile/outbox behavior. Fresh P0/P1-free audit opens the separately sealed
LAB-2B evaluation with one evaluation, at most 12 model turns/24 evidence navigations and USD 2.10
maximum under the USD 18.147816 aggregate ceiling.

# Historical — 69B.5I-E-LAB-1C — fixed local Docker transport correction

LAB-1B created and verified the external host-side clone receipt on the disposable clone, but fresh
independent audit found its remaining causal P1: Docker invocations inherit ambient routing. A
remote Docker context could make the fixed container name refer to a remote daemon. Correct this
exact local transport boundary now; it does not defer product development or the Lab.

Resolve and validate the actual local Docker Unix socket, invoke every Lab Docker command through
that explicit socket plus a sanitized environment, and reject `DOCKER_HOST`, `DOCKER_CONTEXT` or
equivalent routing overrides before inspect, clone or psql. Do not extract a credential and do not
create a second clone. Keep the existing host receipt and in-database marker, add direct pre-exec
negatives, and re-run the read-only preflight against the existing clone. A fresh P0/P1-free audit
opens LAB-2.

No UAT/remote/production, provider call, Topic adoption/publication/serving, Discovery Review or
secret disclosure.

# Historical — 69B.5I-E-LAB-1B — external clone-provenance anchor correction

LAB-1A correctly rejects non-local runtime profiles and lab names with remote labels, but a fresh
independent audit found the remaining causal weakness: its provenance marker is derived from the
same PostgreSQL target that it later verifies. That confirms an internally consistent target, not
that it is this Mac's disposable Docker clone. Do not stop product development or defer ordinary
local migrations over this: correct the local Lab boundary and proceed to the evaluation.

Implement only the minimum external anchor. A clone-creation/provenance step must inspect the
explicit local PostgreSQL Docker container, create the fresh named clone and write a `0600`
host-side receipt outside the target database. It must bind the local container immutable identity,
the clone name, the server system identity and the registered frozen source/snapshot/artifact
digests. The provider-disabled preflight must derive the permitted endpoint and expected identity
from that fixed receipt; it must not accept a caller-supplied URL, target identity or marker value.
Copied receipts, a changed container/server identity, a missing/invalid receipt, remote labels and
non-local profiles must fail before any provider transport. Keep the existing database marker as a
defence-in-depth continuity check.

Run one real disposable local PostgreSQL proof plus direct negatives and ordinary Worker tests. A
fresh independent P0/P1-free audit then opens LAB-2: one bounded local candidate-only evaluation
against the full frozen corpus under the USD 18.147816 conservative remaining ceiling. No UAT,
production, serving, Topic adoption/publication, Discovery Review change or secret disclosure.

# Historical — 69B.5I-E-LAB-1A — disposable Lab target-provenance correction

LAB-1 independently failed `P0=0/P1=1/P2=1`. The real-artifact importer, full-population checks,
read-only transaction, disabled flight card and zero-effect counters pass. The causal defect is
limited: a caller can choose a lab-shaped loopback URL/name, and the runner records
`preview_uat:false` without proving the connected server is a local disposable clone. This could
mistake a loopback tunnel or mislabeled target for the Lab.

Correct only this local boundary:

1. Require a closed Lab runtime profile separate from `uat`, `preview` and `production`; reject
   any missing or unrecognized value before a connection is created.
2. Bind an expected immutable local clone provenance marker to the connection and verify it after
   connect inside the read-only transaction. The marker must be created only during the documented
   clone setup, be namespace/version/clone-name/frozen-source bound, and not be caller supplied in
   the preflight command. A Preview/UAT or production database must fail even if its name matches
   `noisia_topic_eval_lab_*` or it is reached through loopback.
3. Add direct tests for preview, production, lab-shaped UAT/Preview names and post-connect marker
   mismatch. Wire the test into the ordinary Workers test suite/CI path. Preserve existing
   full-corpus, secret non-observation, receipt redaction and zero-write checks.

No provider transport, credential read, clone creation, migration application, UAT/Railway action,
queue, candidate, Discovery Review, Topic adoption/publication/serving, production or AuthZ/infra
change is authorized by this correction. A fresh independent P0/P1-free audit is required before
LAB-2.

# Historical — 69B.5I-E-LAB-1 — disposable full-corpus Topic Evaluation preflight

## Objective and operating boundary

The operator has redirected this work from a release gate to a **disposable product Lab**. The
goal is to prove whether Noisia can produce at least ten coherent, evidence-backed, editable Topic
candidates from the frozen full corpus before investing more work in a Preview/UAT release path.

This is not a shortcut to production. It is a local-only experiment: a fresh named loopback
PostgreSQL clone may receive hand-verified forward-only V2 migrations and temporary candidate data;
it may be discarded afterward. No connection to Preview/UAT, no release executor, no browser
mutation, no Railway action, no push, no serving, no Topic adoption/publication and no Discovery
Review redesign is part of this gate. R37's protected release-executor design remains deferred and
unchanged for a later UAT cut.

## LAB-1 work

1. Verify Docker and the named local pgvector container are healthy, then make no destructive
   cleanup. Identify a fresh disposable clone derived from the frozen source authority. The receipt
   must name only the local clone and never print a URI, password or secret.
2. Reattest the complete source population: 21,195 canonical roots, 115 historical BERTopic
   proposals, 116 catalog entries, 11,186 assigned memberships and 10,009 outliers. The V2
   importer and its private artifacts remain the only source of truth; do not substitute a sample,
   synthetic rows or caller-supplied memberships.
3. Inspect the existing V2 runner/configuration and prepare a dry-run local Lab runner or minimal
   adapter if required. It must use a separate disposable database target and can check current
   provider configuration only as `configured: true|false`; it must never read, print, copy or
   persist a credential. It may not use the UAT Worker process, queue or credential lane.
4. Produce a sanitized LAB-1 flight card only, with purpose, model identifier, bounded
   server-owned evidence navigation, candidate-only output, no automatic retry and an absolute
   one-evaluation ceiling no greater than USD 18.147816. The full candidate pool and Top-10 remain
   projections; success means at least ten recognizable candidates, not truncating all output to
   ten.
5. Add only focused local tests/documentation needed to prove that the Lab cannot target UAT or
   enable adoption/publication/serving. Run relevant local tests, typechecks, lint and diff checks.

No model request is authorized in LAB-1. Once Backend reports READY, independently audit the exact
Lab runner/preflight. Only P0/P1=0 opens a separately recorded LAB-2 call. A failed evaluation
first receives a local diagnosis of corpus-to-Brand-OS handoff and bounded algorithmic alternatives;
it never causes a blind paid retry.

## Deferred release gate — 69B.5I-E-R37

R37 remains preserved below as historical/deferred release work. Its protected-host requirement
applies only before a Preview/UAT migration or release, not to this disposable Lab.

# DEFERRED — 69B.5I-E-R37 — operator-safe protected executor provisioning and action-time UAT preflight

The operator explicitly resumed autonomous work, authorized required delegated tasks and up to
USD 20 of provider expenditure today, and enabled the hourly `noisia-topic-evaluation-recovery-loop`.
R32G fresh independent audit of `c954ebb` is **PASS, P0=0/P1=0/P2=0**. R33A has proven that no
trusted UAT release executor exists yet: this Mac lacks fixed native tools, GitHub has no dedicated
release workflow, and Railway is not an isolated executor. The executor design remains deferred;
Docker remains local-rehearsal-only and must never receive a Preview/UAT credential.

R34.1 is independently PASS: `P0=0/P1=0/P2=1`, and the named explicit-loopback PostgreSQL matrix
passed `1/1` on a newly cloned disposable pre-0115 database. The migration checksum remains
`sha256:7a6b61cc16dba808e0c98645855e2597db8d0f8ca4665979945c866a0bc3e946`; API contracts are
`11/11`, DB/Studio typechecks, focal lint, OpenAPI/i18n parsing and diff checks are green. The
non-blocking P2 is a direct-SQL test-strength improvement only.

R35 has created the isolated local commit `a7d6c13` (`Add reversible topic candidate review`) from
exactly 18 audited files. All unrelated dirty Brand OS, Front, Discovery Review, Prompt Loop and
user work remains unstaged. This commit is local only and is not deployed.

R36.1 is independently PASS `P0=0/P1=0/P2=0`. It rejects absent/blank secret handles and all
symlink/other file types, rechecks custody immediately before receipt creation, and preserves the
no-network/no-database/no-Docker boundary. Commit `6926204` contains only that four-file audited
attestation contract. It proves a future host contract; it does not create a protected host.

R37 is an action-time, operator-safe provisioning/preflight gate. It requires an explicitly
non-production ephemeral host—not this Mac or Docker—with root-owned fixed `/usr/bin/pg_dump` and
`/usr/bin/pg_restore`, the closed root policy and private directories at the documented paths, a
one-process injected UAT-only secret, fresh Workers posture and a recoverable restore process.
Only after this host produces a valid local capability receipt may it run the sealed **read-only**
UAT preflight/capture proof. The literal migration-apply approval is separate and remains closed.

Do not use a user-writable/Homebrew client, Docker, Railway shell, arbitrary command, arbitrary
database, production credential or credential logged in chat. Do not push/deploy/apply a migration,
call a provider, create candidates, alter Discovery Review or adopt/publish/serve Topics. If a
matching protected host is not explicitly provisioned, record the host-capability blocker and do
not manufacture an attestation. Front remains paused.

The next route requires a separately authorized operator-safe release-executor capability:

1. provision or identify a non-production execution host with root-owned, non-group/world-writable
   fixed `/usr/bin/pg_dump` and `/usr/bin/pg_restore`; it must be able to run the sealed runner
   without Docker fallback or caller-selected binaries/containers;
2. prove the executor's non-production target boundary and tool owner/mode/fingerprint without
   printing credentials, then prepare a fresh action-time UAT preflight/restore/attestation; and
3. only after that fresh preflight may a separately authorized literal-approval 0114 application
   gate open. It must preserve zero provider/queue/candidate/adoption/publication/serving effects.

Do not substitute a Homebrew/user-writable binary, a mutable Docker image, an arbitrary command or
remote database. Front remains paused.

## Historical R29C plan — completed; do not execute

R29A passed fresh independent audit with `P0=0/P1=0/P2=1`. The hand-verified forward-only
migration is `0113_signal_topic_evaluation_full_evidence_execution_authority.sql`, SHA-256
`8bb7f5be275d33d4f284f72a9e882314f488466ccdd3adee3ade2acb195f0f71`.

R29B established the original Studio-only deployment condition is impossible without changing
Railway settings. The operator resumed and authorized a safer paired UAT cut instead. Both
services may receive the exact audited revision, but the Topic V2 Worker remains unregistered and
provider-disabled.

Before the remote cut, implement and locally prove the smallest sealed runner for migration 0113:

1. `preflight`, `capture`, `apply` and `verify` modes must pin the UAT target fingerprint, prior
   0112 ledger/checksum, 0113 checksum, sentinels, frozen snapshot/protected digest and all zero
   provider/run/candidate counts; write redacted 0600 receipts.
2. Apply only under a fresh preflight + restore receipt hash, exact literal approval, serializable
   transaction/advisory lock, absent-under-lock check and one exact ledger row. It must stop on
   partial state or any protected/frozen-state drift.
3. Re-run the runner against the local disposable PostgreSQL target and fresh-audit it. Then commit
   only the audited allowlist, push only the existing Preview/UAT branch and reconcile Studio plus
   Workers health/readiness. Workers must show no registered V2 job, authority, run, candidate or
   provider call.

The former preflight established this deployment-isolation fact without a remote change:

- Studio and Workers are both healthy Preview/UAT services on
  `codex/noisia-data-os-cut-1-uat-2026-08-18`, never production.
- Both Railway services auto-deploy on a branch push. Studio watches `packages/**` and
  `infrastructure/db/**`; Workers watches `services/workers/**`, `packages/**`, and
  `infrastructure/db/**`.
- The audited 0113 allowlist necessarily changes those paths. Railway has no service-scoped action
  for deploying a new Studio-only commit; its action menu only handles an existing revision.

Do not call a provider, enqueue a V2 job, create candidates, mutate Discovery Review, access
production, or change Railway settings. This is a paired deployment of inert code only; Topic
evaluation, adoption, publication and serving remain separate later gates.

## Completed — R29A local-only execution-authority transition correction

R29 is **not deployable**. A fresh independent audit found that the new 0113 authority trigger
does not reject a decreased `provider_call_count`, and admits `authorized → failed` while its
paired provider-enabled run remains `planned`. That direct-SQL path makes durable accounting and
the one-active-authority lock disagree. Nothing remote ran: 0113 is unapplied to UAT; provider
calls, candidates, publication, adoption and serving remain zero.

Implement only this causal correction, locally:

1. Tighten the 0113 authorization trigger so `provider_call_count` is monotonic and
   `settled_micro_usd`, `error_code` and `completed_at` cannot be populated or changed before a
   valid terminal transition. A terminal authority must not be reachable from `authorized`.
2. Close the paired-state invariant: a provider-enabled run and authority must transition in
   lockstep. Direct SQL must not terminalize one while the other stays planned/in-progress, nor
   release the unique active authority for a frozen snapshot until the paired run is terminal.
   Preserve the existing durable writer's valid `claimed → completed|failed|outcome_unknown`
   sequence and provider-disabled V2 behavior.
3. Add direct-SQL PostgreSQL adversarial cases for counter decrement, premature settlement/error,
   `authorized → failed`, mismatched run/authority terminal state, and second authority after an
   attempted desynchronization. Prove all reject and valid local failure/known-invalid paths
   remain viable.
4. Re-run the focused local Postgres clone proof, core/Worker tests, typechecks, documentation
   accuracy and `git diff --check`. Preserve unrelated dirty files and use `apply_patch` only.

Do not apply 0113 to UAT, connect to or write UAT, call a provider, enqueue a job, push/deploy,
alter Discovery Review, create candidates, adopt/publish/serve Topics, access production or change
AuthZ/secrets/infrastructure. A fresh independent P0/P1-free audit is required afterward.

## Superseded implementation prompt — E-R29

E-R28 is PASS. The frozen 116-cluster / 21,195-membership authority already has a closed,
provider-disabled future flight card: USD 20 aggregate hard cap, action-time confirmation,
12 model turns, 24 bounded navigation calls, 32,768 bytes per tool result, 262,144 total tool
bytes, 450,000 input tokens, 50,000 output tokens, append-only pending candidates and a separate
Top-10 projection. Focused Query Engine `7/7`, Worker fixture `1/1`, and Studio API/status
`11/11` passed with zero provider/UAT/product effects. The live route remains 403 disabled.

Implement only the local execution control plane required for one future paid evaluation:

1. Add the smallest forward-only, hand-verified migration needed to represent an explicitly
   provider-enabled V2 evaluation run without weakening or rewriting the provider-disabled V2
   default. It must make the UAT enablement, sealed budget reservation, idempotency, run lineage,
   terminal status and no-retry-after-ambiguous-send rules database-enforced.
2. Add a Worker-only adapter that can navigate only the six existing server-owned, sanitized
   evidence operations over the frozen snapshot; it must never receive SQL, raw IDs, raw corpus
   export paths or unbounded mention payloads. It must record durable retrieval and model-turn
   provenance, validate output against the existing candidate-only schema, and write only pending
   candidates plus the Top-10 ranking projection. Preserve all limits from E-R28.
3. The adapter must fail before transport unless the dedicated UAT-only enablement and action-time
   confirmation are valid. It must distinguish a local definitely-not-sent failure from every
   network/provider/unknown-after-send outcome, which must terminate as ambiguous and must never
   auto-retry. No run may become adopted, published or serving.
4. Add direct API/core/Worker/PostgreSQL adversarial tests: execution disabled, wrong environment,
   bad confirmation, stale authority, idempotency conflict, duplicate reservation, budget overflow,
   foreign evidence, tool/result/token overflow, provider HTTP/network ambiguity, invalid output,
   attempted adoption/publication/serving and concurrent terminal transitions. Update the OpenAPI
   contract and the Full Evidence control-plane document to state that execution is still disabled
   until a separate UAT cut.
5. Run focused checks, affected typechecks/lint, OpenAPI YAML/JSON validation and `git diff --check`.
   Preserve all user-dirty files and use `apply_patch` only.

Do not apply the new migration, connect to or write UAT, enqueue a real job, call any provider,
push/deploy, alter Discovery Review, adopt/publish/serve Topics, access production or change
AuthZ/secrets/infra. Return a focused local evidence receipt for independent audit. A later UAT
enablement/deploy gate and an even later one-call operator confirmation remain separate.

## Completed — E-R28 local-only sealed Full Evidence provider-flight preflight

E-R24D is reconciled PASS in Preview/UAT. Commit
`986c9bd5644995deeadffb6ac7d9062f1ab90d78` is active in Studio; migration 0112 is present once
with checksum `sha256:51f6fbff712ec1737b41da9997bda86b068abb81f4edafc9a338af590c462ab5`.
The verified private-artifact import created one frozen snapshot with exactly 116 clusters,
21,195 memberships, 11,186 assigned and 10,009 outliers. Protected state is unchanged and V2
provider/runs/retrievals/candidates/rankings remain zero. No Topic has been adopted, published or
served.

E-R25 completed and passed independent audit P0/P1/P2=0. Its exact eight-file commit is
`1d542e7e1af73f65d57eb9d622af147effcd3470`, and the non-production Studio deployment completed
healthy. The code still has no provider execution control; its `POST` remains disabled.

Railway also activated `workers-uat` deployment `0aa0ff32-0407-42e3-ab3c-ab2258fc7356`, but
read-only source/deployment comparison proved the cause: Workers was still active at `adafcc9`, so
Railway evaluated the cumulative `adafcc9..1d542e7` branch delta containing the prior shared
full-evidence control-plane paths. The new `1d542e7` commit itself has only `apps/studio/**`, and
Workers is now caught up, healthy and has zero claimable jobs. No setting changed; no migration,
run, queue, provider, candidate, publication, adoption, serving or production effect occurred.

E-R27 is PASS. Authenticated Amazon Alexa Preview/UAT read-only QA verified the Full Evidence card
at desktop and 390px: 115 historical proposals, 116 sealed clusters, 21,195 canonical memberships,
Top-10 as a view-only projection, explicit disabled provider/execution and bounded future
navigation. Refresh retained the exact state; the tested control makes GET only and the browser
console was clean. No V2 run/retrieval/candidate/provider/publication/adoption/serving effect was
created.

E-R28 is local-only. Define and implement only the future full-evidence provider-flight **preflight**:
it must bind the sealed authority and complete-corpus membership, enforce an explicit USD 20
aggregate ceiling, closed model/tool/result/token limits, action-time confirmation, no automatic
retry after ambiguous send, terminal reconciliation, candidate-only append-only output and Top-10 as
a view rather than a truncation. It must leave execution disabled and create no provider request,
queue job, run, retrieval, candidate, migration, UAT/browser action, Discovery Review change,
adoption/publication/serving, production access or AuthZ/secret/infra change. Prove all negative
paths fail closed. A fresh independent P0/P1-free audit is required before a separately authorized
UAT enablement/preflight cut; that cut still must not send the provider request.

## Completed specification — E-R24D1 local-only current-0112 checksum re-attestation

The prior E-R24A evidence manifest seals `0112` as
`sha256:86119dc81acb173be16f5f5b609f3f1d51f75a0c67c83f3000c22402ccc4ee23`, but the current file
is `sha256:51f6fbff712ec1737b41da9997bda86b068abb81f4edafc9a338af590c462ab5`. E-R24D stopped
before remote access. This is a provenance receipt mismatch, not a reason to overwrite history or
apply an unsealed migration to Preview/UAT.

Use only recovered Docker and an explicitly local fresh disposable PostgreSQL database restored
from the preserved pre-0112 UAT baseline. Apply the current 0112 one time, run the exact
real-artifact source-of-truth importer proof and record the current checksum plus exact aggregate:
`21,195` canonical memberships, `115` proposals, `11,186` assigned and `10,009` outliers. Prove
the cross-workspace, forged-source-binding and append-only negatives remain fail-closed and that
protected serving/pointer/binding/release state remains unchanged.

Do not modify 0112, use a remote DB, inspect UAT, commit/push/deploy, call a provider, queue a
run, create candidates, change Discovery Review, adopt/publish/serve Topics, access production or
alter AuthZ/secrets/infra. Then conduct a fresh no-edit audit of the actual migration and binding
path. Only P0/P1=0 with a complete current receipt reopens the separately authorized E-R24D UAT
cut.

## Deferred specification — E-R24D focused Preview/UAT full-evidence control-plane cut

E-R24C passes fresh independent audit with `P0=0/P1=0`. The exact real-artifact local
PostgreSQL `0000→0112` provenance proof remains PASS: `21,195` canonical memberships, `115`
historical proposals, `11,186` assigned and `10,009` outliers, with protected serving state
unchanged. The current V2 control plane is still provider-disabled and has created no candidates.

Execute only the separately authorized non-production UAT cut:

1. Prepare a commit with only the audited E-R24A/C full-evidence allowlist. Exclude all unrelated
   dirty Brand OS, Discovery Review, Front and prompt-loop work. Push only the existing Preview/UAT
   branch after proving the target is non-production and capturing a recoverable restore point.
2. Reconcile the commit allowlist and deployed revision. Apply **only** forward-only migration
   `0112_signal_topic_evaluation_full_evidence_control_plane.sql` exactly once, verifying its
   sealed SHA-256 `86119dc81acb173be16f5f5b609f3f1d51f75a0c67c83f3000c22402ccc4ee23`, the ledger,
   expected snapshots/memberships and absence of protected serving/pointer/binding/release drift.
3. Deploy **Studio only**. Workers remain on their current deployment: do not rely on a normal
   repository push if it would redeploy Workers. Verify Studio deep health and read-only V2
   preflight/navigation authority. Reconcile `21,195` memberships, `115` proposals, `11,186`
   assigned and `10,009` outliers, semantic baseline/protected digest and zero providers/runs/
   candidates/publication/adoption/serving effects.

Do not call a provider, enqueue a run, create a candidate, alter Discovery Review, publish/adopt/
serve Topics, access production or modify AuthZ/secrets/infra. If target, checksum, ledger,
deployment, deep health or reconciliation mismatches, stop without remote patch/retry. Only a
healthy E-R24D opens authenticated **read-only** UI QA. A paid full-evidence evaluation remains a
separate sealed flight-card gate after QA.

## Historical completed specification — E-R24C local-only full-evidence API contract alignment

E-R24B is now PASS. After the Docker restart, a fresh explicitly local disposable PostgreSQL target
ran the exact real-artifact `0000→0112` provenance proof once: all `21,195` canonical roots map
one-to-one, `115` proposals reconcile with `11,186` assigned roots and the `10,009` outliers, the
cross-workspace/forged/append-only negatives fail closed, and protected serving state is unchanged.
The importer tests (`3/3`), Query Engine (`6/6`), Worker (`1/1`), Studio API (`6/6`) and all four
typechecks are green. No UAT migration, provider call, queue work, Topic candidate, publication or
serving effect occurred.

The independent audit is nevertheless FAIL P1. `cluster_catalog` intentionally returns a compact
catalog row without `profile` in `infrastructure/db/signal-topic-evaluation-v2.ts`, while
`docs/api/openapi.yaml` declares those rows as `SignalTopicEvidenceClusterV2`, where `profile` is
required. The API contract is untrue for a normal successful response. This is a local contract
correction, not an invitation to widen retrieval size or expose profile text in the catalog.

Implement only this causal correction, locally:

1. Define operation-specific full-evidence response shapes. `cluster_catalog` must document and
   validate exactly its compact summary (`cluster_key`, `proposal_key`, `member_count`,
   `profile_digest`) plus `total_clusters`; single-profile and compare operations may retain their
   complete profile shape. Do not add a `profile` to catalog SQL solely to satisfy the old schema.
2. Replace the permissive `data: z.unknown()` boundary with a closed runtime shape that validates
   the result data appropriate to its operation before the route returns it. Preserve only the six
   already allowed operations, their limits, sanitized text, cursor binding and read-only behavior.
3. Update OpenAPI to the same discriminated response contract and add focused Query Engine/Studio
   API contract tests proving the actual catalog response validates without `profile`, while a
   forged/mismatched operation shape fails. Keep consumer response size bounded.
4. Run the focal tests, all touched-package typechecks, OpenAPI YAML parse/validation and
   `git diff --check`. Preserve all unrelated dirty work. Use `apply_patch` for edits.

Do not amend 0112, apply any migration, commit, push, deploy, mutate Browser/UAT, call a provider,
queue work, create a V2 run, generate candidates, alter the historical V1 evaluator, change
Discovery Review, adopt/publish/serve Topics, access production or alter AuthZ/secrets/infrastructure.
A fresh independent audit with P0/P1=0 is required before a separately authorized UAT deployment
gate.

## Historical implementation specification — completed as E-R24A

E-R21 is PASS. Focal commit `adafcc93b6b9f5ca8fa15a100167ae47a2e5e15f` changed exactly the five
audited paths. Its recoverable UAT restore point has 3,031 TOC entries and SHA-256
`6eaf8de4e1932fc3f19c1608e3ac3bf66cdaddbe4ed2f552fde33e38ed96e8d9`. Workers deployment
`9b593d72-407d-4ee3-941f-e570eef06fac` is active and healthy; Studio remains deployment
`6d56efcc-5d75-4b92-ac40-24daa4dac486` and Railway skipped it because no watched file changed.
No migration was required; UAT ledger remains at 0111.

The human operator completed authenticated visual QA directly and supplied desktop and mobile
screenshots: 115 historical BERTopic proposals, zero candidates, terminal unknown history, bounded
successor dialog, `claude-sonnet-5`, estimated USD 0.33, absolute USD 0.38, one call maximum,
candidate-only/no-adoption/no-publication/no-serving, unchecked acknowledgement and disabled start.
No new request was sent.

Durable Backend reconciliation is sealed at `0/69/11/0/0`, archived `2`, blockers `0`, protected
digest `sha256:25e2beca3e5879e5611be85d4ae81284fcd63ed89c04a50b48da7d732b61a5a7`, and exactly two
historical runs/reservations/outboxes/provider-call records with zero candidates/evidence/revisions/
reviews. No provider call, queue work, UI mutation, generation, semantic decision, publication,
The operator clarified the production direction before spending attempt 3: BERTopic already ran on
the complete 21,195-root modelable population, but Claude currently receives only per-proposal
summaries and opaque evidence references. The desired experiment is instead full-evidence
interpretation: Claude must be able to retrieve bounded, sanitized representative mentions from any
cluster on demand, while Noisia retains server-side access to the full cluster membership. The run
may cost up to USD 20, analyze the full frozen corpus through bounded navigation, emit a ranked
candidate pool and a useful Top 10, and never adopt/publish/serve Topics automatically.

E-R24 delivered a promising provider-disabled control plane: bounded navigation, deterministic
sampling, a USD 20 multi-turn flight-card, provenance, a complete pending candidate pool and a
separate Top-10 projection. The focused core/Worker/API tests and typechecks pass. It is not
deployable: the fresh audit found that `sealSignalTopicEvaluationEvidenceSnapshotV2` accepts
caller-supplied clusters, mention IDs and `source_record_digest` values. Its PostgreSQL fixture
therefore inserts 21,195 synthetic mentions and count-valid memberships rather than proving the
exact immutable `bertopic-bge-detail.seed-17.assignments.npz` mapping over the frozen source export.
The fixed assignment/export digests and count triggers prove shape, not membership identity.

Implement only this causal correction, locally and with provider execution disabled:

1. Replace the generic sealing input with a narrow importer that reads the canonical private,
   mode-0600 frozen assignment and source-export artifacts. Verify their expected manifest/file
   digests before deriving anything. Do not print corpus text, record keys, pseudonym keys or IDs.
2. Deterministically bind every assignment index to its one canonical workspace mention using a
   verified immutable record/content identity. Derive `source_record_digest`, `member_ref`,
   language/market/scope/month, rank and stratum from the verified source/assignment rather than
   trusting caller-provided values. If the frozen artifact cannot map exactly to current canonical
   mentions, return BLOCKED with aggregate mismatch counts; never synthesize or substitute rows.
3. Prove all 21,195 roots appear exactly once; 11,186 map to the 115 exact historical proposals;
   10,009 map to the outlier reservoir; proposal/profile mapping agrees with the frozen result;
   and snapshot/assignment/export digests are recomputed from the real inputs. Add adversarial
   tests for swapped membership, forged digest, missing/duplicate root, stale source export and
   cross-workspace mention. The test must exercise the actual sealed artifact format or a byte-for-
   byte contract fixture—not a fabricated 21,195-row database population.
4. Strengthen the database invariant where possible so a stored membership's identity is tied to
   the current canonical mention row, not only to its workspace and included state. Because 0112
   is not applied or committed, amend it carefully rather than introducing an unnecessary second
   migration. Re-run the disposable 0000→0112 chain.

Preserve the existing V1 evaluator as a historical baseline, all bounded navigation limits,
provider-disabled state, non-adoption/non-publication/non-serving constraints and Discovery Review
surface. Do not connect to or write UAT, call a provider, enqueue a job, deploy, push, commit,
access production or change AuthZ/secret/infra. Return a focused receipt for a new independent
audit. No paid evaluation may open until this exact root-to-mention binding passes.

---

# COMPLETED — 69B.5I-E-R14 — local-only legacy ambiguous-outcome successor compatibility

E-R13 is PROVEN. The historical predecessor on Preview/UAT satisfies every closed ambiguity
predicate, but it uses legacy code `topic_evaluation_provider_outcome_unknown`. The current
management projection, dedicated successor writer and 0110 PostgreSQL validator require only
`topic_evaluation_provider_ambiguous_after_send`, leaving the operator control disabled even
though the existing provider outcome classifier already recognizes both as ambiguous-after-send.

Implement only this forward-only local correction:

1. Add hand-verified migration 0111 that `CREATE OR REPLACE`s only
   `validate_signal_topic_evaluation_successor_operation_v1`. Preserve the full 0110 validator
   verbatim except replace the predecessor error-code check with a closed, null-rejecting pair:
   `topic_evaluation_provider_ambiguous_after_send` or
   `topic_evaluation_provider_outcome_unknown`.
2. Make the management eligibility projection and dedicated successor writer use the same exact
   closed pair before any write. Do not change the provider-outcome classifier, root route,
   envelope, idempotency, run identity, reserved cost, operation count, transport or UI.
3. Add direct static/core regressions proving the legacy code becomes eligible only when every
   pre-existing predicate remains true, while null/other codes stay closed. Add disposable
   PostgreSQL coverage applying 0000→0111 and exercising the legacy predecessor success plus
   wrong-code/no-reservation/no-outbox/candidate-present negative cases.
4. Run focal tests, the PostgreSQL chain, relevant typechecks/lint, migration checksum, OpenAPI
   YAML, i18n JSON and `git diff --check`.

Use `apply_patch`. Preserve unrelated dirty/untracked work. Do not commit/push, touch UAT/browser,
call a provider, queue Workers, create a run, alter the historical predecessor, publish, serve,
access production or redesign Discovery Review. A fresh P0/P1-free audit is required before a
separate 0111 UAT cut and authenticated read-only QA. The second evaluation remains an immediate
financial action-time confirmation after that cut.

---

# COMPLETED — 69B.5I-E-R7 local-only forward-only successor-flight authority migration

E-R6 proves that an in-code correction cannot safely create a successor. The exact envelope digest
is sealed and recalculated at execution/recovery, while migration 0107 makes workspace plus
envelope and workspace plus run key unique. There is no existing append-only table or field for
predecessor/successor authority. Rewriting the historical run or altering its envelope is not
permitted.

Implement one hand-verified, forward-only local migration and its code path:

1. add an append-only successor-operation record with workspace, predecessor run, actor,
   idempotency, confirmation, authority/input digests and sealed result;
2. add predecessor lineage, successor-operation linkage and attempt ordinal to an evaluation run;
3. retain root-flight uniqueness, while allowing one direct successor only for a terminal
   ambiguous-after-send predecessor whose historical provider call, ambiguous reservation and
   dead-letter remain immutable;
4. derive a distinct successor request identity and run key from predecessor plus operation digest;
5. keep ordinary start fail-closed with a typed duplicate-envelope error and make the dedicated,
   acknowledgement-gated successor route the sole creator of successor authority;
6. prove API/core/PostgreSQL adversarial paths: missing/forged/cross-workspace/stale/non-ambiguous
   predecessor, repeated idempotency, second successor, concurrent intents, and exact rollback
   with zero dispatch outside the dedicated authority.

Do not apply the migration, connect to or write UAT, call a provider, queue a job, create a real
run, push, deploy, change configuration, publish, serve, touch production or modify Discovery
Review. Return the focused diff/tests for independent audit. A later deployment and successor
financial action remain separate.

---

# COMPLETED — 69B.5I-E-R6 local-only successor authority feasibility

E-R5 is PROVEN. Preview/UAT received the exact authenticated POST at 2026-08-29 11:01:29 CST
and returned HTTP 409 in about one second. The shared route maps a PostgreSQL 23505 conflict to
409. No new durable run, reservation, outbox, event or provider call exists because the
serializable transaction rolled back before any of those writes. The UI disabled second submit
as designed, and no second request reached Anthropic.

The historical ambiguous_after_send run and the new confirmed intent deterministically derive
the same envelope/run identity. Existing unique constraints correctly reject a duplicate ordinary
flight, but the product currently lacks a deliberate successor-attempt authority for the
operator-authorized retry path.

The requested in-code correction was intentionally not implemented because it cannot safely meet
the required lineage and recovery constraints without forward-only DDL:

1. preserve the historical ambiguous run, its evidence and its cost state immutable;
2. create a server-owned explicit successor-flight identity and lineage that can be invoked only
   through a dedicated, acknowledgement-gated retry authority;
3. require fresh idempotency for a successor and retain one-call/no-automatic-retry behavior;
4. keep ordinary duplicate-envelope requests closed with an operator-safe domain error;
5. add direct API, core writer and PostgreSQL adverse-path tests proving generic requests cannot
   create a successor, duplicate or provider dispatch.

This gate is local-only: no provider transport, queue, real run, database/UAT mutation,
migration, push, deploy, UI operation, publication, serving, production or Discovery Review
change. Return a focused diff, contracts and tests for independent audit. A later deployment and
a new action-time financial confirmation remain separate.

---

# COMPLETED — 69B.5I-E-R5 read-only UAT submit-to-durable-authority diagnosis

E-R4 received immediate operator confirmation and the normal UAT Brand OS UI performed exactly
one acknowledgement-gated submit. The UI disabled its controls and displayed its no-second-submit
guard. Sixteen independent read-only snapshots found only the historical ambiguous run: no second
durable run, reservation, outbox, dispatch or provider call exists. Consequently Anthropic has no
evidenced second request and E-R4 incurred zero new spend.

Diagnose only the unmaterialized boundary, without reusing the idempotency key or issuing another
request. Establish, with read-only UAT data and logs where available:

1. whether client fetch remained pending or received a sanitized response;
2. whether the Topic Evaluation POST route was entered;
3. whether authentication/workspace resolution, dynamic DB pool acquisition, advisory-lock waiting
   or a database transaction blocked before durable run/reservation/outbox inserts;
4. whether a route/runtime/deployment timeout occurred.

Return one of PROVEN, INCONCLUSIVE, or BLOCKED and an exact narrow remediation class. No provider,
queue, retry/recovery, UI operation, database/configuration write, migration, commit, push,
deployment, Semantic Context decision, Discovery Review change, Topic adoption, publication,
serving or production action is permitted. A corrected and separately audited route is required
before any new financial confirmation or candidate assessment.

---

# DEFERRED — 69B.5I-E-R — final operator-confirmed one-call Preview/UAT Topic Evaluation

All zero-cost evidence is complete.  The exact normal UAT Brand OS route is:

```text
https://studio-uat-uat.up.railway.app/studio/brands/af11af41-343e-4b6a-ab98-0c37bf24e41d/brand-os
```

Its workspace owns the frozen evaluation input: 115 historical BERTopic proposals, 21,195
modelable roots and 805 evidence links.  Backend and authenticated UI QA both reconcile exact
`0/69/11/0/0`, archived `2`, protected digest
`sha256:25e2beca3e5879e5611be85d4ae81284fcd63ed89c04a50b48da7d732b61a5a7`, a complete
`claude-sonnet-5` flight card, and zero Topic Evaluation runs/reservations/outboxes/candidates/
provider calls. The UI visibly shows USD 0.33 estimated reservation, USD 0.38 absolute cap,
one call/no retry and candidate-only/no-adoption/no-publication/no-serving boundaries.

This gate is deliberately **not autonomous**. Immediately before interacting with the
acknowledgement checkbox and Start control, obtain a fresh operator confirmation for this exact
financial product action: one Preview/UAT `claude-sonnet-5` evaluation, max USD 0.38, no retry.
On confirmation, use only the normal UI to acknowledge and click Start once. Generate one new
idempotency key in the client and never make a second POST. Backend then monitors/reconciles
read-only until terminal. If completed, assess candidates only; if failed or `<10`, stop spend
and diagnose locally without another provider call. No Topic adoption, publication, serving,
production, Discovery Review edit or second provider action is authorized.

---

# COMPLETED — 69B.5I-E-Q3B — read-only resolution of the UAT Brand OS route owning frozen Topic Evaluation authority

E-Q3A is PASS: the inspected Brand OS workspace correctly returned
`topic_evaluation_input_authority_unavailable`, because it has no historic Discovery Review
packet or Semantic Context generation.  This is not a provider, runtime credential, Railway or
deployment failure.  The generic message is a secondary route-mapping copy defect and no UI
control may circumvent the server-owned authority.

In one `REPEATABLE READ READ ONLY` audit, locate the exact existing Preview/UAT brand and
workspace whose immutable Topic Evaluation envelope owns the 115 historical BERTopic proposals,
21,195 modelable roots and 805 evidence links. Return only the normal authenticated Brand OS URL
and necessary internal IDs. Confirm it still has exact `0/69/11/0/0`, archived `2`, protected
digest `sha256:25e2beca3e5879e5611be85d4ae81284fcd63ed89c04a50b48da7d732b61a5a7`, a complete
preflight configuration and zero Topic Evaluation effects. Do not expose credentials, corpus
text, raw envelope or evidence IDs. Do not mutate UAT, browser, config, DB, source, remote,
deployment, provider, publication or serving.

Return exactly:

```text
BACKEND_69B5IEQ3B=READY|BLOCKED|FAIL
BRAND_ID=<uuid|none>
WORKSPACE_ID=<uuid|none>
BRAND_OS_URL=<uat-url|none>
AUTHORITY_MATCH=true|false
COUNTS=0/69/11/0/0|MISMATCH
ARCHIVED=2|MISMATCH
FLIGHT_CARD_COMPLETE=true|false
TOPIC_RUNS=0
TOPIC_RESERVATIONS=0
TOPIC_OUTBOXES=0
TOPIC_CANDIDATES=0
PRODUCT_PROVIDER_CALLS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
PRODUCTION_ACCESSED=false
```

---

# COMPLETED — 69B.5I-E-Q3A — read-only UAT diagnosis of Topic Evaluation preflight rejection

E-Q2 deployed exact Studio commit `c7ac7f53d530a8c0e24d9c6231c4c52e00e2ef8c` to
Preview/UAT. Restore point, deep health and unchanged Workers all pass; baseline remains
`0/69/11/0/0`, archived `2`, protected digest
`sha256:25e2beca3e5879e5611be85d4ae81284fcd63ed89c04a50b48da7d732b61a5a7`, and every Topic
Evaluation effect counter remains zero.

The authenticated normal Brand OS UI mounts the `Candidatos de tópicos` section at brand
`a6e3459d-6039-4c36-a738-e95c6172fbbe`, but its ordinary GET renders `The Semantic Context
Pack operation was rejected.` The drawer never opened, the acknowledgement was not checked and
no POST occurred.

Perform only a sanitized, repeatable-read/read-only diagnosis. Reconcile the Brand OS
workspace ID passed to `TopicEvaluationManager` with the frozen Topic Evaluation authority and
capture the exact domain error category/code from `loadSignalTopicEvaluationDryRunProductV1`.
Classify it as one of: brand/workspace authority mismatch, runtime configuration preflight,
route/error mapping, or deployed code defect. Confirm normal UI still issues no POST and all
run/reservation/outbox/candidate/event/provider counters remain zero. Do not inspect or print
credentials; do not mutate UAT, settings, DB, UI, source, remote, deployment or Railway.

Return exactly:

```text
BACKEND_69B5IEQ3A=READY|BLOCKED|FAIL
CAUSE=workspace_authority_mismatch|runtime_preflight|route_mapping|deployed_code|other
SANITIZED_ERROR=<code|none>
DEPLOYED_COMMIT=c7ac7f53d530a8c0e24d9c6231c4c52e00e2ef8c|MISMATCH
COUNTS=0/69/11/0/0|MISMATCH
ARCHIVED=2|MISMATCH
TOPIC_RUNS=0
TOPIC_RESERVATIONS=0
TOPIC_OUTBOXES=0
TOPIC_CANDIDATES=0
PRODUCT_PROVIDER_CALLS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
PRODUCTION_ACCESSED=false
```

---

# COMPLETED — 69B.5I-E-Q2 — focal Preview/UAT deploy of normal Topic Evaluation launch surface

E-Q1 passed fresh independent audit: the normal Brand OS control has no API, OpenAPI, migration
or Discovery Review change; preserves server AuthZ, uses the existing strict POST contract and
locks a session after the first intent. Its focused 8/8 suite and Studio typecheck pass.

Create a strict focused commit containing only:

```text
apps/studio/src/components/brands/TopicEvaluationManager.tsx
apps/studio/src/lib/data-os/signal-topic-evaluation-launch.ts
apps/studio/src/lib/data-os/signal-topic-evaluation-launch.test.ts
apps/studio/src/app/studio/brands/[id]/brand-os/page.tsx
apps/studio/messages/es-MX.json
apps/studio/messages/en-US.json
```

Exclude all user-dirty paths, AGENTS.md, BrandEditForm, documentation, PROMPT_LOOPING, `.data`
evidence and tools. Before remote write prove existing UAT/non-production target and capture a
recoverable restore point. Push only the existing UAT branch and deploy only Studio. No migration
or setting change; Workers must remain unchanged. Reconcile read-only deployed commit, Studio deep
health, Workers deployment, exact `0/69/11/0/0`, archived `2`, protected digest
`sha256:25e2beca3e5879e5611be85d4ae81284fcd63ed89c04a50b48da7d732b61a5a7`, complete flight card
and zero Topic Evaluation effects. Do not open the card's drawer checkbox or issue a POST in this
gate; authenticated UI QA is a separate read-only gate.

Return exactly:

```text
BACKEND_69B5IEQ2_UAT_CUT=READY|BLOCKED|FAIL
FOCAL_COMMIT=<sha|BLOCKED>
UAT_TARGET=PREVIEW|BLOCKED
RESTORE_POINT=PASS|FAIL|BLOCKED
DEPLOYED_COMMIT=<sha|BLOCKED>
STUDIO_DEEP_HEALTH=PASS|FAIL|BLOCKED
WORKERS_UNCHANGED=PASS|FAIL|BLOCKED
COUNTS=0/69/11/0/0|MISMATCH
ARCHIVED=2|MISMATCH
PROTECTED_DIGEST=sha256:25e2beca3e5879e5611be85d4ae81284fcd63ed89c04a50b48da7d732b61a5a7|MISMATCH
PRODUCT_PROVIDER_CALLS=0
REAL_GENERATIONS=0
TOPIC_RUNS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
PRODUCTION_ACCESSED=false
```

---

# COMPLETED — 69B.5I-E-Q1 — local operator-safe Topic Evaluation launch surface

E-Q received final confirmation but stopped safely before a POST. The authenticated Studio
browser opens the application, yet raw JSON endpoint navigation yields `ERR_BLOCKED_BY_CLIENT`;
its sandbox has neither `fetch` nor `XMLHttpRequest`, and the browser policy forbids extracting
session credentials, executing an indirect request or changing surfaces to bypass the client.
Final read-only reconciliation remains exact `0/69/11/0/0`, archived `2`, protected digest
unchanged and all Topic Evaluation counters zero. No cost was incurred.

Implement only a local Brand OS operator surface that makes the already-deployed, management-only
Topic Evaluation API usable through normal Studio interaction. Do not touch Discovery Review.

1. Add a small `TopicEvaluationManager` beside the existing Semantic Context pack section on the
   Brand OS page. It reads the existing sanitized GET preflight through normal client `fetch`; it
   must never expose the private envelope, corpus text, raw credential, prompt or evidence IDs.
2. Reuse existing AdminResourceSection, AdminSummaryStrip, WorkspaceDrawer and app button/status
   patterns. The surface must plainly show: the 115 historical proposals, candidate-only output,
   one call/no retry, model, estimated reservation, hard cap and no Topic adoption/publication/
   serving. Do not add a generic "AI" dashboard or a Discovery Review flow.
3. The drawer must have a single explicit cost acknowledgement and one disabled-until-acknowledged
   command. It generates one fresh idempotency key client-side and POSTs the unchanged literal
   confirmation, preflight envelope digest and server-provided hard cap. It must prevent duplicate
   submission locally, preserve focus, cancel on Escape, expose inline errors, and display only
   the returned run status. It may not construct a fallback/retry UI.
4. Preserve server authZ and the existing strict POST parser. No API relaxation, hidden service
   account, direct DB writer, cookie extraction, proxy, migration, provider call, real run,
   UAT/browser mutation, commit/push/deploy or product effect in this gate.
5. Add focused client/contract tests covering sanitized projection, confirmation gating, exact
   request, idempotency and no duplicate submission; add both i18n locales. Reuse existing CSS
   primitives where possible. Run focused tests, Studio typecheck/lint, i18n JSON validation,
   OpenAPI YAML validation and `git diff --check`.

Return exactly:

```text
BACKEND_69B5IEQ1_LAUNCH_SURFACE=READY|BLOCKED|FAIL
AUTHZ_PRESERVED=PASS|FAIL
EXACT_POST_CONTRACT=PASS|FAIL
DUPLICATE_SUBMIT_PREVENTED=PASS|FAIL
DISCOVERY_REVIEW_UNTOUCHED=PASS|FAIL
FOCAL_TESTS=<passed/total>
PRODUCT_PROVIDER_CALLS=0
REAL_GENERATIONS=0
TOPIC_RUNS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
PRODUCTION_ACCESSED=false
```

---

# BLOCKED BEFORE POST — 69B.5I-E-Q — final-confirmation one-call Preview/UAT Topic Evaluation run

E-P reconciled READY. Preview/UAT Studio and Workers have only the nine sealed non-secret
runtime values; both services deployed healthy. The existing product credential was checked only
by masked presence. A `REPEATABLE READ READ ONLY` preflight proves exact `0/69/11/0/0`, archived
`2`, no publication blockers, protected digest
`sha256:25e2beca3e5879e5611be85d4ae81284fcd63ed89c04a50b48da7d732b61a5a7`, and zero runs,
reservations, outboxes, candidates, events, provider calls or other product effects.

Do not execute until the operator gives a fresh final transaction confirmation in this turn.
Once confirmed, use the existing authenticated Preview/UAT Studio surface to make **exactly one**
idempotent Topic Evaluation POST with the server-required literal confirmation and a newly
generated idempotency key. The call uses existing product configuration only:

```text
model: claude-sonnet-5
input/output ceilings: 110000 / 16000 tokens
estimated reservation: USD 0.33
absolute hard cap: USD 0.38
one call maximum: 1
retry/fallback: false / false
success threshold: >=10 coherent evidence-backed editable pending candidates
```

Do not make a second POST for any condition. Reconcile each lifecycle transition read-only until
the one run reaches a terminal state. Verify there is no automatic Topic adoption, publication,
Signal serving, Semantic Context mutation, Discovery Review mutation, production access or extra
provider call. If the run succeeds, report candidates only as pending, editable Topic **candidates**
and open a separate read-only result audit. If it fails or produces fewer than ten candidates,
stop and diagnose the frozen corpus-to-Brand-OS handoff plus bounded local/no-provider alternatives;
never spend a second call automatically.

Return exactly:

```text
BACKEND_69B5IEQ_ONE_CALL=READY|BLOCKED|FAIL
IDEMPOTENCY_KEY_RECORDED=true|false
PROVIDER_CALLS=1|0|other
RUN_TERMINAL_STATE=completed|failed|blocked
RESERVATION_MICRO_USD=330000|other
SETTLED_MICRO_USD=<amount|unavailable>
CANDIDATE_COUNT=<count>
SUCCESS_THRESHOLD_MET=true|false
TOPIC_ADOPTION=0
PUBLICATIONS=0
SERVING_EFFECTS=0
PRODUCTION_ACCESSED=false
```

---

# COMPLETED — 69B.5I-E-P — sealed Preview/UAT runtime configuration and credential-presence preflight

E-O is locally READY: `claude-sonnet-5`, pricing version
`anthropic-claude-sonnet-5-2026-08-29`, USD 2/MTok input, USD 10/MTok output, 110,000 maximum
input tokens, 16,000 maximum output tokens, 85,000 estimated input tokens and a 380,000
micro-USD hard cap. The strict parser/cost guard/provider-adapter tests pass locally with zero
pool, transport, credential lookup, provider call or product write.

The operator explicitly authorizes continuing this exact non-production preparation. Perform a
single, auditable **Preview/UAT configuration-only** cut using the existing authenticated Railway
session and only these non-secret values in both the Studio and Workers runtime environments:

```text
NOISIA_TOPIC_EVALUATION_ENABLED=true
NOISIA_TOPIC_EVALUATION_MODEL=claude-sonnet-5
NOISIA_TOPIC_EVALUATION_PRICING_VERSION=anthropic-claude-sonnet-5-2026-08-29
NOISIA_TOPIC_EVALUATION_MAX_INPUT_TOKENS=110000
NOISIA_TOPIC_EVALUATION_MAX_OUTPUT_TOKENS=16000
NOISIA_TOPIC_EVALUATION_INPUT_USD_PER_MILLION=2
NOISIA_TOPIC_EVALUATION_OUTPUT_USD_PER_MILLION=10
NOISIA_TOPIC_EVALUATION_HARD_CAP_MICRO_USD=380000
NOISIA_TOPIC_EVALUATION_ESTIMATED_INPUT_TOKENS=85000
```

Before every setting change, prove the target is the existing non-production Preview/UAT
environment and capture the previous **non-secret** values plus a service restore plan. Never
read, print, copy, replace or otherwise expose `ANTHROPIC_API_KEY`; report only
`PRODUCT_CREDENTIAL_PRESENT=true|false`. If it is false, restore the changed non-secret values,
stop, and report `BLOCKED`.

Apply only the above values, allow the induced Studio/Workers redeployments to complete, and
reconcile read-only: service identity/deployments, deep health, a sanitized execution preflight
whose configuration is complete, baseline `0/69/11/0/0`, archived `2`, zero publication blockers,
protected digest `sha256:25e2beca3e5879e5611be85d4ae81284fcd63ed89c04a50b48da7d732b61a5a7`, and zero
runs/reservations/outboxes/candidates/provider calls/generations/adoptions/publications/serving
effects. Do not call the evaluator endpoint with POST, create a product run, queue a job, invoke a
provider, operate Brand OS forms, alter Discovery Review, or touch production.

Return exactly:

```text
BACKEND_69B5IEP_RUNTIME_PREFLIGHT=READY|BLOCKED|FAIL
UAT_TARGET=PREVIEW|BLOCKED
NONSECRET_CONFIG_APPLIED=PASS|FAIL|BLOCKED
PRODUCT_CREDENTIAL_PRESENT=true|false|BLOCKED
STUDIO_DEEP_HEALTH=PASS|FAIL|BLOCKED
WORKERS_DEEP_HEALTH=PASS|FAIL|BLOCKED
EXECUTION_CONFIGURATION_COMPLETE=true|false|BLOCKED
COUNTS=0/69/11/0/0|MISMATCH
ARCHIVED=2|MISMATCH
PROTECTED_DIGEST=sha256:25e2beca3e5879e5611be85d4ae81284fcd63ed89c04a50b48da7d732b61a5a7|MISMATCH
PRODUCT_PROVIDER_CALLS=0
REAL_GENERATIONS=0
TOPIC_RUNS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
PRODUCTION_ACCESSED=false
```

---

# COMPLETED — 69B.5I-E-O — local explicit Sonnet 5 flight-card validation

E-N proves the frozen authority is fresh with no existing runs/reservations/outboxes: envelope
digest `sha256:0685f23247e84f97a05055af3fe70ae67b634993b3175f063729c45ea0d4e7ff`, 21,195
modelable roots, 115 proposals, 805 evidence links, 71,223 source tokens and deterministic
input estimate 65,012. It is blocked only because no runtime model/pricing is selected.

Validate the following **local-only, zero-call** flight card using the configuration parser and
cost guard, without applying it anywhere: model `claude-sonnet-5`; pricing version
`anthropic-claude-sonnet-5-2026-08-29`; input rate USD 2/MTok; output rate USD 10/MTok; max input
110,000; max output 16,000; estimated input 85,000; hard cap USD 0.38 (380,000 micro-USD).
This gives a conservative allowance above the 65,012 deterministic estimate for the new
tokenizer while remaining far below the USD 18.807816 aggregate ceiling. It allows exactly one
call, no retry/fallback, requires >=10 pending-only candidates and never adopts/publishes/serves.

Confirm the provider adapter accepts the model identifier and the entire configuration validates
without a pool, transport or credential lookup. State the exact sanitized UAT config names that
a future separate enablement would set, but do not set envs, reserve spend, create a run or call
a provider. No UAT, publication, Discovery Review, production, holdout, AuthZ, secret or
infrastructure mutation.

---

# BLOCKED BEFORE WRITE — 69B.5I-E-K — focal Preview/UAT Topic Evaluation control-plane cut

E-I, E-I1 and E-I2 now pass fresh independent audit (P0=0/P1=0). Create one focused commit
and deploy only this local control plane to the existing Preview/UAT target.

Before any remote write, prove the target is non-production and capture a recoverable restore
point. Curate a strict allowlist of only E-I/I1/I2 code, tests, migration 0107 and the exact
OpenAPI contract hunk. Exclude AGENTS.md, BrandEditForm, prior Brand OS files, unrelated docs,
PROMPT_LOOPING, `.data` evidence and tools. Do not mix a Front change.

Apply only `infrastructure/db/migrations/0107_signal_topic_evaluation_control_plane.sql` once,
with SHA-256 `60dc32934c72f5840c729332f7bf7b661244b3620939a9bd0243645abd42c326`, recorded ledger
and sentinels. Deploy Studio only. Workers must remain on their prior deployment and the Topic
Evaluation feature flag remains disabled. Reconcile read-only: deployed commit, migration ledger,
Studio deep health, current semantic state `0/70/11/0/0`, archived `1`, blockers `0`, protected
digest `sha256:25e2beca3e5879e5611be85d4ae81284fcd63ed89c04a50b48da7d732b61a5a7`, and zero provider,
generation, publication, Topic adoption or serving effects.

On exact READY, open only authenticated read-only QA of GET
`/api/data-os/signal/{workspaceId}/topic-evaluation`: confirm it returns the frozen counts/digests,
`execution_enabled=false`, `execution_configuration_complete=false`, no private envelope/context
payload and no write. Do not POST it. If any mismatch, stop without a remote patch or redeploy.

Return:

```text
BACKEND_69B5IEK_UAT_CUT=READY|BLOCKED|FAIL
FOCAL_COMMIT=<sha|BLOCKED>
UAT_TARGET=PREVIEW|BLOCKED
RESTORE_POINT=PASS|FAIL|BLOCKED
MIGRATION_0107=PASS|FAIL|BLOCKED
DEPLOYED_COMMIT=<sha|BLOCKED>
STUDIO_DEEP_HEALTH=PASS|FAIL|BLOCKED
WORKERS_UNCHANGED=PASS|FAIL|BLOCKED
COUNTS=0/70/11/0/0|MISMATCH
PROTECTED_DIGEST=sha256:25e2beca3e5879e5611be85d4ae81284fcd63ed89c04a50b48da7d732b61a5a7|MISMATCH
PRODUCT_PROVIDER_CALLS=0
REAL_GENERATIONS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
PRODUCTION_ACCESSED=false
```

---

# COMPLETED — 69B.5I-E-I2 — local-only sealed flight-card configuration correction

E-I1 passed: duplicate candidate evidence and source-proposal keys now fail before database
write; the causal fake-provider integration test proves terminal failure and no second call.
Fresh independent audit found the next P1: `signalTopicEvaluationConfigurationFromEnvV1` permits
a future enabled execution with a fallback model, rates, token ceilings and
`pricing_version='unconfigured'`. A client-supplied hard cap is not sufficient cost authority.

1. Add a sanitized `execution_configuration_complete` signal to configuration/preflight.
2. When enabled, require explicit non-empty model, pricing version, input/output token ceilings,
   input/output rates, hard cap and estimated input-token configuration. Validate number forms
   and positivity; do not read, print, persist or expose the credential value.
3. Before connecting to the pool, reject an enabled but incomplete configuration with stable
   `topic_evaluation_pricing_unconfigured` or
   `topic_evaluation_execution_configuration_incomplete` error.
4. Add no-DB regressions for omitted/invalid configuration and valid sealed configuration, then
   update the sanitized OpenAPI preflight contract if necessary.

No migration, remote/UAT/browser/database mutation, provider call, real generation, Topic
adoption, publication, serving, Discovery Review redesign, production, holdout or AuthZ/secret/
infra change. Return the exact E-I2 receipt in `NEXT_PROMPT.md`.

---

# COMPLETED — 69B.5I-E-I1 — local-only strict-output terminalization correction

E-I built the missing one-call Topic Evaluation control plane locally, but fresh independent
audit reproduced a P1 in its strict-output boundary: a candidate may contain a duplicate
`evidence_refs` value (or duplicate `source_proposal_keys` value). The parser accepts it;
the later candidate-evidence relational insert rejects the second evidence row after the
provider response has already been persisted, leaving a nonterminal recovery path for the same
bad output. Close only that causal gap.

1. Reject duplicate `evidence_refs` and duplicate `source_proposal_keys` inside each candidate
   in `parseSignalTopicEvaluationOutputV1`, before any database write, with stable error codes.
2. Add Query Engine contract tests for each rejected duplicate class.
3. Add a causal DB/integration regression where a fake provider returns duplicate evidence refs:
   exactly one fake call occurs, the run settles terminal `failed`, candidate and
   candidate-evidence rows remain zero, and recovery never makes a second call.
4. Rerun only focused Query Engine/DB/Workers/Studio tests, typechecks and `git diff --check`.

No migration change unless a directly failing regression proves one is necessary. No remote,
UAT/browser/database mutation, provider call, real generation, Topic adoption, publication,
serving, Discovery Review redesign, production, holdout or AuthZ/secret/infra change.

Return exactly:

```text
BACKEND_69B5IEI1=READY|BLOCKED|FAIL
DUPLICATE_EVIDENCE_FAIL_CLOSED=PASS|FAIL
DUPLICATE_SOURCE_PROPOSAL_FAIL_CLOSED=PASS|FAIL
TERMINAL_FAILURE_NO_SECOND_CALL=PASS|FAIL
FOCAL_TESTS=<passed/total>
PRODUCT_PROVIDER_CALLS=0
REAL_GENERATIONS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
PRODUCTION_ACCESSED=false
```

---

# COMPLETED — 69B.5I-E-I — local-only one-call Topic Evaluation control plane

E-H proved the frozen corpus and Brand OS inputs are sound, but the current repo has no
single-call idempotent/recoverable Topic-evaluation runner. Build exactly that missing control
plane locally. This is not a Discovery Review redesign and not a topic run.

Implement an additive, server-only control plane for a future one-call Topic Evaluation:

1. A hand-verified forward-only schema (if persistence is needed) that records immutable
   input references/digests/counts, one idempotency key, reservation/settlement/recovery
   state, bounded request metadata, candidate output metadata and mention/evidence references
   relationally. Do not store a corpus/payload blob for browser serving; do not make a Topic,
   alter a release, or change publication/serving tables.
2. A worker/server entrypoint that deterministically builds the evaluation envelope from the
   sealed corpus, Brand OS/Knowledge and 115-proposal packet; validates a strict aggregated
   JSON response; allows exactly one provider request only in a later explicitly enabled
   execution mode; does no retry/automatic fallback; has an idempotency and crash-recovery
   state machine; and writes only candidate-evaluation artifacts when a future permitted call
   succeeds. It must use the server product credential lane by name only—never read, print or
   reuse a secret in this gate.
3. Local dry-run/contract fixtures proving: one-call limit, no-call default, duplicate
   idempotency rejection, failure recovery remains non-retrying, max cost/token validation,
   strict JSON rejection, corpus/Brand-OS digest mismatch rejection, candidate/evidence
   relational integrity, authZ, and zero Topics/publication/serving effects.
4. Documentation for the evaluator explaining what the user will later get: candidate Topics
   that remain editable/rejectable and are not automatically adopted by Signal. Success stays
   `>=10` coherent evidence-backed editable candidates.

No remote/fetch/push, UAT/database mutation, provider/Voyage/Anthropic call, actual
generation, publication, serving, Topic adoption, Discovery Review redesign, production,
holdout or AuthZ/secret/infra expansion. Do not rely on or modify the legacy multi-call
script. When READY, return focused diffs, migration filename/digest if any, tests and the
future flight-card fields. A separate independent audit is required before any UAT deploy or
provider execution.

E-E independently passed: `AUDIT_69B5IEE=PASS`, P0=0, P1=0, P2=0. It directly verified
that 0106 differs from the complete 0105 validation function by exactly one allowed action,
and reran 78 focused tests plus a disposable PostgreSQL 0000→0106 integration test. No local
audit action touched UAT, provider, generation, publication or serving.

Create and push one focused UAT commit containing only the audited E-C4/E-D/E-D1/E-D2 runtime,
test, migration and directly governing contract/documentation files. Exclude `AGENTS.md`,
BrandEditForm, all unrelated docs, PROMPT_LOOPING, evidence artifacts and tools. Before remote
write, prove the target is the existing non-production UAT branch and capture a recoverable
restore point. Apply only migration 0106 exactly once, with its SHA-256
`fd38af0fe93731f6da41a1ef9eecef36c29856f96c1e2a8421374a7f1cd9fb09`, ledger/sentinels and
safe no-op replay evidence. Deploy only Studio through the already proven UAT path; Workers
must remain unchanged.

Then reconcile read-only: deployed commit, migration ledger/sentinels, Studio deep health,
exact semantic state `0/70/11/0/0`, archived `1`, blockers `0`, protected serving digest
`sha256:25e2beca3e5879e5611be85d4ae81284fcd63ed89c04a50b48da7d732b61a5a7` and zero provider,
generation, semantic decision, publication or serving effects. Do not operate the Brand OS
forms. On any mismatch, stop without a remote patch or retry.

No production, provider, real generation, semantic decision, publication, serving,
Discovery Review/Topic redesign, holdout, pointer/binding/reader/read-mode, destruction or
AuthZ/secret/infra expansion is authorized.

Return exactly:

```text
BACKEND_69B5IEF_UAT_CUT=READY|BLOCKED|FAIL
FOCAL_COMMIT=<sha|BLOCKED>
UAT_TARGET=PREVIEW|BLOCKED
RESTORE_POINT=PASS|FAIL|BLOCKED
MIGRATION_0106=PASS|FAIL|BLOCKED
DEPLOYED_COMMIT=<sha|BLOCKED>
STUDIO_DEEP_HEALTH=PASS|FAIL|BLOCKED
WORKERS_UNCHANGED=PASS|FAIL|BLOCKED
COUNTS=0/70/11/0/0|MISMATCH
PROTECTED_DIGEST=sha256:25e2beca3e5879e5611be85d4ae81284fcd63ed89c04a50b48da7d732b61a5a7|MISMATCH
PRODUCT_PROVIDER_CALLS=0
REAL_GENERATIONS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
PRODUCTION_ACCESSED=false
```

Only complete READY opens separate authenticated, read-only UAT QA of the simple Brand OS
controls. Only after that QA may the one-call topic evaluation be sealed.

---

# COMPLETED — 69B.5I-E-C2 — resealed Preview/UAT automatic-policy cut

Focal commit `610b2085f654403db727384a8cb8ea1b15963380` was pushed to the existing UAT
branch; migration 0105 was applied once with its sealed checksum; Studio deep health
passed; Workers stayed on its pre-cut deployment; and UAT reconciled at `0/70/11/0/0`,
archived `1`, zero blockers and zero provider/publication/serving effects.

---

# COMPLETED — 69B.5I-E-C1A — protected pointer drift diagnosis

The single extra pointer is causally identified and did not activate serving. Durable
operator authority is missing, so the result is `UNAUTHORIZED_OR_UNKNOWN` and no write
gate opened.

---

# COMPLETED WITH MISMATCH — 69B.5I-E-C1 — operator-created leaf reconciliation

The new leaf passed every technical authority/lineage/evidence/applicability/collision/
relation check. E-C1 returned MISMATCH solely because the protected pointer set and
serving digest changed outside the leaf. DB reruns and write transaction IDs were zero.

---

# BLOCKED — 69B.5I-E-C — focal Preview/UAT automatic-policy cut

Backend created focal local commit `610b2085f654403db727384a8cb8ea1b15963380` with the
audited 18-path allowlist and passed local validation, but stopped before every remote
write when UAT had one additional operator-created approved leaf. Migration 0105 remains
absent. Studio is healthy and Workers/configuration are unchanged.

---

# ARCHIVED — 69B.5I-E-B3 — fresh independent contract-parity rereview

Backend returned `BACKEND_69B5IEA2_CONTRACT_PARITY=READY`. The fresh independent audit
applied disposable PostgreSQL 0000→0105, ran the terminal fake-provider cohort, passed
policy 8/8 and review/API 33/33, and returned
`AUDIT_69B5IEB3=PASS P0=0 P1=0 P2=0 can_deploy_uat=true` with zero effects.

---

# ARCHIVED — 69B.5I-E-A2 — canonical exception ordering and closed facet parity (local-only)

Fresh independent audit returned
`AUDIT_69B5IEB2=FAIL P0=0 P1=2 P2=0 can_deploy_uat=false`. Correct only the two proven
causes. Preserve every E-A/E-A1 trust boundary and unrelated dirty/untracked change.

## Required corrections

1. In migration 0105, canonicalize the complete SQL exception-reason set after adding
   relation readiness. The result must use the same distinct UTF-8 ordering as the real
   TypeScript policy before exact comparison/digest validation. A typed relation with both
   `semantic_collision` and `relation_target_unresolved` must persist as one deterministic
   exception and must not abort the terminal cohort.
2. Make `facets.review_states` a closed response object that always contains integer
   `ready`, `exception` and `resolved`, including explicit zeroes. Runtime, TypeScript,
   PostgreSQL fixtures, UI and OpenAPI must agree; the client may consume those values but
   must not be the layer that repairs a malformed sparse API response.

## Required proof

- Add failing-before/passing-after coverage for a real terminal fake-provider cohort whose
  typed relation has a semantic collision and a missing/invalid relation target. Prove the
  cohort completes, the leaf is one exception with the canonical reason order, exact
  outcomes/events/run counts reconcile and no unrelated proposal aborts.
- Add contract coverage showing all three review-state facet keys for mixed and zero-count
  states. Update the existing PostgreSQL expectation that currently accepts a sparse map.
- Retain the generic-writer/direct-SQL, evidence/parent/collision/graph, exact key/event,
  stale-parent, legacy digest and localized exception tests from E-A1.
- Execute focal tests, disposable PostgreSQL 0000→0105 and the real terminal path with a
  provider fake. If PostgreSQL cannot run, return BLOCKED; do not promote sealed prior
  evidence as passing-after for this new diff. Run relevant typechecks, OpenAPI/i18n and
  `git diff --check`. Avoid a full build unless a touched runtime contract requires it.

Use `apply_patch`. Local-only: no commit, push, fetch/remotes, UAT, browser, real provider,
real generation, publication, Topics, serving, production, holdout or AuthZ/secret/infra.
Do not touch Front Recovery.

Return exactly:

```text
BACKEND_69B5IEA2_CONTRACT_PARITY=READY|BLOCKED|FAIL
CANONICAL_EXCEPTION_REASONS=PASS|FAIL|BLOCKED
CLOSED_REVIEW_FACETS=PASS|FAIL|BLOCKED
TERMINAL_MIXED_EXCEPTION=PASS|FAIL|BLOCKED
POSTGRES_INTEGRATION=PASS|FAIL|BLOCKED
FOCAL_TESTS=<passed/total|BLOCKED>
PRODUCT_PROVIDER_CALLS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
PRODUCTION_ACCESSED=false
```

Only complete READY opens a fresh independent E-B3 read-only rereview.

---

# ARCHIVED — 69B.5I-E-B2 — fresh independent automatic-policy trust-boundary rereview

Backend returned `BACKEND_69B5IEA1_TRUST_BOUNDARY=READY` with all subgates PASS.
Migration 0105 remains local, uncommitted and unapplied. Run one fresh, bounded, read-only
independent audit of the corrected E-A/E-A1 allowlist and sealed evidence.

The reviewer must reproduce or disprove all prior findings: generic-writer opt-in,
direct-SQL schema/collision forgery, self/cyclic/transitively invalid relations, missing or
extra cohort outcomes, null/wrong event linkage, run-count tampering, terminal-state ready
count divergence, legacy confidence digest drift, nonpersistable exception reasons and
missing localized exception detail. It must exercise the real terminal fake-provider run
path, not only the pure policy.

Audit exact append-only/idempotent authority, parent/evidence freshness, complete key-set
equality, event linkage, run metadata, UI/API/preflight/OpenAPI/i18n parity and the strict
separation from publication. No edits, commit, push, remotes, UAT, provider, real
generation, publication, Topics, serving, production, holdout or AuthZ/secret/infra.

Return:

```text
AUDIT_69B5IEB2=PASS|FAIL|BLOCKED
P0=<n>
P1=<n>
P2=<n>
can_deploy_uat=true|false
```

Only PASS with P0=0/P1=0 opens a separate focal commit/push, exact-once 0105 application,
Preview/UAT deployment and authenticated read-only QA gate.

---

# ARCHIVED — 69B.5I-E-A1 — close automatic-policy trust-boundary bypasses (local-only)

69B.5I-E-A is locally implemented, but fresh independent gate E-B returned
`AUDIT_69B5IEB=FAIL P0=0 P1=5 P2=3 can_deploy_uat=false`. Do not deploy or apply 0105.
Correct only the demonstrated causes below while preserving the approved trust-and-revert
product model and every unrelated dirty/untracked file.

## Required causal corrections

1. Remove public/generic opt-in to automatic disposition. Only the real terminal
   `processSignalSemanticContextProposalRunV1` path may invoke it, bound to the exact
   completed run, settled private response digest, provider lineage, generation authority,
   operation and complete proposal cohort. Generic append and direct SQL attempts must
   fail closed.
2. Make PostgreSQL independently validate every automatic outcome against the same closed
   schema, semantic identity/collision rules, evidence/current-source authority, parent
   applicability and relation graph. Digests alone are not authority.
3. Reject self-relations and cycles. Compute relation readiness to a fixed point so a
   relation whose target is invalid becomes a deterministic exception; it must not become
   ready in TypeScript or abort an otherwise valid cohort in SQL.
4. Enforce exact cohort completeness: every run proposal key appears exactly once as
   either one valid ready successor or one valid exception; no omitted/extra keys; every
   ready event has a non-null exact element link; no null, unrelated, duplicated or marker-
   only events; run ready/exception counts must equal the validated cohort.
5. Define `ready` consistently as current active approved leaves. Rejected, merged and
   archived leaves are not ready. Reconcile run metadata, review facets, summary UI,
   preflight counts and closed OpenAPI.
6. Preserve the pre-0105 numeric confidence representation and digest under its existing
   contract version. If new canonicalization is required, version it explicitly only for
   the new automatic-policy authority; do not silently rehash legacy/nonautomatic rows.
7. Make every advertised exception reason persistable through the production path, or
   narrow the closed taxonomy truthfully. Render localized, operator-safe exception reasons
   in es-MX and en-US instead of only a generic warning.

## Required adversarial proof

- Exercise the real proposal-run completion path and reconcile run counts.
- Prove generic-writer and direct-SQL forgery rejection.
- Cover schema/collision bypass, self/cyclic/transitive relations, missing automatic
  outcomes, null/wrong/extra event linkage, run-count tampering, stale-parent persistence
  and mixed approved/rejected/merged/archived UI counts.
- Retain passing coverage for workspace-envelope inheritance, explicit sealed locale
  variants, provider prose never being evidence, one-Save editing, exact undo and
  archive/restore.
- Rerun focal unit tests, disposable PostgreSQL 0000→0105, complete relevant Studio/DB/
  Workers suites, typechecks, lint, build, OpenAPI/i18n validation, secret scan and
  `git diff --check`.

Use `apply_patch`. Local-only: no commit, push, remotes, UAT, provider, real generation,
publication, Topics, serving, production, holdout or AuthZ/secret/infra changes.

Return exactly:

```text
BACKEND_69B5IEA1_TRUST_BOUNDARY=READY|BLOCKED|FAIL
RUN_BOUND_AUTOMATIC_AUTHORITY=PASS|FAIL|BLOCKED
DIRECT_SQL_FAIL_CLOSED=PASS|FAIL|BLOCKED
RELATION_GRAPH_FAIL_CLOSED=PASS|FAIL|BLOCKED
EXACT_COHORT_EVENTS=PASS|FAIL|BLOCKED
READY_COUNTS_RECONCILED=PASS|FAIL|BLOCKED
LEGACY_DIGEST_STABLE=PASS|FAIL|BLOCKED
EXCEPTION_TAXONOMY_VISIBLE=PASS|FAIL|BLOCKED
POSTGRES_INTEGRATION=PASS|FAIL|BLOCKED
FOCAL_TESTS=<passed/total|BLOCKED>
PRODUCT_PROVIDER_CALLS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
PRODUCTION_ACCESSED=false
```

Only complete READY opens a fresh E-B2 independent read-only audit.

---

# ARCHIVED — 69B.5I-E-A — trust-and-revert automatic generation policy (local-only)

69B.5I-D3 is closed. Backend deployed focal commit
`76c6161e70ac580d5b36139bbea3161f866fd351`, applied only migration 0104 exactly once,
kept Workers unchanged and returned clean deep health. Authenticated read-only UAT
preflight is exact `0 pending / 69 approved / 11 rejected / 0 merged / 0 open annotations`,
archived `1`, locale/global unresolved `0` and publication blockers `0`. No provider,
publication, serving or production effect occurred.

Implement locally the operator-approved product model in
`docs/product/71_BRAND_OS_VOCABULARY_AND_TOPIC_DISCOVERY_PRODUCT_MODEL.md`. The product
goal is not to remove auditability; it is to stop exposing the internal ledger as a form.

## Required behavior

1. A future `Vocabulario y límites de la marca` run produces a usable, fully populated,
   editable draft after its proposal append completes. It must not leave every ordinary
   evidence-backed proposal pending by default.
2. Applicability inherits the sealed parent Acquisition Plan/Brand OS market and language
   envelope. Workspace-wide concepts remain inherited/global across that envelope;
   `en-US` or `es-MX` is explicit only for a genuine locale variant or supported exception.
   Never copy the primary UI locale onto all elements.
3. Add a deterministic, versioned, server-owned automatic disposition policy. It may mark
   a proposal ready only when its closed schema, evidence references, relation targets,
   collision checks, parent authority and applicability all validate. Provider text alone
   is never evidence. Any ambiguity, contradiction, missing evidence, unresolved relation,
   duplicate conflict or unsupported locale becomes an explicit exception.
4. Ordinary ready elements are active/editable without per-leaf reason, prose rationale or
   extra confirmation. The server records policy version, actor/system authority, timestamp,
   before/after state, evidence digest and applicability lineage append-only.
5. Preserve the already shipped one-Save edit, exact version undo, archive/restore and
   simple add flows. A user must be able to correct or remove an automatic result later.
6. Change the UI model from `Claude propone; revisión humana de todo` to a truthful
   `resultado listo + excepciones` model. Show counts for ready elements and genuine
   exceptions. Keep expert evidence/history accessible but secondary. Do not display a
   provider name in product copy.
7. Keep publication separate and fail-closed. This gate does not publish, call a provider,
   execute a real generation, touch UAT or create Topics.

## Tests and evidence

- Start with failing-before fixtures covering at least: ordinary evidence-backed global,
  explicit locale variant, missing/contradictory evidence, exact collision, unresolved
  typed-relation target, invalid schema and stale parent authority.
- Prove passing-after automatic ready draft plus exception-only queue using provider fakes.
- Prove no default-to-primary-locale, no autoapproval from provider prose alone, no orphan
  state, exact counts/digests, idempotency and reversible history.
- Update closed API/OpenAPI/i18n contracts and product documentation where required.
- Run focal tests, real disposable PostgreSQL integration, Studio relevant/full tests,
  typecheck, lint, build, secret scan and `git diff --check`.

Use `apply_patch` for edits and preserve all unrelated dirty/untracked files. No commit or
push. Do not access remotes, browser sessions, Railway, production, holdout, provider,
publication, Signal serving/readers/pointers/bindings/read mode or 10C/10D/10E.

Return exactly:

```text
BACKEND_69B5IEA_AUTOMATIC_POLICY=READY|BLOCKED|FAIL
AUTOMATIC_READY_DRAFT=PASS|FAIL|BLOCKED
PARENT_APPLICABILITY_INHERITANCE=PASS|FAIL|BLOCKED
EXCEPTION_ONLY_REVIEW=PASS|FAIL|BLOCKED
ORDINARY_CEREMONY_REMOVED=PASS|FAIL|BLOCKED
REVERSIBLE_HISTORY=PASS|FAIL|BLOCKED
POSTGRES_INTEGRATION=PASS|FAIL|BLOCKED
FOCAL_TESTS=<passed/total|BLOCKED>
STUDIO_TESTS=<passed/total|BLOCKED>
PRODUCT_PROVIDER_CALLS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
PRODUCTION_ACCESSED=false
```

Only complete READY opens a fresh independent read-only audit. Do not deploy or execute a
real generation in this gate.

---

# ARCHIVED — 69B.5I-D2 — independent audit of archived-leaf publication accounting

69B.5I-D1 is locally READY. Migration 0104 is forward-only and sealed at
`sha256:82cb5b0c887627613311e2c74d9010a0627837b02332c6e56691b6fd32eb95ab`.
Real PostgreSQL proves ordinary and operator-created archived leaves balance the graph
equation while remaining excluded from publication candidates. Rollback-only adversarial
fixtures prove successor forks and merge cycles remain fail-closed. Contract 22/22,
PostgreSQL 1/1, Studio typecheck/lint/build and diff check are green.

Obtain one fresh independent read-only audit of exactly:

- `infrastructure/db/migrations/0104_signal_semantic_context_archived_publication_accounting.sql`;
- the 0104 additions in `signal-semantic-context-publication-v2.test.ts`;
- the 0104 migration ordering, archived assertions and rollback-only fork/cycle matrix in
  `signal-semantic-context-pack.postgres.test.ts`.

The audit must verify wrapper-chain preservation, exact leaf equation including archived,
independent fork/cycle recomputation, preservation of every unrelated blocker/count,
one canonical top/nested preflight object and digest, and zero candidate/evidence inclusion
for archived leaves. It must return P0/P1/P2 counts and `can_deploy_uat`.

The requested Claude CLI review lane is currently BLOCKED by the direct host result
`CLAUDE=NOT_FOUND`. Do not substitute the earlier truncated Advisor response, fabricate an
independent verdict or reuse the exposed credential. A reliable reviewer or explicit
operator delegation of the audit is required before D3.

No commit, push, deploy, UAT mutation, provider, publication, serving, production, 10C,
10D or 10E action is authorized in D2.

---

# ARCHIVED — 69B.5I-D1 — archived-leaf publication accounting correction (local-only)

Authenticated 69B.5I-D QA proved the trust-and-revert UI and also exposed one exact
publication-contract defect. Preview/UAT is terminal at semantic counts `0/69/11/0/0`
with one archived synthetic fixture. The free preflight reports exactly one blocker:
`graph_count_inconsistent`.

The cause is proven. The pre-0102 snapshot counts every current leaf in `total_leaves`
but accounts only pending/approved/rejected/merged in its graph equation. Migration 0102
introduced the terminal archived disposition and adds `counts.archived`, yet its wrapper
does not remove/recompute the inherited graph-count guard. Therefore one legitimate
archived leaf is excluded from candidates as intended but falsely blocks publication.

Implement a minimal forward-only migration 0104 plus tests:

1. Version the current `signal_semantic_context_publication_snapshot_v2` wrapper; never
   rewrite applied migrations 0097–0103.
2. Recompute current leaf `total`, pending, approved, rejected, merged and archived.
   Recompute fork and merge-cycle counts independently.
3. Remove inherited `graph_count_inconsistent` only when the exact equation including
   archived leaves balances and fork/cycle counts are zero. Otherwise retain/re-add it.
4. Preserve all other blockers and counts. Build one canonical top-level/nested preflight
   object and digest it once. Archived leaves stay absent from the candidate/evidence pack.
5. Add failing-before/passing-after PostgreSQL coverage for ordinary and newly created
   archived leaves. Assert archived=1, graph blocker absent, publishability otherwise
   unchanged, exact top-level/nested counts+blockers+publishable, and exact digest. Keep
   adversarial fork/cycle fail-closed coverage.
6. Run focal tests, real PostgreSQL suite, Studio full relevant suites, typecheck, lint,
   build, migration checksum and `git diff --check`.

This gate is local-only. Do not commit, push, deploy, operate UAT, restore the fixture,
publish, call a provider, open 10C/10D/10E or touch production/holdout/serving/readers/
pointers/bindings/read mode/AuthZ/secrets/infra.

Required result:

```text
BACKEND_69B5ID1_ARCHIVED_ACCOUNTING=READY|BLOCKED|FAIL
MIGRATION_0104_CHECKSUM=<sha256|none>
POSTGRES_ARCHIVED_PREFLIGHT=PASS|FAIL|BLOCKED
FORK_CYCLE_FAIL_CLOSED=PASS|FAIL|BLOCKED
PRODUCT_PROVIDER_CALLS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
```

---

# ARCHIVED — 69B.5I-D — focal Preview/UAT cut and authenticated trust-and-revert QA

69B.5I-A/B/C are audited PASS with P0=0/P1=0. Migrations 0101/0102/0103 remain
local-only and unapplied remotely. Preview/UAT remains at its prior exact terminal
baseline and no provider/publication/serving effect occurred.

Create one focal Preview/UAT release cut for the audited applicability, ordinary editing
and simple creation stack. Preserve the shared dirty worktree outside the exact allowlist.

## Focal release authority

1. Create one commit containing only the audited 69B.5I-A/B/C runtime/API/schema/docs/
   test files and migrations 0101, 0102 and 0103. Exclude `AGENTS.md`, `BrandEditForm`,
   docs 00/31/52/55/56/63/64/66, Prompt Looping, tools/codex-advisor and every unrelated
   dirty/untracked file.
2. Prove the existing branch and Railway target are Preview/UAT, never production. Record
   the restore point before any remote effect.
3. Verify the exact migration checksums from STATE. Apply only 0101→0102→0103, each
   exactly once, with ledger and pre/post sentinels. Do not run generation tooling or
   another migration.
4. Push only the existing UAT branch and deploy only affected Studio runtime. Do not
   deploy Workers; stop if repository automation cannot guarantee the service boundary.
5. Verify deep health, deployed commit, migration ledger, semantic-context read-only
   baseline and protected-state digest. No product provider, publication, serving or
   production effect is allowed.

## Authenticated UAT QA

After Backend READY reconciles exactly, use a reliable authenticated task-owned browser
for a reversible synthetic fixture only:

- confirm Brand OS `Vocabulario y límites` loads at desktop and below 760px;
- confirm ordinary inherited items show sealed parent markets/locales without a locale
  decision ceremony;
- edit one synthetic item with one Save and zero reason/rationale/confirmation, refresh,
  Undo, Archive and Restore, reconciling after every step;
- add one synthetic inherited element with one Save; confirm it is immediately active
  and approved, editable, archivable and restorable;
- exercise an exact canonical collision and prove no new element/operation/event; open
  the existing element;
- exercise one normalized-display suggestion and prove it is advisory only, with no
  auto-merge;
- verify focus return, Escape/cancel, rapid double-submit protection, console and layout;
- restore the named synthetic fixture to a clearly documented terminal state.

Required Backend final:

```text
BACKEND_69B5ID_UAT_CUT=READY|BLOCKED|FAIL
FOCAL_COMMIT=<sha|none>
TARGET=Preview/UAT|other
RESTORE_POINT=<sha/deployment>
MIGRATIONS=0101,0102,0103 exact-once|other
DEPLOYED_SERVICE=Studio|other
DEEP_HEALTH=PASS|FAIL|BLOCKED
PRODUCT_PROVIDER_CALLS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
PRODUCTION_ACCESSED=false
```

Required QA final:

```text
QA_69B5ID_TRUST_AND_REVERT=PASS|FAIL|BLOCKED
INHERITED_APPLICABILITY=PASS|FAIL|BLOCKED
EDIT_UNDO_ARCHIVE_RESTORE=PASS|FAIL|BLOCKED
SIMPLE_CREATE=PASS|FAIL|BLOCKED
EXACT_COLLISION_ZERO_WRITE=PASS|FAIL|BLOCKED
DISPLAY_SUGGESTION_ADVISORY=PASS|FAIL|BLOCKED
DESKTOP_QA=PASS|FAIL|BLOCKED
RESPONSIVE_LT760_QA=PASS|FAIL|BLOCKED
CONSOLE_LAYOUT=PASS|FAIL|BLOCKED
PRODUCT_PROVIDER_CALLS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
```

Stop on any allowlist, target, migration, deployment, health, count, digest or QA
mismatch. Do not patch remotely or improvise compensating writes.

Do not publish the Semantic Context Pack, start 10C.3B/10D/10E, create Topics, touch
production/holdout/serving/readers/pointers/bindings/read mode, call a provider, deploy
Workers or expand AuthZ/secrets/infra.

---

# ARCHIVED — 69B.5I-C — simple element creation and duplicate guidance (local-only)

69B.5I-A and 69B.5I-B are audited PASS with P0=0/P1=0. Migrations 0101/0102 remain
local-only and unapplied remotely. Preview/UAT remains exact `0/69/11/0/0` and no
provider/publication/serving effect occurred.

## Required operator experience

1. The workbench exposes `Agregar elemento`. It opens one short form and one primary
   `Guardar` action; cancel/Escape produces zero request.
2. Ask only for human semantic values: type, display name, canonical key, optional scope,
   optional typed relation and optional applicability exception. Do not ask for reason,
   prose rationale, reviewer, evidence IDs, disposition, audit fields or a second
   confirmation.
3. Default applicability is the sealed parent workspace envelope (`Hereda MX + US` in
   the current fixture) with raw `locale=null`. A user may explicitly choose Global or
   one sealed generation locale. A true `locale_variant` cannot inherit an empty locale.
4. One successful save creates one current active approved element atomically. There is
   no second approval step. The created element immediately uses the existing Edit,
   Undo-if-available, Archive and Restore behaviors; creation itself is reversed by
   Archive/Restore rather than deleting history.
5. Before and while editing, show bounded duplicate guidance in plain language. Exact
   active canonical collisions must disable/no-op creation and point to the existing
   element. Exact normalized-display matches are suggestions, not automatic merges.
   `Abrir existente` is non-writing. The operator may still add a genuinely distinct
   concept unless it would create the publication contract's exact canonical collision.
6. Do not auto-merge, auto-archive or move relations based on string similarity. The
   existing structural N-to-1 governed merge stays separate. Retiring an unwanted
   duplicate uses the already reversible Archive command.

## Server-owned creation contract

- Add one closed management action, e.g. `create-semantic-context-element-v1`, with a
  dedicated route and management AuthZ/workspace ownership. Use a forward-only 0103 only
  if a DB contract change is required; hand-verify it and apply only to disposable local
  PostgreSQL after 0102.
- The browser may send only generation key, semantic values and optional applicability,
  plus Idempotency-Key. It cannot choose element key, actor, timestamps, disposition,
  evidence/source refs, operation/event kinds, authority snapshots/digests or lineage.
- Generate an immutable server-owned `element_key` deterministically from the normalized
  semantic input. Collision handling must be deterministic and replay-safe; never make
  the browser guess an internal key.
- The server creates actor/time/create-diff/parent-applicability audit basis and an active
  approved version in one transaction. Exact replay returns the original result; same
  idempotency key with different input fails; an exact active canonical collision writes
  nothing and returns an operator-readable existing key.
- Represent operator-authored provenance honestly. Prefer a typed
  `semantic_context_operator_input` evidence reference bound to the completed management
  operation (or an equally explicit operator-input record). Do not fabricate a Brand OS
  or Knowledge source. DB validation, review evidence projection and preflight must
  recognize that source only for this closed management action and exact workspace/
  actor/operation lineage. Existing provider/Brand OS/Knowledge evidence rules may not
  be weakened.
- Relation targets must be current active approved leaves in the same generation;
  self/cross-workspace targets fail. Applicability authority must reuse the audited 0101/
  0102 resolver and fail closed on missing/stale/spoofed parent authority.
- Duplicate guidance is read-only, bounded (maximum 5), deterministic and server-owned.
  At minimum distinguish exact canonical collision from normalized-display suggestion.
  Do not call a provider or infer semantic sameness from string similarity.
- Current counts, review paging/search/filters, candidate pack and publication preflight
  must include an active operator-created leaf and exclude it when archived. Top-level
  and nested preflight counts/blockers/digest remain exact.

## UI constraints

- Reuse the existing workbench and drawer system; no second modal language or technical
  ledger copy.
- Plain-language duplicate card: existing title/type/applicability plus `Abrir existente`.
  Never expose hashes, operation IDs or evidence internals.
- Preserve focus return, Escape/cancel, rapid double-submit lock, responsive layout and
  bilingual `es-MX`/`en-US` messages.

## Mandatory evidence

- failing-before/passing-after UI and route tests for one short creation form with zero
  reason/rationale/confirmation fields;
- real PostgreSQL create, exact replay, conflicting replay, canonical collision no-write,
  deterministic key, inherited/global/locale applicability and locale-variant guard;
- operator-input source resolves as current and readable, while forged/cross-workspace/
  wrong-action source refs fail closed in direct SQL and preflight;
- relation, stale authority, malformed/extra input and double-event adversarials;
- duplicate suggestions are bounded/read-only/deterministic and never auto-merge;
- archive/restore of a created leaf, immutable operation/evidence/history and exact count/
  digest transitions;
- full 0101/0102 regressions, OpenAPI, i18n, Studio/DB/Workers tests/typechecks, lint/build,
  `git diff --check`, focal secret scan and exact allowlist.

Required first line:

```text
BACKEND_69B5IC_SIMPLE_CREATION=READY|BLOCKED|FAIL
```

Required final fields:

```text
FAILING_BEFORE=<causal proof>
PASSING_AFTER=<exact operator flow>
CREATE_CONTRACT=<version/request/result>
ELEMENT_KEY_RULE=<deterministic rule>
OPERATOR_INPUT_PROVENANCE=<typed exact source contract>
DUPLICATE_GUIDANCE=<exact collision and suggestion behavior>
CLIENT_REASON_FIELDS=0|other
AUTO_MERGES=0|other
GENERIC_AUTHORITY_BYPASS_COUNT=0|other
MIGRATION=<none|0103 checksum/smoke>
REAL_UAT_WRITES=0
PROVIDER_CALLS=0
REMOTE_WRITES=0
PUBLICATIONS=0
SERVING_EFFECTS=0
TESTS=<commands/counts>
ALLOWLIST=<exact paths>
```

Do not commit, push, deploy, operate UAT/browser, run a provider, publish a pack, start
10C.3B/10D/10E or touch production, holdout, serving, readers, pointers, bindings, read
mode, AuthZ, secrets or infrastructure settings.

---

# ARCHIVED — BLOCKED_HANDOFF — E-AS Insights output channel failed twice

Do not resend E-AS to Insights Manager v8 and do not open E-AT. The original E-AS turn
completed with no visible items; the single delivery-only retry also completed empty.
Per the loop contract, this task is now hard output-blocked.

The frozen Backend authority remains valid and no semantic decision was made:

```text
0 pending / 69 approved / 11 rejected / 0 merged / 0 annotations
unresolved locale/global = 52
frozen key = competitor.apple-homepod
writes/providers/publications/serving effects = 0
```

Operator action is required: repair/provision a reliable task-owned authenticated
Insights browser channel, or explicitly delegate the independent evidence audit to a
different reliable operator task. The replacement must start with E-AS read-only using
the already frozen key/signature/digests; do not rerun Backend E-AR.

## Frozen E-AS contract — preserve for repaired channel

69B.5H-E-AR is audited READY. Preview/UAT remains exact `0/69/11/0/0`, unresolved
locale/global 52, two valid locale variants, one
`locale_market_required_unresolved` blocker and zero transient/protected effects.

Use only the Insights Manager v8 task-owned authenticated in-app browser. Refresh and
reread only this exact frozen cohort:

```text
key = competitor.apple-homepod
display = Apple HomePod
kind = competitor_term
scope = competitor
current locale = null
evidence class = brand_os_only
relations = supports only
evidence count = 2
source-set = sha256:744cc4a12f9552c0d34f5ce5a909073db9c791df904d7eade09b3c0c375e73f9
authority digest = sha256:f6f70b418cae77f48cf063e5988772462ee3dd3f4091fdde5d921c22d246ef72
```

The two current Brand OS supports are the governed competitor entry and governed
identity seed for Apple HomePod. Reread their visible text and classify the leaf exactly
once as `evidence_supported_global`, `evidence_supported_en-US`,
`evidence_supported_es-MX`, `knowledge_blocked` or `mismatch`. A competitor identity may
be Global only if the visible evidence supports a market-agnostic identity boundary; do
not infer Global from kind/scope and do not turn MX/US analysis lenses into availability.

If supported, freeze the exact decision target, closed reason, leaf-specific rationale
and confirmation for one later submit. Do not select, inspect or decide
`competitor.apple-siri` or any other leaf. Do not submit.

Required final:

```text
QA_69B5HEAS_PLAN=READY|BLOCKED|FAIL
COUNTS=0/69/11/0/0|other
UNRESOLVED_LOCALE_GLOBAL=52|other
ELEMENT_KEY=competitor.apple-homepod
CLASSIFICATION=evidence_supported_global|evidence_supported_en-US|evidence_supported_es-MX|knowledge_blocked|mismatch
DECISION_TARGET=global|en-US|es-MX|none
DECISION_REASON=<closed reason or none>
DECISION_RATIONALE=<exact leaf-specific rationale or none>
CONFIRMATION=<exact or none>
MUTATING_REQUESTS=0
PROVIDER_CALLS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
```

No provider, publication, clustering, Topic Contract, 10C.3B, 10D, 10E or production.
Only exact audited READY opens a separate one-decision gate.

<!-- Archived exact E-H approval packet:

1. `alias.alexa`
2. `alias.alexa-plus`
3. `alias.alexa-plus-symbol`

Use reason `alias_or_variant` for all three, with these frozen rationales:

1. `alias.alexa`: `El sucesor actual conserva exactamente la forma escrita «Alexa». El
   término vectorial «Alexa», clasificado como «Governed identity seeds», permanece
   Vigente y sustenta directamente este elemento como alias de la identidad gobernada
   Amazon Alexa; la fuente «Brand identity» Histórica se usa sólo como contexto. La
   sucesión append-only es íntegra (v2 aprobada → v3 pendiente), con autoridad de locale
   sellada como Global explícito y sin cambios de payload, scope ni evidencia vigente.
   Aprobar individualmente tal como está.`
2. `alias.alexa-plus`: `El sucesor actual conserva exactamente la forma escrita «Alexa
   Plus». El término vectorial «Alexa Plus», clasificado como «Governed identity seeds»,
   permanece Vigente y sustenta directamente este elemento como alias de la identidad
   gobernada Amazon Alexa; la fuente «Brand identity» Histórica se usa sólo como contexto.
   La sucesión append-only es íntegra (v2 aprobada → v3 pendiente), con autoridad de
   locale sellada como Global explícito y sin cambios de payload, scope ni evidencia
   vigente. Aprobar individualmente tal como está.`
3. `alias.alexa-plus-symbol`: `El sucesor actual conserva exactamente la forma escrita
   «Alexa+». El término vectorial «Alexa+», clasificado como «Governed identity seeds»,
   permanece Vigente y sustenta directamente este elemento como alias de la identidad
   gobernada Amazon Alexa; la fuente «Brand identity» Histórica se usa sólo como contexto.
   La sucesión append-only es íntegra (v2 aprobada → v3 pendiente), con autoridad de
   locale sellada como Global explícito y sin cambios de payload, scope ni evidencia
   vigente. Aprobar individualmente tal como está.`

Insights submits exactly once per key and refreshes after each. Backend reconciles
read-only after every checkpoint. Required checkpoints:

`3/66/11/0/0 → 2/67/11/0/0 → 1/68/11/0/0 → 0/69/11/0/0`.

Unresolved locale/global remains 62. Stop immediately on mismatch; do not retry,
compensate, bulk-approve or touch another leaf.

Required finals:

```text
QA_69B5HEH_APPROVALS=PASS|FAIL|BLOCKED
BACKEND_69B5HEH_RECONCILIATION=PASS|FAIL|BLOCKED
START_COUNTS=3/66/11/0/0
CHECKPOINTS=<exact sequence>
FINAL_COUNTS=0/69/11/0/0|other
INDIVIDUAL_APPROVALS=3|other
UNRESOLVED_LOCALE_GLOBAL=62|other
PROVIDER_CALLS=0
PUBLICATIONS=0
SERVING_EFFECTS=0
```

Only exact dual PASS opens the next deterministic read-only cohort. -->

## Front Recovery — PAUSED on independent-review output delivery

Fase 0G.1 proved and fixed the DB harness locally in one commit on top of the reviewed
candidate:

- `062bbc84de155d409b7e6c5a123ba9fc21591262`
- `6861ceb3285c38dfe5aa55fc8998765832c4cf97`
- `3b574f5d1aceb72784aa7953de346498fd049e86`
- `c0fc67106a145de045854da29b8435555307731d`
- `571486375c089e8af351e350ea2ce878feab26e7` — harness-only correction.

The original independent reviewer completed empty twice. The replacement independent
reviewer also completed empty with no commentary, evidence or verdict. Per the explicit
loop rule, the Front track is paused and no more reviewer tasks, pushes, deployment or
Railway mutations may start automatically.

The local candidate remains preserved: reviewed Front series through `c0fc6710…` plus
one-file harness commit `5714863…`; implementer evidence reports DB integration 1/1,
full DB-backed Studio 463/463, focal 37/37 and static gates green. This does not replace
the missing independent verdict.

The release path also remains blocked: both Railway services watch
`infrastructure/db/**`; the current normal branch push can start Workers. Do not solve
this by convenience. After review PASS, an operator-safe Railway step must either prove
a service-scoped Studio deployment without Workers or explicitly authorize a temporary
UAT autodeploy suppression followed by exact restoration.

Operator repair options are: provision one reliable independent-review task, or
explicitly authorize the auditor to perform the independent code audit directly. Even
after PASS, Railway still needs a service-scoped deployment path or separate explicit
authority for temporary Workers-UAT autodeploy suppression and exact restoration.

## Global stop conditions

No production, product provider, publication, serving cutover, holdout or 10C.3B in
either active gate. Preserve the shared dirty worktree; Front changes remain isolated
and unpushed.

---

# ARCHIVED — 69B.4A — preregister a real Semantic Context review canary

Work exclusively in Preview/UAT on Amazon Alexa generation v6. This gate is read-only.

## Backend packet

In one repeatable-read/read-only audit, prove:

- effective generation and current-leaf authority;
- exact pending/approved/rejected/merged/open-annotation counts;
- no nonterminal proposal/review/publication operation;
- Brand OS, Knowledge, locale and provider lineage remain current;
- approve, guided reject, correction, annotation and N→1 merge endpoints are deployed;
- free publication preflight remains sealed and non-writing;
- protected state and read mode are unchanged.

Return an operator-safe baseline digest and exact action constraints. Do not propose
semantic decisions, write state, operate the browser or call providers.

Required first line: `BACKEND_69B4A_BASELINE=READY|BLOCKED|FAIL`.

## Human-review packet

The current Insights Manager task failed to deliver a visible result twice and must not
be retried again. Reuse the earlier completed read-only packet
`QA_69A7W_PROPOSAL_QUALITY=FAIL`, which inspected 80/80 proposals and performed zero
writes, only if Backend proves that generation v6 and the exact 80 pending / 0 approved /
0 rejected / 0 merged authority are unchanged.

From that packet, preregister a conservative canary covering at most 12 current leaves:

- several genuinely strong approvals across identity/category/product/benefit or scope;
- corrections only where display, canonical key, scope, locale or relation is genuinely
  wrong or incomplete;
- one uncertainty/needs-context annotation when supported;
- one same-kind duplicate pair only when the evidence supports N→1 merge; the packet's
  cross-lens near-duplicates do not qualify and must not be forced into a merge;
- rejection only when the evidence supports out-of-scope, contradiction or unsupported
  content. Never manufacture a rejection to exercise a button.

For every selected leaf record operator-safe key/title, intended action, closed reason,
rationale and evidence/Brand OS basis. Report expected count deltas and mutation order.
Do not click a mutation or publication CTA in this gate. If current authority differs
from the sealed 80/80 packet, return BLOCKED and require fresh human review.

Required first line: `QA_69B4A_DECISION_PLAN=READY|BLOCKED|FAIL`.

## Canary preregistered after independent challenge

Backend proved generation v6 and the exact `80 pending / 0 approved / 0 rejected /
0 merged / 0 open annotations` authority unchanged. The prior 80/80 human packet is
therefore admissible. Independent challenge returned `P0=0`, removed two avoidable
risks and accepted the following exact nine-leaf plan.

Approve individually, with no bulk action:

| Key | Current title | Reason | Governed basis |
|---|---|---|---|
| `abstention.service-vs-device-ambiguity` | Abstain when mention cannot distinguish Alexa service from Echo device, ecosystem, or Amazon generic | `semantic_boundary` | Current Brand OS/Knowledge questions and semantic-boundary sources both support the abstention. |
| `anchor.negative.alexa-not-amazon-company` | Alexa is not synonymous with Amazon the company | `semantic_boundary` | Current identity/category architecture explicitly separates Alexa from Amazon corporate. |
| `anchor.negative.echo-not-competitor` | Echo device family is not a competitor; it is Alexa's own hardware | `semantic_boundary` | Current architecture defines Echo as owned Alexa hardware, not a competitor. |
| `exclusion.amazon-corporate` | Amazon corporate, retail, logistics, Prime, AWS, or e-commerce without Alexa relation | `semantic_boundary` | Current semantic-boundary source explicitly excludes generic Amazon-company conversation. |
| `homonym.alexa-internet-rank` | Alexa Internet / Alexa Rank (web analytics service) | `semantic_boundary` | Current semantic-boundary source distinguishes the governed homonym. |
| `alias.alexa-plus` | Alexa Plus | `alias_or_variant` | The current Brand OS aliases visibly include `Alexa Plus`. |
| `alias.alexa-plus-symbol` | Alexa+ | `alias_or_variant` | The current governed identity seed explicitly contains `Alexa+`; it is a distinct written variant. |

Correct exactly one pending leaf:

- `product.echo`: change only `display_text` from `Echo` to
  `Amazon Echo device family`; reason `canonicalization`; keep
  `canonical_key=product.echo`, kind, scope, entity and all server-owned evidence.
  Rationale: current Brand OS defines Echo as Amazon's owned device family and the
  expanded display removes the ordinary-word homonym without changing the concept.

Create exactly one open annotation:

- `friction.latency`: `needs_more_context`, reason `insufficient_context`.
  Rationale: current governed evidence names latency, but does not yet demonstrate that
  it is a specific and generalizable friction across the governed MX/US markets.

Explicit omissions:

- do not approve `identity.amazon-alexa` in the first canary because one associated
  Brand OS identity source is inactive, despite a current Knowledge support;
- do not approve `friction.privacy-trust` yet because locale is unresolved and evidence
  currently contains supports but no limits/contradictions;
- do not correct `benefit.task-execution`; adding `(Alexa+)` while retaining its broad
  key/scope would create a product/market mismatch;
- do not reject or merge anything; the prior duplicates are cross-kind and cannot use
  the same-kind N-to-one writer safely.

Mutation order for 69B.4B:

1. Revalidate v6 and `80/0/0/0/0` immediately before the first write.
2. Add the latency annotation; refresh; require `80/0/0/0/1`.
3. Correct `product.echo`; refresh; require counts remain `80/0/0/0/1` and the new
   display is the current pending successor.
4. Approve the seven keys above one at a time, refreshing and reconciling after every
   action. Stop on any mismatch or stale authority.
5. Final expected state: `73 pending / 7 approved / 0 rejected / 0 merged /
   1 open annotation`.

No publication, provider, successor-generation, bulk action or downstream effect is
part of this plan.

## Advance rule

The auditor reconciles both packets and performs one bounded adversarial review. With no
P0/P1 and `can_advance=true`, execute 69B.4B exactly as preregistered. Otherwise issue a
narrow correction or stop on an unjustifiable semantic decision.

Zero providers, paid jobs, product writes, publication, holdout or downstream effects.

---

# LAB-2E — isolated successor evaluation after serialized-schema correction

Operate only against one fresh local disposable Topic Evaluation Lab clone. The prior clone has a
terminal `outcome_unknown` receipt caused by Anthropic rejecting a root union schema before any
model turn, retrieval or candidate persistence. Preserve that clone and receipt; never retry it.

First move the current host receipt to a dated private archive, create a new externally anchored
clone from the fixed frozen source, and reconcile exactly `21,195` canonical memberships, `115`
historical BERTopic proposals, `11,186` assigned and `10,009` outliers. Apply Lab-only migrations
0116 and 0117 exactly once. Use only the fixed Docker Desktop local socket and a `REPEATABLE READ
READ ONLY` preflight with zero runs, authorities, outboxes, retrievals and candidates.

The code baseline is local commit `147578c`. It sends Anthropic a strict `{turn: ...}` output
envelope, which has been demonstrated with a no-corpus first-turn probe to return
`evaluation_brief`. Retrieve the credential from macOS Keychain service
`noisia.topic-evaluation-lab.anthropic` at dispatch time only; never read or print any historical
chat credential or put one in the repository. Then run exactly one successor flight with a fresh
idempotency key, hard cap USD 2.10, at most 12 turns and 24 navigations.

If terminal `completed`, reconcile run/turn/retrieval/evidence/candidate/ranking rows and audit
candidate relevance against Brand OS. Candidates remain local, pending and editable only: never
adopt, publish, serve, change Discovery Review, access UAT/production or call a second provider.
If terminal `outcome_unknown` or `failed`, stop and record the durable receipt; diagnose before
any separate successor authorization.

---

# LAB-2F — Brand OS-aligned full-evidence successor evaluation

The prior LAB-2E successor completed cleanly but produced four coherent pending candidates, below
the practical target of ten. Its durable clone and receipt are terminal evidence and must not be
reused. The diagnosis is narrow: the local orientation brief selected its historical proposals by
scope volume, not by current approved Brand OS relevance.

Operate only against one new externally anchored disposable local clone. Apply only its existing
Lab-only 0116 and 0117 migrations once, prove the frozen 21,195 / 115 / 116 / 11,186 / 10,009
authority in a `REPEATABLE READ READ ONLY` preflight, and require a pristine work count before the
one bounded provider execution. The code must use `brand_os_anchor_alignment_v1`: at most 24
historical profile rows ranked by exact normalized current approved Brand OS key anchors; each row
has <=12 sorted matching-key hints and its exact match count. Zero-match rows are incidental
coverage only, never a reserved slot.

Use the dedicated credential only from macOS Keychain service
`noisia.topic-evaluation-lab.anthropic` at execution time. One fresh idempotency key, one call
sequence, <=12 model turns, <=24 navigations and USD 2.10 hard cap. The Lab prompt must request
representative evidence for up to ten distinct non-empty-alignment clusters before finalising when
the brief supports them. Candidates remain local, pending and editable only. No UAT, production,
Topic adoption, publication, serving, Discovery Review mutation or automatic successor is allowed.

After a terminal result, reconcile all durable counts and audit relevance. Success requires >=10
coherent evidence-backed editable candidates, not merely ten generated rows. Failure must stop for
diagnosis before another separately authorized experiment.

---

# LAB-2G — scheduled Brand OS-aligned local successor evaluation

LAB-2F is terminal evidence and cannot be reused: the provider was never reached on its final
synthesis turn after eleven successful bounded tool responses. It produced no candidates and no
UAT, remote, adoption, publication, serving or production effect. The causal LAB-2G.1 correction
is committed locally as `2c78bda` and independently audited `P0=0/P1=0/P2=0`.

Operate only against one **new externally anchored disposable local clone**. Archive the former
host receipt recoverably, create the clone from the fixed frozen source, apply Lab-only 0116 and
0117 exactly once, and require a pristine REPEATABLE READ READ ONLY preflight. Reconcile exactly
`21,195 / 115 / 116 / 11,186 / 10,009` and zero existing run/turn/retrieval/candidate/queue effects
before reading any credential.

The local-only `context_first_lab_v2` provider sequence is structural: turn 0 is
`evaluation_brief`; turns 1–10 are distinct aligned `representative_mentions`, hard-capped at 3;
turn 11 is final-only. Every pre-final turn receives a fair-share input/output/cost allocation;
intermediate turns receive only <=20KiB orientation context and the final receives <=64KiB compact
evidence. One new idempotency key, <=12 model turns, <=24 navigations and USD 2.10 hard cap.
Read the dedicated credential only from macOS Keychain service
`noisia.topic-evaluation-lab.anthropic` immediately before dispatch and unset it immediately after.

Candidates remain **local, pending and editable only**. No UAT/production connection, Topic
adoption, publication, serving, Discovery Review mutation or automatic retry. Reconcile durable
run/usage/turn/retrieval/evidence/candidate/ranking state after the terminal result. If fewer than
ten coherent evidence-backed candidates result, stop for a causal no-provider diagnosis before any
separate successor authorization.

## LAB-2G terminal — read-only candidate-quality audit

The one LAB-2G flight is terminal `completed` in local clone
`noisia_topic_eval_lab_20260905_20e4b67a137a`. It produced exactly ten pending editable candidates,
ten Top-10 ranks and thirty evidence links through twelve model turns / eleven bounded retrievals;
settled cost was USD 0.396885. No additional provider request, clone, migration, UAT connection,
adoption, publication, serving or Discovery Review mutation is authorized now.

Audit the ten persisted candidate revisions read-only against current approved Brand OS. Confirm
that each is coherent, evidence-backed and recognizable/editable; distinguish strong candidates
from duplicates, broad monitoring buckets or weak/unsupported candidates. Reconcile every effect
counter remains zero. If practical success is met, record the experiment as a candidate-quality pass
and open only a separately governed provider-assisted naming/resolution/relationship **design**
gate. Do not create Topic Contracts, adopt, publish or serve Topics. If the threshold is not met,
diagnose the frozen handoff/no-provider alternative before considering a new paid flight.

## LAB-2H — local-only bounded Topic-candidate refinement control-plane design

LAB-2G has now passed its independent quality audit: the local candidate pool contains exactly
ten coherent, evidence-backed, editable candidates; it is not a Topic Contract or a serving
change. Preserve the completed clone and every immutable generated row. Do not re-run LAB-2G or
call a provider in this gate.

Design the next capability around the existing server-owned evidence operations rather than a
raw corpus export. The model may eventually receive a candidate revision plus a finite tool
surface: `candidate_context`, `brand_os_context`, `cluster_catalog`, `cluster_profile`,
`representative_mentions`, `search_cluster` with a stable server-issued cursor and page size at
most 20, and `compare_clusters`. Tool results must be bounded, sanitized, traceable and carry
opaque evidence references only. The model must never receive private mention identities,
artifact paths, SQL, credentials, unbounded corpus data or a write tool.

The intended result is a proposal, not an action: a revised display name/description, bounded
evidence citations, related candidates, and a human-reviewable merge/split suggestion. The
existing ordinary candidate editor remains the only reversible writer. The server must preserve
append-only trace, hard tool/turn/result/token/cost bounds, idempotency and optimistic
concurrency. A proposal cannot approve, reject, merge, adopt, publish, serve or create a Topic
Contract.

This gate is provider-disabled and local-only. First map the existing V2 contracts and record an
implementation/test plan in the full-evidence control-plane documentation. Then obtain a fresh
independent no-edit audit of the focused design/diff before opening a separate implementation
gate. No migration, UAT/production connection, Discovery Review mutation, provider call,
publication, serving, AuthZ/secret/infra change or automatic successor is authorized.

### LAB-2H corrective architecture

The first independent design audit found `P0=0/P1=3/P2=2`. Do not claim that
`candidate_context`, candidate-scoped navigation, session expiry, refinement trace or proposal
persistence already exist. Correct the design first: a future local implementation must create an
append-only session sealing workspace/run/candidate/exact revision/Brand OS authority/actor and
allowed source-cluster keys; it must wrap existing snapshot navigation so every retrieval is
candidate-scoped, cursor/session/expiry-bound and traced. The proposal writer must be separate
from the ordinary editor and prove citations are from that session. Fresh independent re-audit of
the corrected design is mandatory before implementation. Still zero provider, migration, UAT,
production, candidate edit, Topic Contract, adoption, publication or serving action.

### LAB-2H implementation gate

The corrected design re-audit is `PASS` (`P0=0/P1=0/P2=2`), so implement locally only the
isolated refinement control plane. A forward-only migration may be authored but must not be
applied. Sessions must have fixed server-configured expiry/clock semantics and may open only for
`pending && !adopted && !published && !serving` candidates. The dedicated navigation wrapper must
permit only sealed source clusters, use session-bound expiring cursors, cap search at 20 and
compare at exactly two, append a trace, and return sanitized opaque evidence only. The proposal
writer validates same-session trace citations and same-run related candidates; it must be directly
proven unable to invoke editorial, Topic Contract, adoption, publication or serving writers.

Run focused DB/API/core/PostgreSQL adversarial tests plus typecheck/lint/diff checks. Then a fresh
independent code audit is required before any separate provider-enabled flight-card gate. No
provider call, migration application, UAT/production connection, candidate edit, Topic Contract,
adoption, publication, serving, Discovery Review or credential action is authorized.

### LAB-2H disposable PostgreSQL proof

The local implementation re-audit is now PASS (`P0=0/P1=0/P2=2`). Use only an externally
anchored disposable local PostgreSQL Lab clone with the fixed local source authority. Apply
forward-only `0118` exactly once there; never to UAT or production. Then run fresh adversarial
proofs against the actual database triggers: candidate/session ownership and pending-state seal,
fixed server expiry, source-cluster allowlist, session cursor binding, per-result and aggregate
192 KiB trace ceiling, duplicate/idempotency conflicts, citation lineage, same-run relations and
append-only protection. Reconcile the clone ledger and prove zero candidate editorial, Topic
Contract, adoption, publication and serving writes.

Do not call a provider in this gate. Only an independent P0/P1-free PostgreSQL audit opens a
separate provider-enabled refinement flight-card design; its output remains proposal-only and
editable by an Insights Manager, never automatically adopted or served.

### LAB-2H proof audit — current gate

`0118` is now applied exactly once in the externally anchored **local disposable** clone
`noisia_topic_eval_lab_20260905_d2c972b38600`, never UAT or production. The actual trigger proof
committed one provider-disabled synthetic cohort (`1` model-turn, `1` retrieval, `1` candidate,
`1` rank, `0` provider calls, `0` settled cost) and exercised the refinement controls directly:
six maximum-size traces pass, trace seven is rejected (`23514`), session mutation is append-only
rejected (`55000`), tampered start input is rejected (`23514`) and a second proposal is rejected
(`23505`). There are zero editorial/adoption/publication/serving writes.

Run one fresh, no-edit independent audit of that exact clone and commit `ddb8cb0`. Do not run a
provider call, deploy UAT, or touch Discovery Review. A P0/P1-free audit may prepare a separate
UAT secret-configuration gate; it still does not authorize an evaluation or a refinement provider
flight. Any provider-enabled flight needs its own sealed purpose, preflight, idempotency key,
hard cap and action-time confirmation.

### LAB-2H UAT secret configuration — current gate

The post-proof independent audit is PASS (`P0=0/P1=0/P2=1`,
`can_prepare_uat_secret_configuration=yes`). The P2 is only that deliberately rejected SQL leaves
no durable row; it does not reopen code or schema work. Configure exactly one existing Keychain
secret as `ANTHROPIC_API_KEY` at **Railway UAT service scope** for `workers-uat` and `studio-uat`.
Do not write it to the repository, a project `.env`, logs, chat, a browser field other than the
Railway secret control, or production. Reconfirm immediately before transmission because saving a
service variable can restart that UAT service.

After both variables are verified masked/present, stop this gate. No provider call, migration,
candidate operation, Topic Contract, adoption, publication or serving action is authorized by
configuration. The next gate is a new, independently reviewed proposal-only refinement flight
card with its own purpose, preflight, hard cap, idempotency and confirmation.

### LAB-2H proposal-only refinement flight-card design — current gate

UAT now has the dedicated Anthropic credential at `workers-uat` and `studio-uat` service scope;
both services reconciled Online and Studio root/health returned `200`. The secret is
configuration, not authorization. `0118` remains a local-disposable Lab migration and must not
be applied to UAT by implication.

Prepare a no-provider, no-write flight card for exactly one candidate-bound refinement evaluation:
target environment, candidate/session preconditions, model and a maximum of twelve bounded model
turns, the sealed
source-cluster evidence-navigation bounds, a hard USD cap, idempotency key, output proposal-only
contract, expected zero editorial/Topic adoption/publication/serving effects, and a sanitized
receipt for expected negative-control SQLSTATEs. Run a fresh independent review of the card and
implementation target. Only a P0/P1-free review may open a separate execution decision. Do not
deploy, migrate UAT, call the provider, modify Discovery Review or create a Topic Contract during
this design gate.
