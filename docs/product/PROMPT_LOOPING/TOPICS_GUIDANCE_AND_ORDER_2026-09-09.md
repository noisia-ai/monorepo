# 32 Topics UAT: orden y efecto de las guías

Revisión sólo lectura del código 070c94e y recibos retenidos, 2026-09-09. No SQL ejecutado, textos originales, proveedores ni cambios de producto.

El orden editorial es **guided primero y open después, por identificador ASCII**, no por relevancia, tamaño ni afinidad con Brand OS. Python ajusta open primero, pero el adaptador de evidencia vuelve a ordenar `guided:<UUID>` / `open:<UUID>`; la interpretación recorre ese arreglo y omite únicamente checkpoints ya confirmados. Los UUID de grupos son identidades estables, no puntuaciones. Referencias: `services/workers/src/workers/signal-workspace-engine-evidence.ts:101`, `services/workers/src/workers/signal-workspace-engine-interpret.ts:57`, `packages/query-engine/src/signal-workspace-interpretation-v1.ts:250`.

El bundle vigente retenido contiene **177 grupos guided y 180 open, total 357**. El primer checkpoint real de cuatro unidades registra `all_lane: guided`; el cierre conserva esas cuatro y 28 nuevas Sonnet, total 32. Por el orden serial, la hipótesis respaldada por código es que los 32 son guiados; **el desglose completo aún requiere la consulta adjunta**, pues el recibo de materialización sólo conserva el total. No usar el primer fit fallido de 178+178 como denominador vigente. Evidencia en el focal: `.data/workspace-engine-2026-09-08/{uat-interpretation-padding-failure.json,uat-first-valid-interpretations.json,uat-sonnet-daily-close-receipt.json}` y `.data/workspace-engine-progress-2026-09-09/uat-first-topics-receipt.json`.

Las cuatro guías son entradas de vectores de prototipos/contexto, no necesariamente cuatro intereses ni cuatro grupos. El snapshot distingue `guide_key` y `role`; la consulta devuelve sus conteos sin textos. Para cada fragmento, una semilla positiva debe superar el centro global y el negativo de su guía: si gana, el vector guiado mezcla **75% vector original + 25% semilla** y entrega su etiqueta a UMAP mediante `y`; si no gana, conserva el vector. Los negativos vetan la semilla, no eliminan la conversación del corpus. Ambas vías ajustan el corpus completo. Referencias: `tools/signal-semantic-lab/src/signal_semantic_lab/workspace_engine.py:360` (mezcla en 418, UMAP/fit en 454–487, dos vías en 879–920), `infrastructure/db/signal-workspace-engine.ts:238` (origen de guías) y `:256` (Claude recibe contexto Brand OS e intereses completo).

El prompt declara que el contexto explica relevancia pero no puede inventar evidencia ni forzar una conversación ajena a un interés; mantiene abiertas y guiadas separadas. `coherent` significa conversación coherente, no pertinente para la marca. Los cuatro primeros recibos ya incluyen viajes a Madeira y finanzas, por lo que ruido visible es compatible con el contrato actual. **Guided no equivale a pertinencia; esto tampoco demuestra que open sea peor ni prueba duplicados semánticos.** Quedan necesarias las dos vías completas, sin censurar por la guía.

`observe-uat32-lanes.sql` es una consulta parametrizada para el observador Root dentro de `BEGIN ... READ ONLY`: catálogo actual real, conteos por vía/lifecycle, locale heredado del perfil, unidades pagadas y guías. Las columnas/tablas se contrastaron con los lectores vigentes. `coherent/mixed/insufficient` está en el mapping del artefacto privado de materialización, separado de metadata antes de persistir por `services/workers/src/workers/signal-workspace-engine-progress.ts:105`; no se puede extraer honestamente de los nombres del catálogo. La consulta no confunde lifecycle con ese estado ni locale del perfil con idioma detectado del texto. Su cotejo de recibos confirma vínculos y estado para la observación; no sustituye el validador de autoridad ni habilita ninguna operación.

No reordenar ni partir los lotes ya sellados de esta ejecución: requests, calls y recibos pagados permanecen intactos. Una priorización editorial futura debe nacer en una nueva intención/versionado; ordenar la presentación del catálogo es un cambio separado. No hace falta una rúbrica manual por grupo para corregir esta frontera de producto.

## Confirmación posterior de Root — 14:12:16 UTC

La consulta ya fue ejecutada por Root dentro del observador UAT c8f05b9 y una transacción READ ONLY. Confirmó32 Topics draft de la vía guided y32 unidades distintas en8checkpoints, todos con recibos settled coincidentes; cero unidades del catálogo sin checkpoint. Las guías son scope_positive: primary_brand1, competitor2, category1. No hay unidades open interpretadas en este catálogo. La hipótesis condicional del memo anterior queda resuelta por esta evidencia posterior; se conserva el análisis previo.

Locale del perfil: markets[MX], timezoneAmerica/Mexico_City, languages[] y primary_locale null. Esto no prueba por sí solo la causa de las definiciones mixtas de idioma. El orden UUID y la influencia de guías explican la mecánica, no la pertinencia ni la calidad.

Recibo privado focal: .data/overnight-final-product-2026-09-09/uat-studio-product-receipt.json; SHA ff7e4d3e2c7ef8ac593d5fb445ade88a5bec1fd9cf2e12fb17bef631ed4df37f. Worker corroboró los mismos agregados a14:15:04UTC. Ningún envío, permiso o cambio de datos ocurrió.
