# Editor de Topics consolidados — entrega UAT, 25 septiembre 2026

## Resultado

Se añadió una edición de nombre y definición para cada concepto de la propuesta consolidada. La operación toma la revisión validada esperada, copia **todas** las decisiones por grupo y la metadata de prioridad, crea una revisión sucesora completa y prepara su snapshot gratuitamente. Signal conserva la revisión publicada hasta que el usuario selecciona y activa la nueva. Una edición no llama a Claude ni Voyage, no reclasifica menciones y no modifica el censo BERTopic.

Cuando Signal ya usa una consolidación, Topics distingue explícitamente la capa publicada de la lista previa de intereses y resultados parciales. La edición y la selección quedan en la misma sección de Topics, en español e inglés.

## Evidencia y límites

- Corte focal en `codex/noisia-alexa-atomic-census-2026-09-25`, sobre Studio UAT `8f8bf66` activo.
- Dos pruebas de contrato de edición con DB simulada y 19 pruebas de interfaz pasan. Typecheck 11/11, lint 11/11 y diff-check pasaron al cierre. La compilación de Next llegó a código compilado, pero la recolección de páginas se detuvo porque este checkout no tiene `KINDE_ISSUER_URL`; no es un fallo demostrado del cambio.
- La escritura se ensayó en PostgreSQL real privado, con datos sintéticos y rollback físico. SQL0185 se aplicó una sola vez en UAT y se verificó después; el editor se entregó en la rama UAT. Alexa+ continúa con 2/42 lotes y sin catálogo consolidado final, así que todavía no existe un caso real para pulsar el editor.
- La conexión privada de dev-test está recuperada. Una consulta booleana anterior se interpretó erróneamente como ausencia de 0174/0181. Una lectura posterior explícita en el contenedor confirma `current_database=noisia_dev_test`, usuario `noisia_dev`, **299 tablas públicas**, `signal_topic_consolidation_revisions`, `signal_topic_consolidation_decisions` y `prepare_signal_topic_consolidation_snapshot_v1(uuid,uuid,uuid,text)` presentes. El checkout privado de Signal ya conserva el sello de 299 tablas tras su upgrade; **no repetir ese upgrade**. Ninguna de estas lecturas alteró UAT.
- El runner privado específico exigió ese sello, aprobación distinta, base vacía y transporte de proveedores bloqueado; reutilizó el fixture de Brand OS y dos grupos sintéticos existente. Creó la revisión inicial, sirvió un concepto, editó el otro, comprobó decisiones/prioridad y activación, y terminó con rollback físico más verificación de vaciado. Recibo Railway `499d468b-3c04-40b7-9014-06e1f3abef33`: `status=passed`, `provider_transports=0`, `physical_rollback=true`, `post_rollback_empty=true`, 299 tablas antes/después. Autodeploy del runner quedó deshabilitado; rama y comando de solo lectura restaurados y aprobación específica desactivada.
- El ensayo destapó SQLSTATE `42702` en `prepare_signal_topic_consolidation_snapshot_v1`: la variable PL/pgSQL `source` chocaba con una columna de CTE. SQL0185 renombra únicamente esa variable a `source_binding`; primero se ensayó dentro de la transacción revertida de dev-test. En UAT, el runner con aprobación específica y SHA `49b5162506f9fef4127c58a4310aa172cafea8975b90654319e3c2cefeb96a92` confirmó función anterior y 301 tablas; el apply respondió `committed=true` y la verificación independiente encontró función corregida. Tabla, owner, ACL y conteos de revisiones/snapshots conservaron su estado. **No repetir SQL0185.**
- El Worker UAT construyó la revisión de producto `9428066` y Railway la marcó exitosa. Studio se había mantenido deliberadamente en `8f8bf66` durante el cambio SQL; después se restauró su autodeploy y se empujó `823d57f` (documentación del contrato del editor dentro de `apps/studio`) para disparar la compilación exacta del código nuevo. El despliegue Studio `7e80a11c-aaa6-4b86-989b-42d0defa3fbd` pasó compilación, tipos, lint y healthcheck `/api/health`; Railway lo mostró `Active`. El código nuevo quedó entregado en UAT.
- La UI real de Alexa+ consultada antes del nuevo despliegue mostró 1,652/1,652 dossiers numéricos, revisión Claude detenida en 2/42, 36 Topics parciales del catálogo anterior y uno seleccionado con 67 menciones. No se presentó ese resultado parcial como consolidación final.
- Tras activar Studio, se recargó la página real de Topics de Alexa+: abre sin error, conserva 1,652 dossiers, 2/42 lotes y USD 1.585632 confirmados/0 reservado/0 ambiguo, mantiene 36 Topics parciales y la asociación anterior de 67 menciones. El bloque de publicación declara que espera la propuesta. No se pulsó un editor inexistente en datos reales ni se creó una revisión ficticia de Alexa+.
- Signal Alexa+ también abre tras el despliegue: muestra el Topic previo «Acceso anticipado a Alexa+» con 67 menciones y declara 36/1,652 grupos interpretados y clasificación parcial. La nueva revisión consolidada aún no existe en esta marca; no se probó una selección consolidada real.

## Siguiente acción

La prueba real de editar/seleccionar una revisión consolidada requiere terminar Alexa+; con permiso de gasto nuevo y vencimiento explícito, reanudar la misma revisión (40 lotes), evaluar Noise/uniones/separaciones y entonces recorrer edición/selección en UI con datos reales. Después confirmar Signal y una segunda carga real, sin repetir el fit anterior.
