version: "3.8"

services:
  postgres:
    image: pgvector/pgvector:pg16
    profiles: ["local-only"]
    environment:
      POSTGRES_DB: noisia_studio_dev
      POSTGRES_USER: noisia
      POSTGRES_PASSWORD: localdev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  postgres-smoke:
    image: pgvector/pgvector:pg16
    profiles: ["migration-smoke"]
    environment:
      POSTGRES_DB: noisia_migration_smoke
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "55432:5432"
    tmpfs:
      - /var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

  bull-board:
    image: deadly0/bull-board:latest
    ports:
      - "3030:3000"
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      - redis

volumes:
  postgres_data:
  redis_data:
